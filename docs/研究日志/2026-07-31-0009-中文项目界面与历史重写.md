# 2026-07-31 / 0009 / 中文项目界面与历史重写

## 目的

项目面向中文用户公开发布，因此 Git 提交说明、README、命令行帮助和项目元数据统一使用
中文。代码标识符、序列化字段、子命令名称和标准许可证保持原样，避免破坏 API 与格式兼容。

## 修改

- 重写 README，说明当前能力、安装步骤、交互入口、目录结构和正式存档安全边界。
- 将 `pyproject.toml` 的项目描述与贡献者名称改为中文。
- 将 `tc-save --help` 及交互确认提示改为中文。
- 重写仓库已有提交的说明，并使用 `--force-with-lease` 更新远端 `main`。

## 边界

本次只修改项目展示文本和 Git 历史元数据，不启动游戏、不读取或写入正式存档，也不改变
任何候选电路的二进制内容。
