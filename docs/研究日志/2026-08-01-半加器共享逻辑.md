# 半加器共享逻辑

## 2026-08-01 / 提交：优化：以共享与门重构半加器

## 灵感与修改点

半加器的 Carry 本来就是 `A AND B`，而 XOR 可以写成 `(A NOR B) NOR (A AND B)`。
新方案让 Sum 和 Carry 共用同一个 AND，删除原生 XOR，形成三个门、两级关键路径。

候选通过四组真值表、v15 往返、固定端口永久 ID、全引脚连接和无环检查。该结果与
2026-08-01 单关榜公开榜首 `3/2/6` 一致，但仍需游戏内验收。
