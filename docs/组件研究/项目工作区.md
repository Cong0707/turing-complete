# 项目工作区

## 2026-07-31 / 提交：功能：初始化离线存档实验室

## 提交目的

建立一个不依赖游戏进程的独立项目，用来解析、验证、版本控制并最终写回最新版
Turing Complete 存档。此提交不改动正式存档。

## 证据与判断

- 最新版正式存档根目录为 `C:\Users\cong\AppData\Roaming\Turing Complete`。
- 当前实际方案位于 `schematics/<level>/<方案名>/circuit.data`；同级的旧 `.tc`
  文件不是本轮游戏写入的 v15 容器，不能被误当作当前方案覆盖。
- 关卡排行榜的能耗为 `gate * delay`；编程关还乘以周期。排行榜总分使用各关
  能耗的 `log2` 之和。因此候选会同时记录门数、延迟、能耗和（适用时）周期，
  不把“最低延迟”错误地当作唯一目标。

## 修改点

- 初始化 Git、Python venv、零第三方依赖的打包配置。
- 约定所有自动生成的关卡数据均进入 `examples/`，每关独立目录。
- 写回设计采用“游戏未运行检查 -> 同目录临时文件 -> 原子替换 -> 重新解码校验”。
  不创建持久备份，符合本次不保留备份的要求。

## 下一步

完成 v15 二进制模型、导出清单和每个主线关卡目录生成，再解析静态 `campaign/`
资源及排行榜 Pareto 目标。

## 2026-07-31 / 提交：修复：按组件与关卡重构研究记录

- 工具文档移动到 `docs/组件研究/`，文件名使用中文工具组件名。
- 关卡方案继续放在各自 `examples/<level>/history/`，文件名使用中文关卡名。
- 同一组件或关卡的后续研究追加到原文件，并以“日期 / 提交主题”划分，不再创建按日期
  命名的总研究日志。
- 子代理后续只负责一个明确的元件或关卡，结论回到该元件或关卡自己的 Markdown。
