# RNG 开关代数与相位反转研究

## 2026-08-03：提交“研究：构造无 RAM 的 RNG 九延迟候选”

### 结论

现有已经实机通过的 `396 / 10 / 66` 编码状态网络不需要改动随机数核。第十层延迟只来自加载相位控制的极性：

```text
phase Delay(4)
  -> NOT(1)
  -> Architecture Input
  -> mode OR(1)
  -> 两层 XOR(4)
= 10
```

把相位 Delay 的初值设为 `1`、下一状态固定为 `0`，然后交换输入、输出端口的控制极性：

```text
phase.out      -> Architecture Input.control
NOT(phase.out) -> Architecture Output.control
```

即可得到正常元件、无 RAM 的 `396 / 9 / 66` 候选，预测能耗为：

```text
396 * 9 * 66 = 235224
```

相比参考成绩 `431 / 9 / 66 = 256014` 少 `35` 门、少 `20790` 能耗。

### 时序证明

加载周期 `tick 0`：

- `phase=1`，唯一输入端口开启，唯一输出端口关闭；
- 编码状态寄存器捕获 `T(seed)`；
- 相位寄存器捕获常量 `0`。

运行周期 `tick 1+`：

- `phase=0`，输入端口进入高阻态，输出端口开启；
- 编码状态按 `B` 矩阵持续更新；
- 输出解码为 `C(state)=A^n(seed)`。

最长有效路径为：

```text
state -> state:       Delay 4 + mode OR 1 + 两层 XOR 4 = 9
phase -> seed state:  Delay 4 + mode OR 1 + 两层 XOR 4 = 9
state -> output:      Delay 4 + mode OR 1 + 两层 XOR 4 = 9
phase -> output enable: Delay 4 + NOT 1 = 5
```

原方案的 `NOT -> Architecture Input` 不再位于加载数据路径中，因此结构延迟从 `10` 降到 `9`，门数和周期数不变。

### 离线验证

生成脚本：

```text
.research/rng_switch_algebra/derive_phase_reversed_candidate.py
```

候选与证书：

```text
.research/rng_switch_algebra/phase_reversed_396_9_66.data
.research/rng_switch_algebra/phase_reversed_396_9_66.json
```

候选 SHA-256：

```text
29b355cefb02c1f8f9a4cccb051175e185fcbcdaca6ff321cb45447b60a09b05
```

已通过：

- v15 编码、解码往返；
- 155 个受支持元件、479 个引脚全部连通；
- 无多驱动、无悬空网络、无位宽错误、无组合环；
- 保守几何检查和当前 sprite alpha 检查；
- `0` 个元件重叠单元；
- `0` 条内部导线穿过元件；
- `0` 条导线穿过非目标引脚；
- 256 个固定可复现种子，每个 66 tick，共 16896 tick；
- 每个种子的加载状态、65 个输出以及每拍编码状态均与 xorshift32 数学模型一致。

复现命令：

```powershell
.\.venv\Scripts\python.exe .research\rng_switch_algebra\derive_phase_reversed_candidate.py
```

离线验证只能证明逻辑、布线和按已审计成本得到的结构预测。最终 `396 / 9 / 66` 必须由游戏本体和服务端重算确认。

### 被排除的局部 Switch 路线

`search_critical_band.py` 对规范网络中造成第三层 XOR 的 `y12..y26` 做了精确 RC2 优化，并同时计入：

- XOR2：`3 gate / 2 delay`；
- 四 Switch XOR3 数据面：`8 gate`；
- 可全局共享的 AND/NOR 控制；
- 固定 XOR2 与新 XOR2 免费暴露的内部 AND/NOR。

精确最优结果记录在 `critical_band_optimum.json`：

```text
固定 34 XOR2                 = 102 gate
临界带最优新增成本           = 198 gate
组合核                       = 300 gate
加现有 198 gate 外壳         = 498 / 9 / 66
```

因此，只重综合临界输出带的 cancellation-free XOR2/Switch-XOR3 路线明显弱于相位反转候选，不应继续在这个局部模型上消耗搜索时间。

此前出现的 `shared_control_233` 也不是已知可行前沿：它属于 cancellation-free 两层 XOR2/四 Switch XOR3 模型，而既有精确 RC2 结果已经证明，即使所有 AND/NOR 控制免费，组合核仍至少为 `262 / 4`。求解器的 `UNKNOWN(timeout)` 不能视作存在 `233` 门构造的证据。

### 后续方向

在游戏确认 `396 / 9 / 66` 之前，优先保持此候选结构不变。若继续压门，应研究跨全部 32 个输出与 32 个状态的联合基变换、允许消去的非局部 Switch 网络，或者让同一组高阻控制同时承担加载选择和 XOR 数据面；局部修补规范 xorshift 的 15 位临界带已被精确结果否定。
