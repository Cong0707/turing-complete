# Hub79 主线物化检查点

## 2026-08-03 恢复后完成

冻结点恢复后，Switch enable 采用“先向正 y 逃逸一格，再转入左侧独占 trunk”
的走线。候选已连续两次确定性生成并通过全部离线门槛：

```text
candidate: hub79_bootstrap/candidate/circuit.data
candidate SHA-256: cd56a9245cbd12e83673eaf51665807583871bb61337dc4c186331919c22211a
machine certificate: hub79_bootstrap/machine_certificate.json
semantic proxy: hub79_bootstrap/semantic_proxy.circuit.data
score: 154 gate / 4 delay / 616 energy
components/wires: 116 / 254
truth: 131072/131072
Z multi-driver networks: 18
packed conflicts: 0
global/Sum/Cout depth: 4/4/4
Switch enable connected: 49/49
sprite unsupported/overlap/wire collision/interior pin contact: 0/0/0/0
v15 round trip: passed
```

仅保留公开拓扑中有意存在的 `kind111.in0` 隐式零输入和未使用的
`kind109.out0`。candidate 的 width mismatch、undriven、sinkless、cycle 和不安全
非 Z 多驱动网络均为 0。以下“冻结状态”与失败过程保留为历史记录，不再代表当前
候选状态。

## 2026-08-03 冻结状态

本检查点响应“立即暂存全部进度”的要求。冻结期间没有启动游戏、没有修改
`examples/byte_adder/candidate`、没有写正式存档、没有操作 Git，也没有创建子代理。
检查时，本目录没有残留 Python 进程。

## 已完成并可复现

### 通用 Byte Adder 布局链路

`route_byte_adder.py` 已实现以下链路：

1. `LogicNetwork -> TuringNetlist`；
2. campaign immutable `A/B/Carry in/Output/Carry out` 接口；
3. kind 17 U8 拆分器与 kind 16 U8 合并器；
4. 当前组件精灵 alpha 和全部非端点引脚作为障碍；
5. v15 编解码往返、连通性、真实精灵几何和完整真值审计。

参考 ripple 电路已经完成一次完整复现：

```text
gate/delay: 79/18
components: 87
wires: 170
vectors: 131072/131072
SHA-256: cc2e4e7ae8b53e35bbaab16be69abf43603ca74369e4092b205fffb73ce83a66
observed RSS: about 25 MiB
```

对应产物为 `ripple_reference.circuit.data`。它只证明生成、布局和审计链路，
不是排行榜候选。

### Hub79 原始拓扑证据

源文件：

```text
examples/rng/research/archive/rng_public_artifacts/hub-79-adder/main/circuit.data
SHA-256: 79615b3a78e4824f8b4c4973c69cc76d972745cd5283183d698b1a14206e422d
```

已读取并复核的原始事实：

- `154 gate / 4 delay / 616 energy`；
- 116 个元件、430 条导线；
- 49 个 Bit Switch、12 AND、2 NAND、16 OR、25 NOR、1 NOT；
- A/B 为 U8，Cin 只使用最低位；Sum 为 U8，Cout 只使用最低位；
- 公开冻结审计覆盖 131072 个输入，Z/多驱动冲突为 0，输出及全局深度为 4。

## Hub79 物化脚本当前能力

`hub79_bootstrap.py` 已实现：

- 将 3 个 kind 79 Foundry 输入替换为 campaign immutable A/B/Carry in；
- 将 2 个 kind 81 Foundry 输出替换为 campaign immutable Output/Carry out；
- campaign 的 Cin/Cout 使用真实 U1 宽度，消除公开 Custom 外壳中的两个接口位宽不匹配；
- 保留全部内部逻辑、Switch enable、高阻输出和端点网络；
- 将 111 个内部元件按冻结语义深度重新放置；
- 本地补充已安装的 kind 109 `com_splitter_bit_2.png` 精灵映射；
- 构造 campaign 语义代理，计划复用冻结位并行求值器证明完整真值、Z 冲突和全局深度；
- 清除 Custom 专用 `custom_id/hub_id/design`，生成非 Custom v15 容器。

## 冻结时尚未完成（历史）

冻结时没有生成：

```text
hub79_bootstrap/candidate/circuit.data
hub79_bootstrap/machine_certificate.json
```

当时 Hub79 版本不是可部署候选；当前状态以上面的“恢复后完成”为准。

最后一次已执行的版本采用每个逻辑网络一条外部通道。v15 编码已经走到几何审计，
但 Switch 的 center-column `enable` 引脚从负 y 方向进入时会穿过两个 Switch body
单元，审计正确拒绝了该布局。冻结前最后一个源码改动将 center pin 的 trunk 改到
元件左侧：

```python
trunks[pin.position] = component.position[0] - 3 - rank
```

该修复尚未重跑，不能声称几何、语义或机器证书通过。恢复工作时应从这里开始：

```powershell
.\.venv\Scripts\python.exe `
  .\.research\byte_adder_builder_layout_agent\hub79_bootstrap.py
```

只有输出 `machine_certificate.json` 且同时满足以下条件，才可交给父任务写共享候选：

- 131072/131072 真值正确；
- packed Z 冲突为 0；
- 全局、Sum、Cout 深度均为 4；
- 成本分解合计 154；
- v15 往返相同；
- unsupported、component overlap、wire collision、interior pin contact 全为 0。

## 冻结时正式存档（历史）

```text
C:\Users\cong\AppData\Roaming\Turing Complete\schematics\byte_adder\Default\circuit.data
SHA-256: 6acfc377ce6fa1a07aa9d91732217ecc1ac4c7d329097be102c49eaa14bfdb6f
```

恢复研究后已完成全部审计。根任务随后按用户的直接覆盖要求，把
`hub79_bootstrap/candidate/circuit.data` 写入正式存档。当前正式存档与仓库候选的
SHA-256 均为：

```text
cd56a9245cbd12e83673eaf51665807583871bb61337dc4c186331919c22211a
```

该候选不含 kind 30 `com_add`，等待实机验收 `154/4/616`。

## 精确冻结清单

本目录受 `.gitignore` 的 `.research/` 规则忽略。若父任务决定提交，必须逐项
`git add -f`，不得使用 `git add -A`：

```text
.research/byte_adder_builder_layout_agent/README.md
.research/byte_adder_builder_layout_agent/route_byte_adder.py
.research/byte_adder_builder_layout_agent/ripple_reference.circuit.data
.research/byte_adder_builder_layout_agent/hub79_bootstrap.py
.research/byte_adder_builder_layout_agent/2026-08-03-Hub79主线物化检查点.md
.research/byte_adder_builder_layout_agent/hub79_bootstrap/candidate/circuit.data
.research/byte_adder_builder_layout_agent/hub79_bootstrap/semantic_proxy.circuit.data
.research/byte_adder_builder_layout_agent/hub79_bootstrap/machine_certificate.json
```

不要提交 `__pycache__`。
