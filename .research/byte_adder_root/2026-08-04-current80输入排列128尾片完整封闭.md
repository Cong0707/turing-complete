# 字节加法器 current80 输入排列 128 尾片完整封闭

## 结论

本轮继续使用已验收 `80 gate / 7 delay / 560 energy` 的 21 输入、9 输出 reachable residual，
完成确定性输入排列序列的尾部分片：

```text
permutation range = [576, 704)
permutations      = 128
output phases     = 13
static tasks      = 128 * 13 = 1664
D6 recipes        = 4
timing results    = 1664 * 4 = 6656
```

两阶段都正常结束，且没有 worker 或 recipe error。完整 summary 中的最优结果为：

```text
static best       = 85 / 7 / 595
timing best       = 85 / 7 / 595
delay <= 6        = 0
energy < 560      = 0
ABC D6 unmet      = 6656 / 6656
```

因此本分片没有正式覆盖资格。正式存档继续保持用户已经实机验收的 `80/7/560`，本轮没有
启动游戏，也没有读取或写入正式存档。

## 搜索契约

输入仍是权威 current80 DAG 导出的 `.type fr` reachable relation：

```text
boundary inputs       = 21
outputs               = 9
unique reachable rows = 23328
```

permutation 生成器由四个固定排列和 `random.Random(seed).shuffle` 的确定序列组成。本分片
显式覆盖 `[576,704)`，并与 13 个前期筛选出的 output phase 组合。每项都经过 Espresso
最小化、原输入顺序恢复、23328 个 care point 重放及 ABC 面积映射。

静态阶段：

```text
start UTC       = 2026-08-03T23:40:16Z
end UTC         = 2026-08-04T00:46:19Z
exit/class      = 0 / solver_exit
completed       = 1664 / 1664
errors          = 0
workers         = 4
```

D6 follow-on 在 static summary 完整性检查通过后，对每个候选执行 `plain_d6`、`dch_d6`、
`dc2_d6`、`resub8_d6` 四个 timing recipe：

```text
start UTC            = 2026-08-04T00:46:19Z
end UTC              = 2026-08-04T00:46:58Z
exit/class           = 0 / solver_exit
completed candidates = 1664 / 1664
timing records       = 6656 / 6656
worker/recipe errors = 0 / 0
```

## 跨平台审计修复

第一次在 Ubuntu 原始目录直接执行独立审计时，8320 个映射都被同一工具错误拒绝：权威
metadata 中的 `source` 是 Windows 路径，而 Linux 的 `pathlib.Path.name` 不识别反斜杠，
把整个 `D:\\...\\byte-adder-hybrid-phasefold-g80-d7.json` 当成 basename。

`graft_abc_mapped_residual.py` 已改为先把反斜杠归一化为 `/`，再提取 basename。修复后先在
Ubuntu 对 `perm606/phase086` 做完整跨平台探针，得到：

```text
metrics                         = 85 / 7 / 595
full truth rows                 = 131072
mismatch/conflict assignments   = 0 / 0
primary-output Z counts         = all 0
mapped/shell/generated dead     = 0 / 0 / 0
deterministic rebuild identical = true
```

该修复只改变路径比较，不改变 DAG、BLIF、逻辑语义或成本模型。

## 独立抽样与导入审计

仓库保留完整 summaries、运行记录、合并日志，以及并列能耗最优代表
`perm606/phase086`，不复制 1664 个候选目录。独立审计器完成：

1. 复刻确定性 permutation 生成器并检查 `[576,704)` 的全部 record key；
2. 检查 1664 个 static record 与 6656 个 timing record 的成本、延迟、能耗和 recipe 覆盖；
3. 对代表的 `input.blif` 重放全部 23328 个 care point 与 output phase 恢复；
4. 检查原始映射、四个 timed 映射及其日志的记录 SHA；
5. 对一份 static 和四份 timing 映射分别执行权威 residual importer 双构建。

最终审计终态：

```text
status                  = accepted
static records          = 1664 complete
timing records          = 6656 complete
local selected/accepted = 5 / 5
rejected                = 0
summary hit energy<560  = 0
import hit energy<560   = 0
```

关键 SHA-256：

```text
38af89e4b0ddaa09c0656d76f7cf0bcd49e021fb5650bb1721d816c2b9425d3c  static summary
76b6ea3bba08fb0d534cc05ea036c33d7a8585264868e2b23f61842e4364c724  timing summary
b62b3b66aa1e6be335d8d2d8c33122887ed4377579675647f17f3e39a5551cf2  independent audit
53acd85edd309361e19f8d24e7ce1e71e2e4b0cd50d4bc1af6f549a0d397dc35  selected input BLIF
```

## 边界与后续

该终态只封闭 `[576,704)` 的确定性输入排列、指定 13 个 output phase、Espresso 和四个
ABC timing recipe，不是任意布尔网络、Switch/Z 网络或字节加法器的全局下界。后续搜索
继续集中于精确 Switch/Z 联合切口和 `101/5/505`、`102/5/510` 高 Switch 组成空间。

规范证据：

```text
.research/byte_adder_root/abc_residual_current80/care_pla/input_permutation_576_703x13/
.research/byte_adder_root/input_permutation_576_703x13_SHA256SUMS.txt
```
