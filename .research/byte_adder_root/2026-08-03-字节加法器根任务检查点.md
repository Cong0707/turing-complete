# 字节加法器根任务检查点

## 2026-08-03 / 转入字节加法器

### 已确认基线

- 随机数生成器的正常无 RAM 方案已经实机验收为 `402/9/67`，本阶段不再修改。
- Byte Adder 的官方测试覆盖 `U8 x U8 x U1` 全部 `131072` 个输入，不能依赖样本缺口。
- 公开成绩前沿为 `56/18` 至 `154/4` 的一组权衡点；当前已经取得并独立复算的完整门级端点是 Hub 88 的 `56/18` 与 Hub 79 的 `154/4`。
- 当前账号尚无经过实机认证的 Byte Adder 成绩。仓库与正式存档中的单个 `com_add` 文件只是 `103/5` 待验收候选，文件头不等于服务端认证成绩。

### 根任务产物

- `export_hub79_semantics.py`：从 Hub 79 原件导出语义网表。
- `hub79_semantic_netlist.json`：导出的中间语义资料。
- `build_hub79_campaign.py`：把 Foundry 接口转换到主线 Byte Adder 接口的实验构建器。

### 当前边界

`build_hub79_campaign.py` 的第一次全向量验证因逐向量实现过慢而超时，后续进程也没有生成
`hub79_campaign_154x4.data` 或证书。因此，本目录目前只保存可继续开发的转换代码和中间资料，
**没有可部署的 Hub 79 主线候选**。不得据此覆盖正式存档。

后续必须完成以下项目后才能部署：

1. `131072/131072` 全真值验证；
2. 18 条三态总线的 0/1 冲突审计；
3. 全局到达时间与真实成本复算；
4. v15 编解码往返；
5. 导线不穿越元件实体和非端点引脚的真实精灵几何审计。

### 已排除误判

- Hub 79 的逐总线局部替换不能保持输入到达时间；真实前沿是 `154/4`、`142/5`、`126/6`、`117/7`。
- 常规前缀结构的当前最好结果是 `75/10/750`，未击败公开第一名 `103/5/515`。
- 由 `154/4` 简单按位宽缩放或串接，不能直接推出 `103/5` 的完整 DAG。

