# 字节加法器远程计算记录

## 2026-08-04 / Ubuntu 工作站初始化

授权工作目录：

```text
/root/congProjects/turing-complete-works
```

远程节点只用于离线 SAT、Z3、枚举和真值复算，不读取或修改 Windows 正式存档，
不启动 Turing Complete。所有生成器与任务配置先保存在本机仓库，再上传到远端独立
任务目录；结果取回本机后还要经过现有物化器和全链验证器，不能仅凭远程求解器输出
覆盖存档。

### 资源边界

初始化时节点为 32 线程、60 GiB 可见内存，已有长期服务约占 35 GiB，available 约
24 GiB。首批 sweep 固定为：

```text
并发求解器：8
单进程虚拟内存上限：2048 MiB
CPU affinity：0-23
nice：5
单任务外层硬超时：7200 秒
```

`remote_sweep.py` 为每个值建立独立进程组；超时后先 TERM，30 秒后仍未退出则 KILL。
该外层限制不依赖 CaDiCaL 或 Glucose 是否响应 Python timer，避免再次留下超时孤儿。

### Python wheel

远端直连 PyPI 速度过低且连接重置后出现哈希不一致，因此没有禁用哈希校验。本机从
官方 PyPI 下载指定 Linux wheel，校验后通过 SSH 上传，再使用 `--no-index` 安装：

```text
python_sat-1.8.dev24-cp312-cp312-manylinux_2_24_x86_64.manylinux_2_28_x86_64.whl
201d77637ff7f784db2be7d8ef03cd843c12d3143718c1e53d148381ba9e93ee

z3_solver-4.15.4.0-py3-none-manylinux_2_17_x86_64.manylinux2014_x86_64.whl
7e103a6f203f505b8b8b8e5c931cc407c95b61556512d4921c1ddc0b3f41b08e

six-1.17.0-py2.py3-none-any.whl
4721f391ed90541fddacab5acf947aa0d3dc7d27b2e1e8eda2be8970586c3274
```

远端已完成 `pysat 1.8.dev24`、`z3 4.15.4` 导入，并分别用 `cadical195`、
`glucose42`、`minisat22` 解一个单子句实例通过自检。

固定依赖及哈希同时写入 `requirements-remote.txt`。从已上传 wheel 离线重建时使用：

```bash
.venv/bin/python -m pip install \
  --no-index --find-links shared/wheels --require-hashes \
  -r requirements-remote.txt
```

## 2026-08-04 / 首批任务：Q34、C5、C7 联合残差

合法 `95/6` 的 Q 相中段残差为 18 门。若能在相同真实三态接口下将 `C5@4,C7@4`
联合重综合到 16 门，则完整电路成为 `93/6/558`，严格低于当前正式 `80/7/560`，应在
全链验证后立即覆盖存档。

任务配置 `q34_c5_c7_g16_components.json` 对 `components=1..16` 全部检查，使用严格
weighted gate bound 16、两路输出最终可用 Z 表示假 carry、真实 C3/V56 driven mask、
完整 BUS conflict 和 physical-net partition 约束。内部 solver timer 设为 0，仅采用
`remote_sweep.py` 的外层硬超时。

远端产物按以下层级保留：

```text
jobs/q34_c5_c7_g16/
  logs/<components>.log
  run-results/<components>.json
  results/q34_c5_c7_g16_n<components>_glucose42.json
  q34-c5-c7-g16-summary.json
```

远端 sweep 已于 2026-08-04 完成。`components=1..16` 全部返回 `UNSAT`，16 个逐项
JSON 的 SHA256 均与汇总文件记录一致；检查到的公共参数为：

```text
interface = q34_c5_c7
gate_bound = 16
max_delay = 4
physical_nets = true
output_deadlines = [4, 4]
solver = glucose42
```

取回后的完整证据位于 `results/q34_c5_c7_g16/`，包括逐任务命令、日志、原始求解
JSON 和 sweep 汇总。汇总文件 SHA256：

```text
4aa1cffa81c6ddb52fa386cc7baaf72884d7716b8cfd876e308244a95bd1bd2f
```

因此，在该 paid-Q34、A56/V56、C3 到达时间和真实三态接口保持不变的局部模型内，
residual 16 已严格封闭；这不是全局六延迟加法器下界，后续必须跨 paid leaves、C3 或
相位边界重构。

## 2026-08-04 / 第二批任务：慢 C7 的 S7、C8 尾部

在合法 95/6 Q34 基线中，慢 C7 重构与六门 C8 宏的精确完整账本为：

```text
S0..S5 基座                         60
bit6/bit7 G、P、Q 叶                 6
T、D、A                              3
C7 Switch-BUS                        4
D6 S6 宏                             8
C8 六门宏 + S7 六门残差             12
合计                                93 gate / 6 delay / 558 energy
```

若残差证书的实际成本低于 6，则完整结果还会继续下降。旧搜索脚本顶部提到的
`97 -> 96` 是过时基线，不能用于当前总账。

本机已枚举 2000 个六门 C8 网络并去重成 123 类普通相位。`S7` 追加成本 6 的
`4 components / 2 Switch`、`3 components / 1 Switch / 1 XOR`、
`2 components / 2 XOR`、`4 components / 1 XOR`、`3 components / 3 Switch`
已在本机对全部 123 类严格 UNSAT。`5 components / 1 Switch` 的 `0..31` 分片留在
本机继续运行。配置 `s7_c8_tail12_remaining.json` 只上传尚未重复覆盖的 14 个任务：

```text
5 components / 1 Switch：32..122，共 6 段
6 ordinary components：0..122，共 8 段
```

第二批最终使用 14 并发、每进程 1536 MiB、CPU 0-31、nice 0、单分片外层硬超时
1800 秒。配置 SHA256：

```text
01557f89ae12a360607af610982b5c24d13ff38af22248f3c141cbac5bb5d474
```

首轮实际求解已经得到 UNSAT，但旧 runner 没有预建脚本参数指定的 `results/` 目录，
导致 14 个进程在写终态 JSON 时失败。该轮没有被当作证据。修复 runner 后原样重跑，
结果为 14/14 `UNSAT`、0 timeout、0 runner error、14 个输出哈希全部匹配。取回证据：

```text
results/s7_c8_tail12_remaining/
summary SHA256 = 7d3ca8bf84038375312764d0ca023e52f3e29fe63134f0ad7235f36d75c8e552
```

结合本机 `0..31` 与其它成本分解，这封闭了“已枚举 2000 个 C8 网络去重为 123 个
ordinary phase 类”内的 tail=12 搜索。`d6_c8_g6_networks.json` 自身没有声称穷尽任意
六门 C8 网络，所以不能把该结论外推成全局 C8 下界。

## 2026-08-04 / Runner 修正与满载调度

`remote_sweep.py` 增加三项可复现性修正：

- 启动子进程前自动创建 `--output` 的父目录；
- 保留原始 return code，但以完整 `sat/unsat` JSON 判定任务成功，兼容用返回码 1
  表示“未找到 SAT”的研究脚本；
- `nice` 由任务配置显式指定，并写入 sweep 汇总。

当前 runner SHA256：

```text
c0d38b185890e0f02adfc643a08742b641df958bae48d70dec31a886f2257592
```

工作站按 32 个独立求解分片满载，而不是在同一搜索上重复开进程。已发射的互补配置：

```text
q12_q34_retime_tier0.json  17 个无 XOR 分片，16 并发
q12_q34_retime_tier1.json  19 个单 XOR 分片，4 并发
q12_q34_retime_tier2.json  40 个多 XOR 分片，5 并发
s7_c8_direct_tail11.json    2 个联合尾部构型，2 并发
```

配置 SHA256：

```text
q12_q34_retime_tier0.json 45a22ca002f4bd2af23906bd48f8d314ac6f8fee7fa423ef180a87edcbc97a9b
q12_q34_retime_tier1.json e20cce760f6b213ee236072e798a59904e3c807efce6b22fbc2c49a702165184
q12_q34_retime_tier2.json 577a8713410882a29300773a6a7adc6ecdd85edd5ddbfeff0aa9e28299911b8e
s7_c8_direct_tail11.json   f8474f3ef8a4f84abb5bb79f0587e5b5aacf9b380c7580e5e69abbb47fcf67a7
```

Q12/Q34 三批共同覆盖 residual cost 21..23、components 10..16、XOR 0..3 的全部
76 个 manifest 分片。任一 SAT 给出 `C3/C5/C7@4` 后可形成 `<=93/6`。direct-tail11
任一 SAT 则形成 `92/6/552`。这些仍是超过正式 560 的中间目标；最终榜一目标保持
`energy < 515`，即 `<=102/5`、`<=85/6` 或其它更优 Pareto 点。
