# 字节加法器

## 2026-08-01 / 候选构建

### 思路

当前版本的原生 U8 加法器同时提供 `carry_in`、8 位和输出与 `carry_out`，可以直接
覆盖旧的八级全加器链。候选保留关卡的三个输入和两个输出，只替换可编辑区域。

### 修改点

- 在 `(0, 0)` 放置一个 `com_add`（kind `30`，U8）。
- 分别接入 Carry in、A、B，并从原生元件输出接到 Output、Carry out。
- 所有线路只在端点触及元件引脚；导线之间的交叉不作为连接点。

### 离线验证

- 穷举 `A(256) * B(256) * Carry in(2) = 131072` 组输入。
- 每组均比较低 8 位和与第 9 位进位。
- v15 编解码往返、引脚连通性、组件实体碰撞和引脚穿越审计均通过。

候选成绩头为 `103 gate / 5 delay / 515 energy`，仍待游戏内实际验收。

## 2026-08-03 / 研究：建立字节加法器可信边界与公开前沿

### 提交

`研究：归档字节加法器协议与公开拓扑基线`

### 协议与测试边界

- 官方测试覆盖完整的 `A:U8 × B:U8 × Cin:U1`，共 `131072` 组输入；不存在
  可用于样本特化的缺失输入。
- 当前账号的 `levels.txt` 只有关卡完成标记，`byte_adder` 成绩前沿仍为空。
- 已完成关卡的 `unlocks_components` 会进入运行时预算，但电路文件头本身不能伪造
  元件成本；服务端仍会接收实际电路并重新计算。
- 因此，现有单个 `com_add` 文件只是待验收基线，不能把序列化的 `103/5` 直接写成
  当前账号已经认证的成绩。

### 公开电路与前沿

官网和个人页确认了以下公开权衡链：

```text
56/18, 57/17, 59/16, 61/15, 62/14, 63/13, 65/12,
67/11, 68/10, 71/9, 74/8, 79/7, 88/6, 103/5, 154/4
```

其中目前能取得完整网表并独立复算的端点是：

- Hub 88：直接门级 ripple，`56/18`，全 `131072` 输入通过；
- Hub 79：49 个 Bit Switch 与 56 个普通门，`154/4`，全输入通过，18 条三态
  多驱动总线无任何 0/1 冲突。

`88/6`、`79/7` 等中间拓扑仍没有公开原件，不能仅凭排行榜数字虚构电路。

### 已排除的误判

- 对 Hub 79 每条 BUS 独立替换得到的“`131/4`”只是冻结输入到达时间的局部账本。
- 完整 backward slice 和真实递归时序复算表明：保持四级延迟时只有原始
  `154/4` 组合可行；局部替换的真实前沿为 `142/5`、`126/6`、`117/7`，均不能
  击败 `515`。
- 常规 ripple、Brent-Kung、Kogge-Stone、Sklansky、Han-Carlson 与 carry-select
  共审计 2177 个结构，最好为 `75/10/750`，没有低于 `515` 的候选。

### 下一步

1. 把已验证的 Hub 79 手工电路转换成主线 Byte Adder 接口，建立当前账号首个
   可重算的 `154/4` bootstrap 前沿；
2. 用三态转移双轨 `K=00 / P=01 / G=11` 重建公开权衡链；
3. 优先寻找 `<=102/5` 或 `<=85/6` 的完整 DAG；
4. 任何候选必须同时通过全真值、Z 冲突、全局到达时间、v15 往返和真实精灵几何审计。

## 2026-08-03 / 修正：关卡禁止原生加法器并部署合法四延迟基线

### 规则修正

实机规则确认 `byte_adder` 关卡不能使用原生 `com_add`。此前单个 `com_add` 的
`103/5/515` 文件即使能够离线求值，也不是本关卡可提交的合法解，现已从正式存档和
仓库候选中移除。后续研究只允许使用关卡内实际可放置的基础门、开关、连线及由这些
元件构成的自定义结构。

### 本次部署

把公开 Hub79 的三态前缀加法器转换为当前主线关卡接口，并直接覆盖：

```text
C:\Users\cong\AppData\Roaming\Turing Complete\schematics\byte_adder\Default\circuit.data
```

候选指标和构成：

- `154 gate / 4 delay / 616 energy`；
- 49 个 Bit Switch，门成本 98；
- 12 AND、2 NAND、16 OR、25 NOR、1 NOT；
- 不含 kind 30 `com_add`；
- 候选 SHA-256：`cd56a9245cbd12e83673eaf51665807583871bb61337dc4c186331919c22211a`。

### 离线验收

- `131072/131072` 组 `A × B × Carry in` 全真值通过；
- 18 条三态多驱动总线，0/1 冲突为 0；
- Sum、Carry out 与全局语义延迟均为 4；
- 49/49 个 Switch enable 均已连接；
- v15 往返通过；
- 不支持元件、元件重叠、导线穿元件和导线穿非端点引脚均为 0。

该版本是等待实机验收的合法低延迟基线，不是最终榜一方案。当前公开第一名仍为
`103/5/515`；下一阶段同时攻击 `<=128/4`、`<=102/5` 和其他能量低于 515 的完整结构。

## 2026-08-03 / 验收：纯门级四延迟基线通过

### 实机结果

用户已在 Turing Complete `2.1.290` 中完成候选验收：

```text
154 gate / 4 delay / 616 energy
```

电路能够完成关卡验证，且不含原生 `com_add`。这把 `154/4/616` 从离线候选提升为
已实机确认的合法基线，但其能耗仍高于公开第一名 `103/5/515`，不是本项目的最终方案。

### 后续目标

研究继续以严格低于 `515` 为准，优先搜索：

```text
<=128/4
<=102/5
<=85/6
<=73/7
<=64/8
<=57/9
```

任何新方案仍须保持 `com_add == 0`，并在写入正式存档前通过完整 `131072` 组真值、
三态冲突、全局延迟、v15 往返和物理几何审计。

## 2026-08-03 / 候选：物化八十四门七延迟 Q 相位方案

### 结构来源

在 `85/7` Q 相位 A/V 计划上，把位 0 的常规 GP 叶和求和逻辑替换为精确综合的
`10 gate / 4 delay` 联合 `S0/C1` 宏。宏使用两只 Bit Switch 组成一条独占 BUS；
进位为假时允许以高阻态表示零，后续普通门按照当前游戏语义把 Z 读取为逻辑零。

完整 DAG 指标为：

```text
84 gate / 7 delay / 588 energy
output arrivals: 4, 4, 6, 5, 7, 6, 7, 7, 7
ordinary gates: 18 AND + 1 NAND + 10 OR + 31 NOR
tri-state: 12 Bit Switch / 6 resolved BUS
native com_add: 0
```

### 物理物化与离线验收

`materialize_hybrid84.py` 将完整 DAG 转换为主线 `byte_adder/Default` 的关卡自有电路，
没有创建 architecture，也没有使用自定义元件或原生加法器。每个内部元件拥有独立
横向槽，每个针脚拥有独占纵向 trunk，每条逻辑网络拥有独占外部 lane。

严格审计结果：

```text
components / wires: 81 / 162
truth table: 131072 / 131072
physical node replay: 84 / 84, mismatch 0
BUS conflict assignments: 0
physical / intended resolved networks: 87 / 87
mixed / fragmented networks: 0 / 0
tri-state outputs with exactly one resolved network: 12 / 12
unsupported / overlap / wire collision / interior pin contact: 0 / 0 / 0 / 0
recursive global / Sum / Carry delay: 7 / 7 / 7
v15 round trip: passed
```

通用非 Z 感知连通图显示单位深度 9，因为它会把多驱动 Switch BUS 当成普通串行图；
权威检查是位并行三态求值器对 84 个 DAG 节点逐一比较值、driven 掩码、conflict 掩码
和 arrival，全部一致。6 条 BUS 允许存在设计内的 Z，但最终九个输出始终被驱动。

### 直接覆盖

游戏未启动时，候选已直接覆盖以下两个文件，没有创建备份：

```text
examples/byte_adder/candidate/circuit.data
C:\Users\cong\AppData\Roaming\Turing Complete\schematics\byte_adder\Default\circuit.data
```

两者 SHA-256 均为：

```text
4aa3097e60d0c2ee1a08b12bc18f8e763d23aaf0a1401be13ecac9e38f8fdd3a
```

该版本尚待游戏实机验收；此前 `154/4/616` 仍是已实机确认前沿，不能把本节的离线
通过状态误写成排行榜认证成绩。

## 2026-08-03 / 候选：折叠第六位进位得到八十二门

### 修改灵感

`84/7` 在 bits5:6 先单独生成 `C6`，再分别计算 `S6` 与二位组进位 `C7`。精确搜索
表明可以直接让 `S6` 消费 `C7` 及已有的 `G/Q/P` 相位，从而删掉独立的四门 `C6`
开关网络。新增的五门普通门重构只取回其中两门，局部块由 23 门降到 21 门：

```text
T  = AND(P5, P6)
D  = NOR(Q6, T)
A  = OR(G5, G6)
C7 = BUS(Switch(T, C5), Switch(A, D))
S6 = NAND(OR(Q6, C7), NAND(OR(C5, G5), NOR(Q5, P6)))
```

`C7=0` 仍可用 Z 表示；所有最终 Sum 与 Carry out 始终主动驱动。

### 严格结果

```text
82 gate / 7 delay / 574 energy
components / wires: 78 / 157
output arrivals: 4, 4, 6, 5, 7, 6, 7, 7, 7
truth table: 131072 / 131072
physical node replay: 82 / 82, mismatch 0
BUS nodes / conflict assignments: 5 / 0
native com_add: 0
unsupported / overlap / wire collision / interior pin contact: 0 / 0 / 0 / 0
v15 round trip: passed
```

通用物化器在默认非部署模式回归后，以显式 `--deploy` 直接覆盖仓库候选和正式存档，
没有创建备份，也没有启动游戏。两份部署文件 SHA-256 均为：

```text
bb8e7415752e1a74adefe0d769156b975a6b4d92a9204aa56af25353bf01181d
```

该点严格优于离线 `84/7/588`，但仍高于公开第一名的 515 能耗，只作为下一轮先进
前缀、Ling 条件和及 Switch-Z 联合技术映射的可验收中间前沿。

## 2026-08-03 / 候选：共享第五位求和相位得到八十一门

### 修改灵感

`82/7` 的 `S5` 使用三门 XOR，而 `S6` 的五门重构没有利用 XOR 内部已经存在的
`P5 AND C5`。把 `S5` 改写为等价的三门相位形式后，可以免费暴露这个乘积项：

```text
T5 = AND(P5, C5)
S5 = NOR(NOR(P5, C5), T5)
```

随后对 `P5,C5,G5,P6,C7` 的局部完整真值表进行穷举，得到四门 `S6` 公式：

```text
S6 = AND(NAND(C7, P6), OR(T5, OR(G5, P6)))
```

它替换 `82/7` 中原有的五门 `S6` 重构，进位网络和所有其他位保持不变，因此整体
净省一门，并保持七延迟。

### 严格结果与直接覆盖

```text
81 gate / 7 delay / 567 energy
components / wires: 79 / 159
output arrivals: 4, 4, 6, 5, 7, 6, 7, 7, 7
truth table: 131072 / 131072
physical node replay: 83 / 83, mismatch 0
BUS nodes / conflict assignments: 5 / 0
final output Z assignments: 0
native com_add: 0
unsupported / overlap / wire collision / interior pin contact: 0 / 0 / 0 / 0
v15 round trip: passed
```

候选已在确认游戏未运行后直接覆盖仓库候选与正式 `byte_adder/Default`，未创建备份，
也没有启动游戏。两份电路 SHA-256 均为：

```text
a70d5ce1965037971119cd1d7d84f7429b5f42f281a8d01e75ee395fdca1ca7b
```

完整 DAG、生成器及机器证书位于：

```text
.research/byte_adder_root/build_hybrid_phasefold_81_d7.py
.research/byte_adder_root/byte-adder-hybrid-phasefold-g81-d7.json
.research/byte_adder_root/materialized_g81_d7/machine_certificate.json
```

该版本尚待实机验收，且 `567 > 515`，因此仍只是中间前沿。下一阶段继续优先攻击
`<=102/5`、`<=85/6` 与 `<=73/7` 的正常游戏结构。


## 2026-08-03 / 候选：G/V 相邻进位融合降至八十门

### 正向设计来源

本次不按排行榜数字反推，而是沿 Jackson/Talwar reduced-generate 与 Ling 相位融合的
思路，观察 bit2 已经为快速 `C3` 路径付费生成了约化状态：

```text
G2 = A2 AND B2
V2 = A2 OR B2
C2 = bit1 的实际输出进位
C3 = bit2 的实际输出进位
```

对合法的 kill、propagate、generate 三态，`Sum2` 可直接写为：

```text
S2 = (V2 OR C2) AND NAND(C3, NAND(G2, C2))
```

三种状态分别把该式化成 `C2`、`NOT C2`、`C2`，与 `P2 XOR C2` 完全一致。
因此不再需要只为 `S2` 单独存在的 `Q2` 和 `P2` 两级；新式使用四个普通门，替代
旧式的 `Q2 + P2 + 三门求和相位` 五门，净省一门。`S2` 从延迟 6 变成 7，仍处于
全局七延迟预算内。

### 严格验证与直接覆盖

```text
80 gate / 7 delay / 560 energy
components / wires: 78 / 157
output arrivals: 4, 4, 7, 5, 7, 6, 7, 7, 7
truth table: 131072 / 131072
physical node replay: 82 / 82, mismatch 0
BUS nodes / Switch outputs: 5 / 10
BUS conflict / final output Z: 0 / 0
native com_add / com_mux: 0 / 0
unsupported / overlap / wire collision / interior pin contact: 0 / 0 / 0 / 0
v15 round trip: passed
```

游戏未运行时，通用物化器以显式 `--deploy` 直接覆盖仓库候选与正式存档，没有创建
备份。研究候选、仓库候选和正式存档三者字节完全一致，SHA-256 均为：

```text
26da8aa59195f534c195e926b528369535f815d4d4d2fc6a7edd54fbad799067
```

核心产物：

```text
.research/byte_adder_root/build_hybrid_phasefold_80_d7.py
.research/byte_adder_root/byte-adder-hybrid-phasefold-g80-d7.json
.research/byte_adder_root/materialized_g80_d7/machine_certificate.json
```

该候选等待实机验收；研究继续转向 generalized Ling、Jackson/Talwar A/V、
conditional-sum/BEC、Han–Carlson/Knowles 非均匀前缀与 Switch-Z 的跨边界联合映射。

## 2026-08-03 / 提交 81c1f75：Q 相中段降至九十五门六延迟

### 已验收基线

正式存档中的 `80/7/560` 已由用户完成实机验收。后续研究保留它作为当前最低能耗、
可直接运行的权威基线；除非完整候选低于 `560`，否则不覆盖正式存档。

### 六延迟新前沿

从完整合法的 `97/6` A/V 结构出发，删除 bit3、bit4 的两个独立 V 叶，改用 Q 相生成
低二位组状态：

```text
A34 = OR(G3,G4)
N34 = NOR(Q3,Q4)
V34 = OR(G4,N34)
C5  = BUS(SW(A34,V34),SW(C3,V34))
```

高二位组仍保留快速 `A56,V56`。四位组的 V 轨不再先物化 `V34` 的开关结构，而是将
`V34 = G4 OR N34` 展平为三只同数据 Switch：

```text
A36 = OR(A34,A56)
V36 = BUS(SW(N34,V56),SW(G4,V56),SW(A56,V56))
C7  = BUS(SW(A36,V36),SW(C3,V36))
```

三个 `V36` 驱动的数据均为同一根 `V56`，重叠启用不会产生 0/1 冲突；完整物理网络
也没有把驱动子集拆到不同 BUS。替换区由 20 门降至 18 门，得到：

```text
95 gate / 6 delay / 570 energy
output arrivals: 4, 4, 6, 5, 6, 6, 6, 6, 6
truth table: 131072 / 131072
BUS conflict / final output Z: 0 / 0
physical net intended / actual: 89 / 89
mixed / fragmented physical net: 0 / 0
geometry / connectivity / v15 round trip: passed
native com_add: 0
```

独立验证还穷尽了固定 paid Q34/AV56 接口下 C7 后端总成本 `<=10` 的全部组件数，
`components=1..10` 均为严格 UNSAT。因此当前 11 门 C7 后端在该接口内最优；继续达到
`<=93/6` 必须跨越 Q 前端、C5、C7 与 Sum6 的边界共同综合。

核心产物：

```text
.research/byte_adder_pair_macro_exact/build_av_reduced_95_d6.py
.research/byte_adder_pair_macro_exact/byte-adder-av-reduced-g95-d6.json
.research/byte_adder_pair_macro_exact/materialized_g95_d6_independent_verify/machine_certificate.json
.research/byte_adder_pair_macro_exact/字节加法器.md
```

该点把完整六延迟前沿从 `97` 降到 `95`，但 `570 > 560`，所以只固化为研究 Pareto
点，没有覆盖正式存档，也没有启动游戏。下一阶段并行攻击 `<=93/6`、`<=79/7` 和
可完整物化的 `<=102/5`，命中低于 560 的候选后立即执行全链复核并覆盖正式存档。

## 2026-08-04 / 提交：五延迟负相低位与尾部严格边界

### 七门 `S1/S2` 前端

快速负相前缀现在有一份真实普通门结构：

```text
v  = NOR(Q1, P2)
a  = AND(P2, nC3)
w  = NAND(nC1, P1)
x  = AND(v, w)
u  = OR(nC1, P1)
S1 = NAND(w, u)
S2 = OR(a, x)
```

七个节点全部按 `1 gate / 1 delay` 计费；没有把 XOR 当成一门，也没有使用 RAM 或原生
`com_add`。完整 bit0→A/V12→`nC3`→`S1/S2` 重放通过全部 `131072` 行：

```text
S0/S1/S2 arrival = [4,4,5]
mismatch          = 0
BUS conflict      = 0
final Z           = 0
```

由此得到严格的完整 D5 账本：

```text
fixed low outputs and paid high state = 73
S3..S7/C8 joint residual target       = 29
complete target                       = 102/5/510
```

### 新的限定下界

- 固定 fast-negative paid-source 接口下，`S5/S6<=7/D6` 的 30 种 ordinary/Switch/XOR
  成本分解全部严格 `UNSAT`；这不排除跨 `S3..S6/C5/C7` 的联合重写。
- 慢 C7 paid-source `S7/C8` 联合尾部的 exact-cost-11 方程恰有 16 种合法 kind 分解；
  13 种由完整 CNF、余下 3 种由互斥完备的 `slot0-kind` 分片全部严格 `UNSAT`。因此，
  在当前真实三态、BUS conflict、physical-net partition、deadline=6、dead-component
  精确模型内，tail11 已全局封闭；这不是全局字节加法器下界，也不自动推出所有
  `cost <= 11` 均 `UNSAT`。
- 单独 `S4@D5` 的总成本 `<=8` 全部 `UNSAT`，首次 SAT 是 `9 gate` 且使用两只 Switch，
  证明高位 29 门必须跨输出共享，不能逐位独立拼装。

### 公开拓扑线索

Hub 33 的公开 32 位加法器包内含一个真实 `103/5` 八位切片，但它依赖外围免费提供的
`AND/NOR/XOR/XNOR` 预计算总线，不能直接当成 Byte Adder 榜一解。当前只提取其
Switch 多输出共享控制函数，并把缺失预计算成本显式计回新的 D5 联合模型。

详细公式、哈希、求解时间和限定范围记录在：

```text
.research/byte_adder_phase_shortcut_restart/2026-08-04-字节加法器五延迟负相前端与尾部边界.md
.research/byte_adder_remote_compute/results/s7_c8_direct_tail11_global_remaining/2026-08-04-慢C7-S7-C8-tail11精确模型全局UNSAT报告.md
```

本检查点没有更改正式存档；当前可玩基线仍是用户已验收的 `80/7/560`。研究继续并行攻击
`<=79/7`、`<=93/6` 与严格超越榜一的 `<=102/5`。

## 2026-08-04 / 提交：D5 高位十八门单源末端族局部下界

### 目标与严格范围

沿用 `fixed 73 + high residual 29` 账本，并固定已有 `S3/S4` 十一门 witness 后，
`S5/S6/S7/C8` 的剩余预算恰为 18。本轮只封闭下面这个显式受限的结构族：

```text
2 ordinary + 8 Switch = 2 + 8*2 = 18
ordinary kinds = NOT / AND / OR / NAND / NOR
ordinary 全部位于 Switch 之前，第二个 ordinary 可以读取第一个
ordinary 与 Switch 的每个有效输入 BUS 都恰含一个源
每个目标输出恰由两个私有 Switch driver 组成
ordinary output arrival <= 4，Switch output arrival <= 5
```

ordinary dead-component 约束被放宽，因而枚举集合比要求所有组件 live 的同拓扑更大。
对重复 truth table 只保留最早 arrival；较晚的重复源在全单源端口模型中不增加功能，
所以这是功能等价类商，不是漏枚举。

### 枚举终态与计数修正

权威 `domain_s34567c8_leaf()` 共 486 行，含常量在内共有 29 个 paid source。正式枚举
得到 1008 个唯一首门 truth，并检查 1,041,696 个 ordered 两门函数池。四个目标各自的
双 Switch cover 计数均为 0。

临时版本的 `1,041,648` 计数使用了 truth 首次出现的 witness arrival，其中 18 个 truth
并非最早到达。按最小 arrival 纠正后新增 48 个网络；新增网络已全部检查，结论不变。

更强地，把末端单源 Switch driver 数量放宽为任意多个，所有合法 driver enable 的并集
仍不能覆盖完整 486 行：

```text
任意 driver 数可解源池：S5=0, S6=0, S7=0, C8=0
双 ordinary 最大覆盖行：S5=414, S6=426, S7=484, C8=485
```

快速 profile 与朴素 `(enable,data)` driver-pair/全集 enable 并集枚举交叉检查 1148 个
确定性样本，两类 mismatch 都为 0；结果 JSON 连续两次重放 SHA 完全相同。

### 不能外推的边界

该结论**只能**表述为“前置 ordinary + 全单源端口末端 Switch 族局部 UNSAT”。它不覆盖：

- ordinary 读取已经 resolved 的 Switch BUS；
- ordinary/Switch 交错多层结构；
- ordinary 或 Switch 输入为多源 BUS；
- ordinary 直接驱动最终输出或每输出非对称 driver 分配；
- XOR 或其他 ordinary/Switch 成本分解。

因此不能把本结果外推成完整 `o2/s8`、完整成本 18 或完整高位 29 门下界。下一步专门
搜索 ordinary 读取 resolved BUS 的交错族，以及依赖多源 BUS 解析的结构。

### 待提交证据

```text
.research/byte_adder_han_knowles_fused_agent/enumerate_s567c8_o2s8_two_driver_restricted.py
.research/byte_adder_han_knowles_fused_agent/s567c8_o2s8_two_driver_restricted_result.json
.research/byte_adder_han_knowles_fused_agent/2026-08-04-s567c8-o2s8双单源Switch受限族枚举.md
.research/byte_adder_han_knowles_fused_agent/s567c8_o2s8_two_driver_restricted_SHA256SUMS.txt
examples/byte_adder/history/字节加法器.md
```

本检查点未启动游戏，未读取或写入正式存档，未修改或部署 candidate/metadata，也没有
提交 Git。

## 2026-08-04 / 提交：D5 交错 tail 相位 3x3 严格下界

### 固定交错位置

在同一 `S5/S6/S7/C8 <=18/D5` 权威物理模型内，进一步完整封闭：

```text
SWITCH, SWITCH, K1, K2, SWITCH, SWITCH, SWITCH, SWITCH, SWITCH, SWITCH
K1,K2 ∈ {NOT,NOR,OR}
```

前两只 Switch 位于 ordinary 之前，输入 BUS 仍完全自由；它们可以组成 resolved BUS，
slot2/slot3 ordinary 可以读取该 BUS、paid source 或前一 ordinary。因此这是真正的交错
结构，不是前置 ordinary 的单源功能近似。

9 个有序 kind 对全部严格 `UNSAT`：

```text
          NOT      NOR      OR
NOT       UNSAT    UNSAT    UNSAT
NOR       UNSAT    UNSAT    UNSAT
OR        UNSAT    UNSAT    UNSAT
```

模型保留自由多源 BUS、Switch Z/conflict、physical-net partition、public output driven、
deadline=5 与 dead-component 约束。三个 Glucose 300 秒 `UNKNOWN` 片分别由 CaDiCaL
重审为严格 `UNSAT`；`UNKNOWN` 本身没有计入覆盖。

审计终态为：

```text
status=unsat-covered
ordered_pair_count=9
selected_evidence_count=9
missing/invalid/non_unsat/duplicate=0/0/0/0
```

该结论只覆盖固定 slot2/slot3 且 kind 属于 `{NOT,NOR,OR}` 的 3x3 矩阵；不能外推到
`AND/NAND`、其他 ordinary 位置、更深 interleave、其他成本分解或完整成本 18 下界。

### SHA 与待提交证据

Windows 上 worker stdout SHA 是写入前 LF 内容 SHA，而实际 JSON 文件由
`Path.write_text` 落成 CRLF。审计器同时校验两种 SHA；正式 `SHA256SUMS` 使用实际磁盘
字节 SHA。

```text
.research/byte_adder_han_knowles_fused_agent/audit_s567c8_interleave_ss_tail_phase_3x3.py
.research/byte_adder_han_knowles_fused_agent/s567c8_interleave_ss_tail_phase_3x3_audit.json
.research/byte_adder_han_knowles_fused_agent/2026-08-04-s567c8-o2s8交错tail相位3x3严格UNSAT.md
.research/byte_adder_han_knowles_fused_agent/s567c8_interleave_ss_tail_phase_3x3_SHA256SUMS.txt
```

```text
auditor SHA256 = c43e389bbd23974cf0ce4f590dff3ac94ad7f6a5f1bac918374bde23935bb42b
audit SHA256   = 904abce96a4c2610a00babfb15f3d51aaef608addbd05c22025ed7656448fb73
```

本检查点同样没有启动游戏、没有接触正式存档或 candidate/metadata，也没有提交 Git。

## 2026-08-04 / 提交：封闭前置普通门固定拓扑

### 本次研究对象

在五延迟高位联合模型中，固定四个目标输出与精确成本：

```text
outputs      = S5,S6,S7,C8
gate         = 18
delay        = 5
components   = 10
ordinary     = 2
Switch       = 8
XOR          = 0
slot order   = [ordinary, ordinary, SWITCH x8]
ordinary kind = NOT / AND / OR / NAND / NOR
```

前两个普通门槽的种类被拆为 `5×5=25` 个有序、互斥的 fixed-kind 分片。每片均使用
authoritative `physical_exact.py`，包含真实三态、高阻态、BUS conflict、输出必须有驱动、
递归延迟、physical-net source-set partition 与 dead-component 约束。

### 结果

```text
25/25 strict UNSAT
SAT / UNKNOWN / missing / invalid / overlap = 0 / 0 / 0 / 0 / 0
solve seconds = 145.384 .. 683.404
audit status  = unsat-covered
```

根任务重新运行专用审计器，并逐项复算 32 个证据对象的 SHA-256；两项检查均无错误。
正式求解器的 25 片全部在 `800 s` 外层硬超时前生成规范 UNSAT JSON，因此没有把超时、
被终止进程或无结果文件误记为证明。

### 限定结论

这只封闭“两个 ordinary 全部在前、八只 Switch 全部在后”的固定非交错拓扑。它**不是**
完整 `g18` 下界，也不覆盖 ordinary 读取 resolved Switch BUS、ordinary/Switch 交错、
`o4/s7`、`o6/s6`、含 XOR 的结构或 fixed paid-source shell 的联合重写。

规范证据位于：

```text
.research/byte_adder_phase_shortcut_restart/2026-08-04-s567c8-g18-o2s8前置普通门固定拓扑完整分片.md
.research/byte_adder_phase_shortcut_restart/local_s567c8_g18_o2_s8_kind_matrix_w6.json
.research/byte_adder_phase_shortcut_restart/local_s567c8_g18_o2_s8_kind_matrix_w6_audit.json
.research/byte_adder_phase_shortcut_restart/server-results/local_s567c8_g18_o2_s8_kind_matrix_w6/
.research/byte_adder_phase_shortcut_restart/server-runs/local_s567c8_g18_o2_s8_kind_matrix_w6/sweep-summary.json
.research/byte_adder_phase_shortcut_restart/s567c8_g18_o2_s8_kind_matrix_SHA256SUMS.txt
```

本检查点没有可部署的新电路，正式存档仍保持用户已验收的 `80/7/560`；游戏未启动，
存档未读取也未写入。研究继续攻击 ordinary/Switch 交错、`o4/s7`、D6 carry retime、
D7 suffix567 与 `fixed69 + high33` 的更大函数宇宙。

## 2026-08-04 / 提交：封闭尾部控制 multiset 的全部排列

### 搜索对象

已知 `S7/C8@16/D5` 正见证使用的四个 ordinary kind 是：

```text
NOT, NOR, OR, OR
```

本轮把它投影到 `S5/S6/S7/C8` 的十八门 residual，并穷尽重复 `OR` 去重后的
`4!/2!=12` 个唯一排列：

```text
gate / delay = 18 / 5
components   = 11
ordinary     = 4
Switch       = 7
XOR          = 0
slot order   = [perm(NOT,NOR,OR,OR), SWITCH x7]
```

### 结果与复核

```text
12/12 strict UNSAT
SAT / UNKNOWN / missing / invalid / overlap = 0 / 0 / 0 / 0 / 0
solve seconds = 90.827 .. 273.349
sum solve seconds = 1595.037
audit status = unsat-covered
```

专用审计确认 `orders_seen=12`、全集约束 SHA 与 worker SHA 均匹配；正式四 worker
runner 最终 `finished=true`、结果 12/12 UNSAT，进程树完全退出。18 项证据清单逐项
复算 SHA-256 为零错误，没有把 timeout、无 JSON 或被终止进程记成 UNSAT。

### 严格边界

该结论只封闭“`{NOT,NOR,OR,OR}` 的 ordinary 排列全部在前、七只 Switch 全部在后”
这一 terminal-Switch 类。它不覆盖其它 ordinary multiset、ordinary/Switch 交错、
`o6/s6`、含 XOR 的成本分解或 fixed paid-source shell 重写，因而不能写成完整
`g18/o4/s7` 下界。

规范证据：

```text
.research/byte_adder_phase_shortcut_restart/2026-08-04-s567c8-g18-o4s7尾部控制multiset固定拓扑完整排列.md
.research/byte_adder_phase_shortcut_restart/local_s567c8_g18_o4_s7_tail_multiset_w4.json
.research/byte_adder_phase_shortcut_restart/local_s567c8_g18_o4_s7_tail_multiset_w4_audit.json
.research/byte_adder_phase_shortcut_restart/server-results/local_s567c8_g18_o4_s7_tail_multiset_w4/
.research/byte_adder_phase_shortcut_restart/server-runs/local_s567c8_g18_o4_s7_tail_multiset_w4/sweep-summary.json
.research/byte_adder_phase_shortcut_restart/s567c8_g18_o4_s7_tail_multiset_SHA256SUMS.txt
```

本检查点没有完整新电路；游戏未启动，正式/candidate 存档未读取也未写入。搜索已经继续
扩展到该 ordinary multiset 的相邻族，并保持与 Han 交错拓扑路线互不重复。

## 2026-08-04 / 提交：封闭八十门方案的局部普通门根替换

### 新检查的缺口

旧 `80/7` local-resub 审计只检查一门、两门替换，但 S0、S6 各有四门私有锥，S2、S4
各有五门私有锥。若分别存在三门或四门替换，就可以直接得到 `79/7/553`。

本轮用完整 `131072` 行投影补齐普通门范围：

```text
S0/S6：全部 live 三门 chain/fanout/parallel 拓扑，0 hit
S2/S4：四门公式树各 51 万/56 万函数，0 hit
S2/S4：任意内部扇出的四门 SAT，CaDiCaL 与 Glucose42 均 UNSAT
```

### 编码正回归

同一 SAT 模型切换为五门时，两个目标、两个求解器都返回 SAT。解码 witness 均通过原始
`131072` 行真值与 D7 实际到达检查：

```text
S2 5-gate：725 variables / 11371 clauses / arrival 7
S4 5-gate：725 variables / 11441 clauses / arrival 7
```

因此局部普通门根替换已经封闭，但这不是全局 `79/7` 下界。精确成本 4 的
`ordinary+Switch`、双 Switch、`ordinary+XOR` 混合以及跨输出联合改写仍未被本结果覆盖，
下一步已直接转入这些剩余族。

规范报告与机器证据：

```text
.research/byte_adder_root/2026-08-04-字节加法器七延迟局部根替换审计.md
.research/byte_adder_root/audit_80d7_three_gate_roots.py
.research/byte_adder_root/three-gate-root-resub-80d7.json
.research/byte_adder_root/audit_80d7_four_gate_dag_sat.py
.research/byte_adder_root/four-gate-dag-sat-80d7.json
.research/byte_adder_root/four-gate-dag-sat-80d7-glucose42.json
```

本检查点没有可部署的新候选，未启动游戏，也未读取或修改正式存档；已验收前沿仍为
`80/7/560`。

## 2026-08-04 / 提交：封闭八十门方案 S2/S4 混合成本4根替换

### 固定接口与剩余分解

继续沿用上一检查点经过审阅的两个局部 retained-source pool：

```text
S2 / node 81：4,5,6,7,22,23,24,25,45,50,51,52,54,55,56,76,77
S4 / node 86：8,9,10,11,28,29,30,31,32,56,57,59,60,61,62,82,83
deadline=7，现有私有成本=5，替换目标成本=4
```

旧双求解器审计已经封闭四个 ordinary component 的 `o4`。本轮使用 reviewed physical
BUS encoder 补齐成本 4 的全部剩余可实现分解：

```text
o2+s1    ordinary x2 + Switch x1
s2       Switch x2
o1+x1    ordinary x1 + XOR x1
```

BUS source `45/55/56/62` 直接保留权威 DAG `logical_states` 的精确
`bits/driven/conflict/depth`，没有当作恒 driven Boolean source。混合模型按 value+driven
联合签名压缩为 48 行；旧 Boolean-only `o4` 投影为 32 行，行数差异正是三态 driven
信息带来的细分。

### 结果与正回归

CaDiCaL 195 与 Glucose42 对 S2、S4 的三种混合分解全部严格 UNSAT，无 SAT、UNKNOWN 或
分歧；加上既有 `o4`，两个固定 pool 下的 exact weighted-cost 4 已完整封闭。

同一编码器切换为五门后，S2/S4 均返回 SAT，并独立重放完整 `131072` 行：

```text
S2 arrivals=4,5,6,5,7  output arrival=7
S4 arrivals=5,2,5,6,7  output arrival=7
mismatch / conflict / undriven / partition violation / dead component = 0
```

该结论仅适用于两个显式 retained-source pool，不联合重综合 upstream nodes，不覆盖跨输出
共享改写，不能写成全局 `79/7` 下界。本轮没有新候选或 graft；游戏未启动，正式存档与
candidate/metadata 未读写，已验收前沿仍为 `80/7/560`。

规范证据：

```text
.research/byte_adder_builder_verify_restart/audit_80d7_weighted4_mixed_roots.py
.research/byte_adder_builder_verify_restart/weighted4_mixed_roots_80d7.json
.research/byte_adder_builder_verify_restart/2026-08-04-80d7-S2S4混合成本4根替换完整封闭.md
.research/byte_adder_builder_verify_restart/weighted4_mixed_roots_80d7_SHA256SUMS.txt
```

## 2026-08-04 / strict-C3 bit3:4：C5 输出完备二分关闭固定 g13/n11/s2/x0

### 固定搜索对象

本轮继续攻击当前 `80/7/560` 的 bit3:4 residual，只关闭以下精确成本分解：

```text
profile / bound = d7_80 / 13
components      = 11
ordinary/Switch/XOR = 9/2/0
deadlines       = S3@5, S4@7, C5@4
domain          = 16 raw × {Z0,D0,D1} = 48 rows
physical nets   = true
all components  = live
```

若存在 SAT，固定壳账本会得到 `66+13=79/7/553`；本轮最终没有 SAT 或可 graft 证书。

### C5 驱动数的完备二分

exact encoder 强制 `C5` 至少选择一个 driver。其 active-BUS normalizer 又保证 paid
source、常量或任意非 Switch component output 不能与其它 driver 同选；因此多驱动
`C5` 只能由 Switch 输出构成。基础实例精确含两只 Switch，所以全部网络严格分为：

```text
exactly one C5 driver
exactly the two Switch outputs form C5
```

双 Switch 分支按两只 Switch 的普通门祖先数 `k=0..9` 形成十片。physical-net partition
使两只最终 C5 Switch 不可能互相依赖，因此所有祖先可 WLOG 排在前、两 Switch 紧随、
其余普通门排在后。

单驱动分支由一个 source/constant shard 与 component-driver 祖先数 `k=0..10` 十一片
组成。component 分片把 driver 的全部祖先排在前、driver 紧随、非祖先放在后。两个分支
的 prefix user clauses 都保证每只前缀门沿有限 DAG 到达目标 driver，不会混入非祖先门。

### 精确终态

双 Switch C5 net：

```text
10/10 MapleChrono UNSAT
UNKNOWN / missing / SAT = 0 / 0 / 0
solve seconds = 0.098061 .. 56.643558
sum solve seconds = 228.981008
```

单驱动 C5：

```text
Glucose42: source,k0..k8 UNSAT；k9,k10 在 30 s 为 UNKNOWN
MapleChrono: k9,k10 UNSAT，22.036114 / 26.504028 s
merged: 12/12 UNSAT，UNKNOWN-only/missing/conflict/SAT = 0/0/0/0
```

`UNKNOWN` 没有被提升为 UNSAT。合并器逐片核对 source artifact、搜索脚本、exact
encoder 与 constraint digest 后，最终输出：

```text
coverage_complete = true
all_unsat          = true
errors             = []
conclusion         = fixed d7_80 g13/n11/s2/x0 is UNSAT
```

### 正回归与独立审计

已知 14 门 D7 C5-pair 网络被重排到 `k=3` 同一规范形后返回 SAT；48 行重放的 truth
mismatch、BUS conflict、undriven output 与 physical partition violation 均为 0，证明
cone 约束没有误伤已知合法网络。

`byte_adder_builder_verify_restart` 子任务随后独立静态审计 driver-count 二分、两 Switch
互不依赖证明、两类 ancestor normal form 和 commutative BUS 顺序，结论为
`coverage argument sound`，未发现覆盖漏洞；该审计没有重复运行 SAT。

### 严格边界

该结论只关闭固定 `g13/n11/s2/x0`，不能外推到其它 component 数、Switch/XOR 数或
weighted gate-13 成本分解，更不是完整 `g<=13` 下界。远端 49 个 slot0/slot1 kind
分片仍由原 runner 继续执行，没有因本地证明被停止或修改。

规范证据：

```text
.research/byte_adder_conditional_sum_restart/2026-08-04-strict-C3-bit34-C5输出驱动完备二分固定分解UNSAT.md
.research/byte_adder_conditional_sum_restart/2026-08-04-bit34-C5输出分区独立覆盖审计.md
.research/byte_adder_conditional_sum_restart/bit34_d7_g13_n11_s2_x0_c5_pair_cone_maplechrono_complete.json
.research/byte_adder_conditional_sum_restart/bit34_d7_g13_n11_s2_x0_c5_single_driver_cone_merged_complete.json
.research/byte_adder_conditional_sum_restart/bit34_d7_g13_n11_s2_x0_c5_output_partition_complete.json
.research/byte_adder_conditional_sum_restart/bit34_c5_pair_cone_positive_g14.json
.research/byte_adder_conditional_sum_restart/bit34_c5_output_partition_SHA256SUMS.txt
.research/byte_adder_conditional_sum_restart/bit34_c5_output_partition_submit_files.txt
```

本检查点没有启动游戏，没有读取或修改正式存档，没有修改或部署 candidate/metadata，
也没有暂存或提交 Git；当前已验收前沿仍为 `80/7/560`。

## 2026-08-04 / 提交：封闭八十门方案 S0/S6 混合成本3根替换

### 固定接口与完整成本分解

沿用 reviewed local-root 的两个 retained-source pool：

```text
S0 / node 49：2,3,18,43,44,45
S6 / node 73：12,13,14,15,34,35,36,37,38,39,62,63,64,65,66,67,68,69
deadline=7，现有私有成本=4，替换目标成本=3
```

按 `ordinary=1, Switch=2, XOR=3`，成本 3 只有 `o3 / o1+s1 / x1`。既有 reviewed
枚举已经封闭全部 live 三 ordinary 拓扑；本轮 physical BUS SAT 补齐 `o1+s1` 与 `x1`。
BUS source `45/62/69` 保留权威 DAG 的精确 value/driven/conflict/depth，不按恒 driven
Boolean rail 处理。联合签名压缩为 S0 8 行、S6 48 行。

### 结果与正回归

```text
S0 o1+s1：509 variables / 3869 clauses / 双求解器 UNSAT
S0 x1：   227 variables / 1514 clauses / 双求解器 UNSAT
S6 o1+s1：1729 variables / 20341 clauses / 双求解器 UNSAT
S6 x1：   799 variables / 9818 clauses / 双求解器 UNSAT
```

同一严格 Z/BUS/physical-net 编码器切换为四个 ordinary component 后，两个目标在
CaDiCaL 195 与 Glucose42 下均返回 SAT。四份 witness 独立重放完整 `131072` 行：

```text
S0 arrivals=2,2,3,4  output arrival=4
S6 arrivals=3,6,6,7  output arrival=7
mismatch / conflict / undriven / partition violation / dead component = 0
```

结合既有 `o3`，两个固定 pool 下 exact weighted-cost 3 已完整封闭。该结果不联合重综合
upstream nodes、不覆盖跨输出共享或 paid-source shell 改写，不能外推成全局 `79/7`
下界。本轮没有新候选或 graft；未启动游戏，正式存档与 candidate/metadata 未读写，前沿
仍为 `80/7/560`。

规范证据：

```text
.research/byte_adder_builder_verify_restart/audit_80d7_weighted3_mixed_roots.py
.research/byte_adder_builder_verify_restart/weighted3_mixed_roots_80d7.json
.research/byte_adder_builder_verify_restart/2026-08-04-80d7-S0S6混合成本3根替换完整封闭.md
.research/byte_adder_builder_verify_restart/weighted3_mixed_roots_80d7_SHA256SUMS.txt
```

## 2026-08-04 / 提交：封闭 D5 尾部相邻非门 terminal-Switch 族

### 固定搜索对象

在 `fixed 73 + high29` 的五延迟账本下，冻结既有 `S3/S4=11/D5` 正见证后，
`S5/S6/S7/C8` 的剩余成本为 18。本轮从已知 tail ordinary multiset
`{NOT,NOR,OR,OR}` 出发，只把一个 `OR` 改为 `NOT`，穷尽两个 `NOT` 对称去重后的
12 个排列：

```text
gate / delay = 18 / 5
components   = 11
ordinary     = 4
Switch       = 7
XOR          = 0
slot order   = [perm(NOT,NOT,NOR,OR), SWITCH x7]
```

每片均使用 authoritative `physical_exact.py`，保留三态、高阻态、BUS conflict、
public output driven、递归延迟、physical-net source-set partition 与 dead-component
约束。

### 结果、恢复与审计

```text
12/12 strict UNSAT
SAT / UNKNOWN / missing / invalid / overlap = 0 / 0 / 0 / 0 / 0
solve seconds = 87.782 .. 220.110
sum solve seconds = 1453.766
audit status = unsat-covered
```

三 worker 启动后 Free RAM 接近 `2.5 GiB` 下限，因此停止 supervisor、保留两个在途
solver 并终止第三片；两个保留片自然写出 UNSAT 后，用相同 12 项约束、相同输出目录的
两 worker manifest 恢复。最终 summary 为 `2 reused + 10 completed`。原始与恢复
manifest 的 `name/proof_scope/values/arguments/working_directory/script` 逐项相等；被终止片
由恢复进程重新求解，没有把中断、timeout 或无 JSON 状态记为 UNSAT。

专用审计确认 `orders_seen=12`、constraint-set SHA 与 worker SHA 全部匹配；独立交叉检查
又确认 summary 的 12 个 value 与原 manifest 相等、12 个 `output_sha256` 与磁盘相等，
结果目录没有额外或缺失 JSON。

### 严格边界与证据

该结论只封闭 `[perm(NOT,NOT,NOR,OR), SWITCH x7]`，不能外推到其它 ordinary
multiset、ordinary/Switch 交错、`o6/s6`、含 XOR 的成本分解或完整 `g18` 下界。它是
条件 `102/5/510` 路线的一个局部下界，不是当前 `80/7/560` 前沿的新电路。

```text
.research/byte_adder_phase_shortcut_restart/2026-08-04-s567c8-g18-o4s7相邻OR转NOT固定拓扑完整排列.md
.research/byte_adder_phase_shortcut_restart/make_s567c8_g18_o4_s7_adjacent_or_to_not.py
.research/byte_adder_phase_shortcut_restart/audit_s567c8_g18_o4_s7_adjacent_or_to_not.py
.research/byte_adder_phase_shortcut_restart/local_s567c8_g18_o4_s7_adj_or2not_w3.json
.research/byte_adder_phase_shortcut_restart/local_s567c8_g18_o4_s7_adj_or2not_resume_w2.json
.research/byte_adder_phase_shortcut_restart/local_s567c8_g18_o4_s7_adj_or2not_audit.json
.research/byte_adder_phase_shortcut_restart/server-results/local_s567c8_g18_o4_s7_adj_or2not_w3/
.research/byte_adder_phase_shortcut_restart/server-runs/local_s567c8_g18_o4_s7_adj_or2not_w3/sweep-summary.json
.research/byte_adder_phase_shortcut_restart/s567c8_g18_o4_s7_adj_or2not_SHA256SUMS.txt
```

本检查点没有启动游戏，没有读取或写入正式/candidate 存档，也没有修改
`physical_exact.py`。

## 2026-08-04 / 提交：strict-C3 bit3:4 broad weighted-g13 完备封闭

### 固定接口与正常形

固定 D7 bit3:4 residual：paid sources 为 `a3,b3,a4,b4,G3,Q3,P3,G4,Q4,P4,C3@3`，
输出为 `S3@5,S4@7,C5@4`。严格边界是 `16 raw x {Z0,D0,D1}=48` 行，保留
Switch Z、ordinary 读 Z 为 0 且主动驱动、BUS conflict、输出 driven、physical-net
driver-set partition、component live 与 `ordinary/Switch/XOR=1/2/3`。

对每个 `n=0..13`，C5 完备分为 direct source/constant、单 component driver `single_kK`
与多 Switch driver `multi_dD_kK`。多驱动 BUS 只能由 Switch 构成；physical partition
排除最终 Switch 之间的依赖，所以可 WLOG 把全部祖先排在前、`D` 个 driver 紧随、非祖先
放在后。weighted bound 给出 `D<=min(n,13-n)`。逐 n shard 数为：

```text
1,2,4,7,11,16,22,28,31,31,28,22,13,14；合计 230
```

独立静态审计复算 ordered domain、230 个唯一 key/name/digest、祖先闭包和 commutative BUS
顺序，结论为 coverage sound、无漏洞。

### 正回归、远端终态与下载核验

已知 14 门网络固定到 `g14/n12/multi_d2_k3` 后 SAT；独立 48 行重放的
mismatch/conflict/undriven/partition 均为 0。`n=0..2` 的 7 片 smoke 全 UNSAT。

远端在 available RAM 约 `13.4 GiB` 时启动 3 worker、每进程 4096 MiB、`nice=10` 的
stop-on-first-SAT 队列；原 49 片 runner 保持 `nice=5` 并继续运行，没有被停止、重启或
修改。完整 230 片在 251.485 秒 wall time 内结束：

```text
UNSAT/SAT/UNKNOWN = 230/0/0
missing/error     = 0/0
finished          = true
stopped_on_sat    = false
```

下载后 frozen summarizer 逐片复算 constraint/search/dependency SHA，得到
`coverage_complete=true, all_unsat=true`。transport verifier 又把 remote summary 的 230 个
`output_sha256` 与本地文件逐项核对，零差异；第二份独立终态审计确认 spec/summary/complete/
transport 四方 230-name 集、output SHA 与 constraint digest 全部一致。

由此固定接口下 `weighted gate<=13` 完整 UNSAT；结合 14 门正见证，bit3:4 residual 的
精确最小成本为 14。冻结 `66+residual` shell 中不能只替换该 residual 得到 `79/7`。
这不是全架构 Byte Adder 的全局下界；其它 paid sources、跨区域共享与 shell 联合改写未被
覆盖。当前已验收候选仍为 `80/7/560`。

规范证据：

```text
.research/byte_adder_conditional_sum_restart/2026-08-04-strict-C3-bit34-broad-C5正常形g13完备UNSAT.md
.research/byte_adder_conditional_sum_restart/2026-08-04-bit34-broad-C5正常形独立静态覆盖审计.md
.research/byte_adder_conditional_sum_restart/2026-08-04-bit34-broad-C5远端230片终态独立审计.md
.research/byte_adder_conditional_sum_restart/bit34_d7_g13_broad_c5_normal_form_workers3.json
.research/byte_adder_conditional_sum_restart/bit34-d7-g13-broad-c5-normal-form-workers3-summary.json
.research/byte_adder_conditional_sum_restart/bit34_d7_g13_broad_c5_normal_form_complete.json
.research/byte_adder_conditional_sum_restart/bit34_d7_g13_broad_c5_remote_transport_verify.json
.research/byte_adder_conditional_sum_restart/bit34_d7_g13_broad_c5_remote_completion_manifest.json
.research/byte_adder_conditional_sum_restart/bit34_broad_c5_SHA256SUMS.txt
.research/byte_adder_conditional_sum_restart/bit34_broad_c5_submit_files.txt
```

本检查点没有启动游戏，没有读取或修改正式存档，没有修改或部署 byte_adder candidate，
也没有暂存或提交 Git。

## 2026-08-04 / 提交：封闭可达域输出相位、输入排列与真实 D6 映射

### 搜索范围

从已验收 `80/7/560` DAG 导出 21 个边界变量、9 个输出和 23,328 个唯一可达边界点，
把其余 `2,073,824` 个不可达点交给 Espresso 作为 don't-care extension。本轮完整搜索：

```text
512 output phases                       512
512 phases x 4 timing recipes         2048
64 permutations x 2 phases             128
64 x 2 x 4 timing recipes              512
64 permutations x 13 phases            832
64 x 13 x 4 timing recipes            3328
10 output groupings                      10
-------------------------------------------
total                                  7370
```

所有 search/timing summary 均满足声明组合完整、worker error 为 0、recipe error 为 0。
timing BLIF 使用 metadata 中的真实 boundary arrival、`required=6`，并以 ABC old BLIF
parser 的 `read_blif -n` 读取；最终延迟由 mapped netlist 递归复算，不采用 ABC 的局部
`map -D 6` 文本作为成绩。

### 严格结果

```text
energy < 560 = 0
delay <= 6   = 0
best         = perm 21 / phase 0x09e = 85/7/595
local tie    = perm 22 / phase 0x087 = 85/7/595
```

本地并列最佳 `perm_022_phase_087` 的五个 mapped BLIF 全部通过 importer 双构建；其中
untimed、`dch_d6`、`resub8_d6` 为 `85/7/595`，`dc2_d6` 为 `87/7/609`，
`plain_d6` 为 `87/8/696`。`phase 0x087` 的四个反相输出在全部 23,328 个 care 点补回后
mismatch 为 0。

最佳 untimed BLIF 又连续两次通过完整物理管线：131,072 行全真值、BUS/Z/conflict、
recursive delay、physical-net partition、dead component、连通性、几何、v15 round-trip、
`com_add=0`、独立物理审计与确定性重建全部通过。关键 SHA：

```text
1019b65880903b9e467dbe0276a399880d22e6b5f1276bfceca15179333eadf8  64x13 independent audit
579ba1cfc936f22ac77b4c4e811da2e38d5528b6547d94d698941d5425c2504d  pipeline summary
b48220901fe4882b85abd4dbd3d6dcc6c32984643535ec33b5a2baa137da0663  materialized candidate
3e983eeb6e53b1233f3840312412c313697f7132962e0fb4e18a5e34ee058579  103-entry manifest
```

搜索脚本随后加入 `--permutation-start` 和 `[start, stop)` 契约，使后续批次能够只计算新
排列；默认 `start=0` 保持旧行为，远端 `start=64,count=1` 正回归通过。本检查点的最佳值
仍高于 560，因此没有覆盖正式存档，也没有启动游戏。

规范证据：

```text
.research/byte_adder_root/2026-08-04-reachable-output-phase-timing完整封闭.md
.research/byte_adder_root/reachable_output_phase_timing_SHA256SUMS.txt
.research/byte_adder_root/audit_reachable_output_phase_artifacts.py
.research/byte_adder_root/audit_reachable_input_permutation_artifacts.py
.research/byte_adder_root/audit_reachable_output_grouping_summary.py
.research/byte_adder_root/abc_residual_current80/care_pla/input_permutation_64x13/
```

## 2026-08-04 / 提交：封闭三早期 Switch、相邻 ordinary 的 D5 wildcard

### 固定拓扑与完整覆盖

继续固定 `fixed 73 + high29` 的五延迟路线，对 `S5/S6/S7/C8` 搜索成本 18 的尾部：

```text
slot order  = SWITCH,SWITCH,SWITCH,*,*,SWITCH,SWITCH,SWITCH,SWITCH,SWITCH
gate/delay  = 18/5
components  = 10
Switch/XOR  = 8/0
ordinary    = 2
```

成本和组件数已经固定 8 只 Switch，因此两个 wildcard 只能各取
`{NOT,AND,OR,NAND,NOR}`；单个 wildcard shard 精确覆盖全部 `5 x 5 = 25` 个有序赋值，
不存在 solver-free 漏项。搜索保留三态、高阻态、BUS conflict、public output driven、
递归延迟、physical-net source partition 和 dead-component 约束。

### 终态与边界

```text
status        = strict UNSAT
variables     = 174812
clauses       = 1340420
solve seconds = 824.413718
watchdog      = 900 s, not triggered
solver        = CaDiCaL 195
```

独立审计核对 result/log/run record、live dependency SHA、固定 kind、slot9 shard、25/25
赋值、LF 和求解时间一致性；连续两次生成相同 audit 与 manifest。关键 SHA：

```text
e5c47ef05745b250d01414fe09e95fdefce68e5b607c7831dd5173859574c271  result
29e6e59e2ca635cab956379571810460c413748a7a9dc0d82edd9fb4340c5941  independent audit
09c9d74eb1ca9d958d275d19fc90db737a4e68414c4b1ec1d1625ad5799aee54  coverage manifest
00161b4e75b7db18a96076347b78034cc71e8d9162fa6d60ec8e560386add4d8  SHA manifest
```

该结论只封闭上述相邻 ordinary 的固定十槽拓扑，不能外推成完整 `g18/o2s8` 下界，也不
排除其它 ordinary/Switch 交错或不同 component 数。本轮没有候选可部署，正式前沿仍为
`80/7/560`。

规范证据：

```text
.research/byte_adder_han_knowles_fused_agent/2026-08-04-s567c8-o2s8三早期Switch相邻ordinary-wildcard完整UNSAT.md
.research/byte_adder_han_knowles_fused_agent/audit_s567c8_interleave_sss_oo_s5_wildcard.py
.research/byte_adder_han_knowles_fused_agent/s567c8_interleave_sss_oo_s5_wildcard_audit.json
.research/byte_adder_han_knowles_fused_agent/s567c8_interleave_sss_oo_s5_wildcard_manifest.json
.research/byte_adder_han_knowles_fused_agent/s567c8_interleave_sss_oo_s5_wildcard_SHA256SUMS.txt
```

## 2026-08-04 / 提交：封闭 D5 尾前缀双普通门槽完整 5×5

### 固定类

在 `fixed 73 + high29` 五延迟路线中，冻结 `s34567c8_leaf` 接口并搜索：

```text
NOT,NOR,OR,OR,K1,K2,SWITCH,SWITCH,SWITCH,SWITCH,SWITCH,SWITCH
K1,K2 in {NOT,AND,OR,NAND,NOR}

gate/delay = 18/5
components = 12
ordinary   = 6
Switch/XOR = 6/0
```

`K1/K2` 是有序固定槽，因此 25 个 fixed-kind 子问题互斥且完整覆盖该精确拓扑类。所有
分片均保留 486 行相关域、三态、高阻、BUS conflict、public output driven、递归延迟、
physical-net partition 与 dead-component 约束。

### 严格终态与审计

```text
25/25 strict UNSAT
SAT / UNKNOWN / missing / invalid / overlap = 0 / 0 / 0 / 0 / 0
solve seconds = 53.503862 .. 303.230016
sum solve seconds = 4686.157176
runner = 6 reused + 19 completed
```

先导 wildcard 在 800.233732 秒后没有 result JSON，`status=null`，因此始终严格记为
UNKNOWN，不参与上述证明。最终 auditor、basename crosschecker、runner summary 与 25 个
本地 payload 的 kind、constraint-set、worker SHA、terminal state 和 output SHA 全部一致：

```text
audit status       = unsat-covered
crosscheck status  = verified
mismatch_count     = 0
errors             = []
constraint SHA     = fd65030d671011c42e3f9f0cee64ddf25332b1ea4822349e6efa67ce1bdf4985
worker SHA         = c48a96e55d8c5076418999f5fa5ee95e9f8207c03f138cd4bccf48908a69c071
SHA manifest       = 1627630c3c8d717e00703b03b84aca1294fd31873108d7d56d48317ae9d22076
```

该结论只关闭固定 `NOT,NOR,OR,OR` 前缀、两个 ordinary 槽和六只 terminal-Switch 的
精确类，不覆盖其它 ordinary multiset、ordinary/Switch 交错、Switch 后读取 resolved BUS、
含 XOR 的成本分解或 shell 联合改写；因此没有得到 `102/5/510` 候选，正式前沿仍是
`80/7/560`。

规范证据：

```text
.research/byte_adder_phase_shortcut_restart/2026-08-04-s567c8-g18-o6s6尾前缀双槽terminal-Switch完整5x5.md
.research/byte_adder_phase_shortcut_restart/s567c8_g18_o6_s6_tailprefix_matrix_SHA256SUMS.txt
.research/byte_adder_phase_shortcut_restart/audit_s567c8_g18_o6_s6_tailprefix_two_slot.py
.research/byte_adder_phase_shortcut_restart/crosscheck_s567c8_g18_o6_s6_tailprefix_matrix.py
.research/byte_adder_phase_shortcut_restart/server-results/local_s567c8_g18_o6_s6_tailprefix_matrix_w2/
```

## 2026-08-04 / Q12/Q34 carry-retime cost19 tier0 终态

对完整物理三态 carry-retime 模型的 cost19 tier0 七种成本分解完成终态归档：

```text
exact UNSAT   = 3
exact SAT     = 0
outer UNKNOWN = 4
running       = 0

c19-n13-s3-x0  UNSAT  solve_seconds=2156.52474024595
c19-n11-s4-x0  UNSAT  solve_seconds=5561.884010576003
c19-n9-s5-x0   UNSAT  solve_seconds=6725.074518380978

c19-n1-s9-x0   UNKNOWN  outer watchdog=7200 s
c19-n3-s8-x0   UNKNOWN  outer watchdog=7200 s
c19-n5-s7-x0   UNKNOWN  outer watchdog=7200 s
c19-n7-s6-x0   UNKNOWN  outer watchdog=7200 s
```

三个严格 UNSAT 结果均覆盖完整 `case_indices=0..539`，约束为 `max_delay=4`、三路
deadline `[4,4,4]`、`physical_nets=true`；runner 状态均为 `completed/unsat`、
`return_code=0`，子求解器使用 `--timeout 0`。独立终态审计还核对了成本分解、组件数、
runner 输出 SHA 与 exact JSON 字节 SHA。该审计验证终态及 provenance，不是 DRAT proof
checker。

四个 watchdog 截止任务均没有 exact JSON，且 `status=null`，因此只能记为 UNKNOWN；它们
不能与三个严格 UNSAT 合并成完整 cost19 UNSAT。当前最高价值未决分解为
`c19-n3-s8-x0`，它保持现有 23 门 carry SAT 的 ordinary=3，只减少两只 Switch。

本轮没有产生 `<560` 的完整构型，正式前沿仍为 `80/7/560`。规范证据：

```text
.research/byte_adder_av_reduced_forward/2026-08-04-q12-q34-retime-cost19-tier0终态.md
.research/byte_adder_av_reduced_forward/q12_q34_retime_cost19_tier0_terminal_manifest.json
.research/byte_adder_av_reduced_forward/q12_q34_retime_cost19_tier0_SHA256SUMS.txt
.research/byte_adder_av_reduced_forward/remote-cost19-terminal/
.research/byte_adder_av_reduced_forward/verify_cost19_unsat_terminal.py
```

## 2026-08-04 / 提交：封闭四早期 Switch 的相邻与深交错 D5 wildcard

### 两个固定拓扑类

继续固定 `fixed 73 + high29` 的 `s34567c8_leaf` 接口，对 `S5/S6/S7/C8`、
`gate/delay=18/5`、`components=10`、`Switch/XOR=8/0` 严格求解了两个新位置族：

```text
A = SWITCH,SWITCH,SWITCH,SWITCH,K1,K2,SWITCH,SWITCH,SWITCH,SWITCH
B = SWITCH,SWITCH,SWITCH,SWITCH,K1,SWITCH,K2,SWITCH,SWITCH,SWITCH

K1,K2 in {NOT,AND,OR,NAND,NOR}
```

十个 slot 均有 exactly-one primitive-kind 约束。每族的八个固定 Switch 已占满
`exact_switches=8`，`exact_xors=0` 又排除 XOR，因此两个 wildcard 各自严格自由于五种
ordinary kind；单个 exact solve 即覆盖完整 `5 x 5 = 25` 个有序赋值。`split_slots=1`
切的是固定的 `slot9=SWITCH`，`shard_count=1/shard_index=0` 分配 width-1 universe 的全部
六个签名，不限制 A 的 slot4/5 或 B 的 slot4/6 wildcard。

A 首次完整允许四只 early Switch 拆成两个独立 2-driver resolved BUS 同时喂 K1；B 在保留
该能力的同时，把一只 Switch 放到 K1/K2 之间，使 K1 可以控制或参与中间 Switch，再由 K2
读取后续合法 BUS。两族都保留三态/Z、BUS conflict、public driven、physical-net partition、
递归 D5 timing 与 dead-component 约束。

### 严格终态

```text
A status/solver       = UNSAT / CaDiCaL 195
A variables/clauses  = 174812 / 1340420
A solve/wall seconds = 832.4837622490013 / 832
A wrapper/exit/class = 4175542 / 0 / solver_exit

B status/solver       = UNSAT / CaDiCaL 195
B variables/clauses  = 174812 / 1340420
B solve/wall seconds = 346.87196670199046 / 347
B wrapper/exit/class = 27626 / 0 / solver_exit

watchdog / AS / nice = 900 s / 6291456 KiB / 5
timer_errors         = [] / []
```

两次均由 solver 在 watchdog 前正常退出，不是 UNKNOWN 或截断。独立 auditor 对每族核对
result/log/run 的实际字节 SHA 与 LF、log/result 重合、live worker/dependency SHA、486 行接口、
固定 kind、slot9 shard、25/25 assignment、成本 18、正常 run record 与 solve/wall 一致性；
audit 和 manifest 连续重放稳定。两份 `SHA256SUMS` 均逐项 `7/7` 匹配。

关键 SHA：

```text
# A: SWITCH x4, K1, K2, SWITCH x4
916390da488c9a064052505922e1d89e91c296b5f504eb8d04f8ca48323617cc  result
37cd75507f795bce75ed6c9c5c9fd5c287e941308b9da572cc7330c2f7bd4009  log
a2d1522c890d9ee7429a8c6ae5815ae2959abe7b609156060a91e7bdce2ee44b  run
d5402a8f958a3dd495e0b55524936b7c6486db910e2244e547c8fb9619698962  audit
498f53ae213b9f095768525ed7b81a95e8468ae1087e6603429ee5089c7d09b1  manifest
adfcc4665a27f104355daeb30ec5ae0950e94994fe52712ebf05f8a7efe84e94  SHA256SUMS

# B: SWITCH x4, K1, SWITCH, K2, SWITCH x3
61b9a27f390f6dfc504e79abe92c7698a98cc95757a25e66298f8f08a2be8482  result
1c256c81f5b0567a0f08ebc3e786497b3103809550f8e51d03422c2c57d7f54f  log
4fb0fe2d829ee6e1650521b428c74f69ab393fa3d049162c1aedbddbc0e644f6  run
d445d54524bded5d7ca3e319e138e7032e8b1e9c550efc8c004b0f9af13ff17c  audit
519e32fffa7af350524b7c69ac108a9ec6d5ee50fc2db2cc36a9505646871e65  manifest
7ea1542cc70f5564249e521b16c18d41a654fa5d34fc839745b58bfddb76c5e0  SHA256SUMS
```

这两个结论只封闭上述两个固定十槽拓扑，不能外推成完整 `g18/o2s8` 或完整 cost-18 下界，
也不覆盖其它 ordinary/Switch 位置、不同 component 数、XOR 分解或 shell 联合改写。本轮未
产生 `<560` 候选，正式前沿仍为 `80/7/560`。

规范证据：

```text
.research/byte_adder_han_knowles_fused_agent/2026-08-04-s567c8-o2s8四早期Switch相邻ordinary-wildcard完整UNSAT.md
.research/byte_adder_han_knowles_fused_agent/audit_s567c8_interleave_ssss_oo_s4_wildcard.py
.research/byte_adder_han_knowles_fused_agent/s567c8_interleave_ssss_oo_s4_wildcard_audit.json
.research/byte_adder_han_knowles_fused_agent/s567c8_interleave_ssss_oo_s4_wildcard_manifest.json
.research/byte_adder_han_knowles_fused_agent/s567c8_interleave_ssss_oo_s4_wildcard_SHA256SUMS.txt
.research/byte_adder_han_knowles_fused_agent/2026-08-04-s567c8-o2s8四早期Switch深交错wildcard完整UNSAT.md
.research/byte_adder_han_knowles_fused_agent/audit_s567c8_interleave_ssss_o_s_o_s3_wildcard.py
.research/byte_adder_han_knowles_fused_agent/s567c8_interleave_ssss_o_s_o_s3_wildcard_audit.json
.research/byte_adder_han_knowles_fused_agent/s567c8_interleave_ssss_o_s_o_s3_wildcard_manifest.json
.research/byte_adder_han_knowles_fused_agent/s567c8_interleave_ssss_o_s_o_s3_wildcard_SHA256SUMS.txt
```

## 2026-08-04 / current80 suffix567 exact-cost 9 全 composition 覆盖封闭

对固定 current80 `suffix567` source shell，联合目标为 `S5/S6/S7/C8`，固定
`interface=s6`、`gate_bound=9`、`max_delay=7`、deadline `6,7,7,7`、
`physical_nets=true`。primitive 成本为 ordinary=1、Switch=2、XOR=3，因此 exact-cost 9
的非负整数 composition 必须满足：

```text
components + switches + 2*xors = 9
components >= switches + xors
```

独立枚举得到且仅得到 12 个 composition；每项均以 slot 0 的完整七类
`NOT/AND/OR/NAND/NOR/XOR/SWITCH` 分区。除 `n9_s0_x0` 外，其余 11 项共 `77/77`
个 slot0 shard 全部严格 UNSAT。

`n9_s0_x0` 的 slot0 `AND/NAND/NOR/OR` 四项最初为外部 300 秒 timeout，随后按 slot 1
的五种 ordinary kind 完整拆成 20 项，其中 18 项 UNSAT，`(NOR,NOR)` 与 `(NOR,OR)`
继续 timeout；这两项再按 slot 2 的五种 ordinary kind 拆成 10 项，全部 UNSAT。由于
`exact_switches=0`、`exact_xors=0`，五种 ordinary kind 正是后续 slot 的完整可行 kind
集合。

最终覆盖树为：

```text
composition                     12 / 12
raw jobs                       114
terminal UNSAT leaves          108
timeout parents fully refined    6 / 6
SAT / runtime failure / unknown  0 / 0 / 0
missing / duplicate / orphan     0 / 0 / 0
terminal prefix overlap          0
```

独立审计器逐项核对 10 份 spec、10 份 summary、114 份 record、108 份 UNSAT output、
114 份 log 与 6 个编码依赖，共 362 个输入证据；同时验证 command 展开、composition、
forced prefix、output/log SHA、空 timeout log、wrapper 单元 kind 约束和 exact Switch/XOR
cardinality。`PYTHONHASHSEED=1` 与 `777` 的跨进程输出 SHA 一致：

```text
7acd366220b5cee91ad921e37e1c97b0e3b8f892a442b9f8518763a8783b5fad  audit script
3a494f6676b18a7824694d11b4ef622a3dd08f210c520dbc6d828905c026f691  audit JSON
```

因此同一固定 suffix 的重复 `79/7` 搜索已无新增覆盖价值。该结论不是全局 `79/7` 下界，
不覆盖 upstream source shell 改写、跨 suffix 联合重综合、其它 primitive/BUS/delay 模型；
现有 UNSAT 证据也不是 DRAT/LRAT proof。本轮没有候选、graft 或部署，正式前沿仍为
`80/7/560`。

规范证据：

```text
.research/byte_adder_builder_verify_restart/suffix567_g9_exact_cost9_coverage/audit_suffix567_g9_exact_cost9_coverage.py
.research/byte_adder_builder_verify_restart/suffix567_g9_exact_cost9_coverage/independent_coverage_audit.json
.research/byte_adder_builder_verify_restart/suffix567_g9_exact_cost9_coverage/2026-08-04-suffix567-exact-cost9全composition覆盖封闭.md
.research/byte_adder_builder_verify_restart/suffix567_g9_exact_cost9_coverage/suffix567_g9_exact_cost9_coverage_SHA256SUMS.txt
```

## 2026-08-04 / `bit3:5` 跨边界联合切口 `n14/C5=multi_d2` 完整封闭

以权威 `80/7/560` DAG 的 `bit3:5` 联合切口为基线：原切口含 17 个加权门，固定 shell
含 63 个门；若联合区域能降到 `g<=16`，完整电路即至多 `79/7/553`。本轮在严格 96 行
相关真值域、精确 Z/驱动/BUS 冲突语义、物理网分区、组件活性和逐输出 deadline 下，
完整封闭以下子域：

```text
gate bound = 16
components = 14
C5          = exactly two Switch drivers (multi_d2)
T5 drivers  = 1
S5 drivers  = 1
C5 strict-ancestor count = k0..k12
```

十三个两两不重叠的 normal-form shard 全部由 `cadical195` 正常退出并经独立 terminal audit
判为严格 UNSAT：`13 UNSAT / 0 SAT / 0 UNKNOWN / 0 missing / 0 extra`。累计 solver 时间为
`1878.391126884031 s`，最难片为 `multi_d2_k3`，耗时 `1394.1300354499836 s`。

该子域的完备性来自加权约束 `g = components + switches + 2*xors <= 16`：在 `n=14`
且 C5 已使用两个 Switch 时，强制总计 `switches=2,xors=0`；C5 消耗全部 Switch，T5/S5
又已由 96 行静态等价检查排除 paid source 与常量，因此二者只能各用一个 component driver。
`n=14,d=2` 的合法 C5 严格祖先数恰为 `k=0..12`。这不是全局 `g16` UNSAT；仍不覆盖
C5 的 `source`、`single_k*`，或较小 component count 下允许额外 Switch/XOR 的类别。

SAT 接受链已用独立 g17 证书正回归：96 行证书重放 PASS；graft 回权威 shell 后完整电路为
`80/7/560`，独立重放全部 `131072` 行，九输出 mismatch、BUS conflict、输出 Z、死节点均为
零。后续若出现 `g<=16` SAT，仍必须依次执行 transport SHA、独立 96 行重放、graft、独立
131072 行重放，并要求最终 `gate<=79,delay<=7,energy<560`。

关键证据：

```text
.research/byte_adder_cross_boundary_joint_restart/2026-08-04-bit35-n14-multi-d2-完整封闭.md
.research/byte_adder_cross_boundary_joint_restart/bit35_joint_g16_n14_c5_multi_d2_t1_s1_complete_audit.json
  SHA-256 e803d21011b30bf9dea2a9d267480ce1ed93992c60829de7a66f8de6b04a7fa1
  collection SHA-256 e43bd4255055058b3b5b21ec92fc70b26274a914e22d1103d8ca6c3374c21249
.research/byte_adder_cross_boundary_joint_restart/bit35_joint_g16_n14_multi_d2_SHA256SUMS.txt
  SHA-256 edc08d5611b51377dc337e028c37f2677c0be4b5fbcdecb13a41b8c2b7df4b7e
.research/byte_adder_cross_boundary_joint_restart/bit35_joint_g17_grafted_full_dag.json
  SHA-256 c5134186e0da67d6982b2cef2cfa3427fcef083992630f6841ac6f70e4db34d9
.research/byte_adder_cross_boundary_joint_restart/bit35_joint_g17_grafted_full_independent_verify.json
  SHA-256 27622d0894cc552e656f76b76ab65b67306748548190237e7a5cad1a9f4af8bc
```

下一完整方向为 `g16/n13/C5=multi_d2/T5=1/S5=1`，顺序
`k3,k2,k4,k1,k5,k0,k6,k7,k8,k9,k10,k11`；当前按单 worker 串行推进。本轮没有候选、
graft 或部署，正式前沿仍为 `80/7/560`。

## 2026-08-04 / 提交：封闭五早期 Switch 的相邻 ordinary D5 wildcard

### 固定拓扑与覆盖

在同一个 `fixed 73 + high29`、`s34567c8_leaf` 接口上继续搜索：

```text
SWITCH,SWITCH,SWITCH,SWITCH,SWITCH,K1,K2,SWITCH,SWITCH,SWITCH
K1,K2 in {NOT,AND,OR,NAND,NOR}

outputs     = S5,S6,S7,C8
gate/delay  = 18/5
components  = 10
Switch/XOR  = 8/0
```

八个固定 Switch 占满 `exact_switches=8`，`exact_xors=0` 排除 XOR，因此 slot5/slot6
两个 wildcard 各自只能且必须取五种 ordinary kind 之一。`split_slots=1` 仍切固定的
`slot9=SWITCH`，单个 `shard_count=1/shard_index=0` solve 分配全部六个 width-1 签名，
不限制两个 wildcard；故一次 exact solve 完整覆盖 `5 x 5 = 25` 个有序赋值。

该位置族把 early Switch 数量从四增加到五，完整允许 3-driver 与 2-driver 两条独立
conflict-free resolved BUS 同时喂给 K1。模型保留三态/Z、BUS conflict、public driven、
physical-net partition、递归 D5 timing 和 dead-component 约束。

### 严格终态与审计

```text
status/solver       = UNSAT / CaDiCaL 195
variables/clauses  = 174812 / 1340420
solve/wall seconds = 445.3284288670402 / 445
wrapper/exit/class = 67976 / 0 / solver_exit
watchdog / AS/nice = 900 s / 6291456 KiB / 5
timer_errors       = []
```

solver 在 watchdog 前正常退出。独立 auditor 核对 result/log/run 的实际磁盘 SHA 与 LF、
log/result 重合、live worker/dependency SHA、486 行接口、fixed kinds、slot9 shard、25/25
assignment、成本 18 与 solve/wall 一致性；连续三次输出稳定为 `unsat-covered`，
`missing=0, invalid=0`。`SHA256SUMS` 逐项 `7/7` 匹配。

```text
a4cfe04b54b576a0becb0bc1c2643e7877533567476a7cf37a44293df1876e95  result
e5194c0242a3c7a919bfd4ba91fb155795182d911f8d9a9bac352f479a91c153  log
9682c1b0fc7ce5372f0c54c6da880bc05c23adc111d02b452a0178bac2c4bd86  run
eee03ee1e2a5d6aa59909daf2d00837e8411094d29dbaaa4a030ac5ee8c92a2a  audit
547ac82d5535214dc310ecba917d4dd6394f2ef7b0b8ec9886c362f9a52376c6  manifest
df8959da345c36fed45fcb39d2fe9108184aaf6dd4281ac0e63c2e659bce0898  SHA256SUMS
```

该结果只封闭上述固定十槽 topology，不能外推成完整 `g18/o2s8` 或完整 cost-18 下界；
其它 ordinary/Switch 位置、component 数、XOR 分解和 shell 联合改写仍未覆盖。没有产生
`<560` 候选，正式前沿仍为 `80/7/560`。

规范证据：

```text
.research/byte_adder_han_knowles_fused_agent/2026-08-04-s567c8-o2s8五早期Switch相邻ordinary-wildcard完整UNSAT.md
.research/byte_adder_han_knowles_fused_agent/audit_s567c8_interleave_s5_oo_s3_wildcard.py
.research/byte_adder_han_knowles_fused_agent/s567c8_interleave_s5_oo_s3_wildcard_audit.json
.research/byte_adder_han_knowles_fused_agent/s567c8_interleave_s5_oo_s3_wildcard_manifest.json
.research/byte_adder_han_knowles_fused_agent/s567c8_interleave_s5_oo_s3_wildcard_SHA256SUMS.txt
```

## 2026-08-04 / 提交：封闭五早期 Switch 深交错 D5 wildcard

### 固定拓扑与完整覆盖

紧接五 early Switch 相邻普通门族，继续只交换一个 ordinary 与 Switch 的位置：

```text
SWITCH,SWITCH,SWITCH,SWITCH,SWITCH,K1,SWITCH,K2,SWITCH,SWITCH
K1,K2 in {NOT,AND,OR,NAND,NOR}

outputs     = S5,S6,S7,C8
gate/delay  = 18/5
components  = 10
Switch/XOR  = 8/0
```

固定 Switch slots 为 `0,1,2,3,4,6,8,9`，恰好占满 `exact_switches=8`；
`exact_xors=0` 排除 XOR，因此 slot5/slot7 两个 wildcard 各自严格自由于五种 ordinary
kind。`split_slots=1` 只切固定的 `slot9=SWITCH`，单个 shard 分配全部六个 width-1
签名，不限制 wildcard；一次 exact solve 完整覆盖 `5 x 5 = 25` 个有序赋值。

该族同时保留五 early Switch 的 3-driver + 2-driver 双 BUS 能力和 K1/K2 之间的中间
Switch。完整物理模型继续保留三态/Z、BUS conflict、public driven、physical-net partition、
递归 D5 timing 与 dead-component 约束。

### 严格终态与审计

```text
status/solver       = UNSAT / CaDiCaL 195
variables/clauses  = 174812 / 1340420
solve/wall seconds = 110.4526788610383 / 111
wrapper/exit/class = 95609 / 0 / solver_exit
watchdog / AS/nice = 900 s / 6291456 KiB / 5
timer_errors       = []
```

solver 正常退出而非 watchdog/UNKNOWN。独立 auditor 校验 result/log/run 的实际 SHA 与 LF、
log/result 重合、live worker/dependency SHA、486 行接口、fixed kinds、slot9 shard、25/25
assignment、成本 18 和 solve/wall 一致性；连续三次均为 `unsat-covered`，
`missing=0, invalid=0`。`SHA256SUMS` 逐项 `7/7` 匹配。

```text
fe0cdb48ed75213450dcca28bbd97952ffea04c5a35dfd7c846656b5fddba3a9  result
a0b2aa447af8d9703f7b571d57325bcdca27f413b599d716f092eb2504a3e196  log
c9c319f702ae59c7447373c4573ba4d3192c826da0245637bf09af965c33df68  run
db009172585bfda99104e7bc4ac17c510f771151bd688339e81ce4a71e580673  audit
d38306621b34860c64347a0936f2b838a8762ab2347f43d07d34626471b45849  manifest
61d9df32b6ea74ce7cd6485f8ea6a65f881ddd4cdc82499b68667aaba03bc743  SHA256SUMS
```

该结果只封闭上述固定十槽 topology，不是完整 `g18/o2s8` 或 cost-18 下界；其它位置、
component 数、XOR 分解和 shell 联合改写仍未覆盖。没有产生 `<560` 候选，正式前沿仍为
`80/7/560`。

规范证据：

```text
.research/byte_adder_han_knowles_fused_agent/2026-08-04-s567c8-o2s8五早期Switch深交错wildcard完整UNSAT.md
.research/byte_adder_han_knowles_fused_agent/audit_s567c8_interleave_s5_o_s_o_s2_wildcard.py
.research/byte_adder_han_knowles_fused_agent/s567c8_interleave_s5_o_s_o_s2_wildcard_audit.json
.research/byte_adder_han_knowles_fused_agent/s567c8_interleave_s5_o_s_o_s2_wildcard_manifest.json
.research/byte_adder_han_knowles_fused_agent/s567c8_interleave_s5_o_s_o_s2_wildcard_SHA256SUMS.txt
```

## 2026-08-04 / current80 输入排列 512 扩展完整封闭

根线把 reachable residual 的 deterministic input permutation 从既有 `0..63` 扩展到
`[64,576)`，并对每个排列检查 13 个 output phase：

```text
static tasks                 = 512 * 13 = 6656
static completed/errors      = 6656 / 0
D6 timing recipes            = 4
timing records               = 26624
timing worker/recipe errors  = 0 / 0
static/timing best           = 85/7/595
delay<=6 / energy<560        = 0 / 0
ABC reported D6 unmet        = 26624 / 26624
```

独立审计完整检查两份 summary 的范围、确定性 permutation、record key、成本/延迟/能耗和
recipe 覆盖；另抽取并列最佳 `perm72/phase087`，对其 23328 个 care point、output phase
恢复、原始映射与四个 timing 映射执行权威 importer 双构建。结果为 `5/5 accepted`、
`0 rejected`、`0 hit`，审计 SHA-256 为：

```text
9fe2a1900a6c154ad20d8f0af948d2cd3c0aaeaffdfd2aa2f49e3902d842147f
```

这只封闭指定 permutation/phase/Espresso/ABC recipe 组合，不是全局网络下界。本轮没有
候选可覆盖，正式存档仍为用户已验收的 `80/7/560`，且没有启动游戏。完整报告与证据：

```text
.research/byte_adder_root/2026-08-04-current80输入排列512扩展完整封闭.md
.research/byte_adder_root/abc_residual_current80/care_pla/input_permutation_064_575x13/
.research/byte_adder_root/input_permutation_064_575x13_SHA256SUMS.txt
```

## 2026-08-04 / 提交：封闭六早期 Switch 的相邻 ordinary D5 wildcard

### 固定拓扑与完整覆盖

在 `fixed 73 + high29` 的 `s34567c8_leaf` 接口继续搜索成本 18、D5 的四输出尾部：

```text
SWITCH,SWITCH,SWITCH,SWITCH,SWITCH,SWITCH,K1,K2,SWITCH,SWITCH
K1,K2 in {NOT,AND,OR,NAND,NOR}

outputs     = S5,S6,S7,C8
components  = 10
Switch/XOR  = 8/0
```

固定 Switch slots `0,1,2,3,4,5,8,9` 占满 `exact_switches=8`，`exact_xors=0`
排除 XOR，因此 slot6/slot7 两个 wildcard 各自严格自由于五种 ordinary kind。
`split_slots=1` 只切固定 `slot9=SWITCH`，一个 shard 分配 width-1 universe 全部六个签名，
不限制 wildcard；单次 exact solve 完整覆盖 `5 x 5 = 25` 个有序赋值。

这是目前该线最强的相邻 ordinary early-BUS 位置族：六只 early Switch 可拆成两条独立
3-driver conflict-free resolved BUS，同时喂给 K1 的两个输入。模型保留三态/Z、BUS conflict、
public driven、physical-net partition、递归 D5 timing 与 dead-component 约束。

### 严格终态与审计

```text
status/solver       = UNSAT / CaDiCaL 195
variables/clauses  = 174812 / 1340420
solve/wall seconds = 436.59204191801837 / 437
wrapper/exit/class = 114346 / 0 / solver_exit
watchdog / AS/nice = 900 s / 6291456 KiB / 5
timer_errors       = []
```

solver 在 watchdog 前正常退出。独立 auditor 核对 result/log/run 实际 SHA 与 LF、log/result
重合、live worker/dependency SHA、486 行接口、fixed kinds、slot9 shard、25/25 assignment、
成本 18 与 solve/wall 一致性；连续三次均为 `unsat-covered`，`missing=0, invalid=0`。
`SHA256SUMS` 逐项 `7/7` 匹配。

```text
16a6a9cc71cd2597a88bca3fd97e7976632d02d7bcde983a556f2f7600d73994  result
c51a2039721d574cf0cd30020a1e819d3a2e5e5199b9a737f73f3664a10db444  log
d300c387b1b24da7e4d2dd75807287f21a59c7c7cc7f720c03c62dbd41fd5172  run
843fe6dfcf1e4c2604f40eb2ba271892f67dbf648ba81f0a43f0aed9b7c83d48  audit
6cb35b4d87ff09776378d64490393dc696e9de193ebe76443e99b360c0fae6a2  manifest
4729a3a731f3f62152fe6c8a30bb74907bb50b767b3aac68ba422e54e7247715  SHA256SUMS
```

该结果仍只封闭上述固定十槽 topology，不是完整 `g18/o2s8` 或 cost-18 下界；其它位置、
component 数、XOR 分解和 shell 联合改写未被覆盖。没有 `<560` 候选，正式前沿仍为
`80/7/560`。

规范证据：

```text
.research/byte_adder_han_knowles_fused_agent/2026-08-04-s567c8-o2s8六早期Switch相邻ordinary-wildcard完整UNSAT.md
.research/byte_adder_han_knowles_fused_agent/audit_s567c8_interleave_s6_oo_s2_wildcard.py
.research/byte_adder_han_knowles_fused_agent/s567c8_interleave_s6_oo_s2_wildcard_audit.json
.research/byte_adder_han_knowles_fused_agent/s567c8_interleave_s6_oo_s2_wildcard_manifest.json
.research/byte_adder_han_knowles_fused_agent/s567c8_interleave_s6_oo_s2_wildcard_SHA256SUMS.txt
```

## 2026-08-04 / 提交：封闭双 Switch 前置的相邻 ordinary D5 矩阵

Phase 线在冻结的 `fixed 73 + high residual 29`、486 行 paid-source 接口上完整检查：

```text
NOT,NOR,OR,OR,SWITCH,SWITCH,K1,K2,SWITCH,SWITCH,SWITCH,SWITCH
K1,K2 in {NOT,AND,OR,NAND,NOR}

outputs/components = 4 / 12
ordinary/Switch/XOR = 6 / 6 / 0
gate/delay bound    = 18 / 5
```

与此前 terminal-Switch 族不同，这一拓扑允许 `K1/K2` 读取 slot4/slot5 形成的 resolved
Switch BUS，后四只 Switch 再读取 ordinary 结果。`exact_switches=6`、`exact_xors=0`
使两个 wildcard 严格只覆盖五种 ordinary，因而 `5 x 5 = 25` 个有序对就是该固定槽序的
完整 kind 空间。

最终结果：

```text
strict UNSAT = 25 / 25
SAT/UNKNOWN/timeout = 0 / 0 / 0
missing/invalid/overlap = 0 / 0 / 0
variables/clauses per shard = 233910 / 1735683
solve seconds min/max/sum = 34.604754567029886 / 207.33021329698386 / 2477.015154790948
```

独立 preflight、远端 auditor、本地 auditor 和 crosscheck 均通过；另用 `S7/C8` 正见证
校准 component order、成本、递归 D5、Z/BUS、physical-net partition 与 dead-component
语义。最终 39 项证据清单逐项匹配，清单 SHA-256 为：

```text
48fa8ae8e1627f03b9bca40f551ce79f2850811435705f4ff447893fe50415c8
```

该结论只封闭这一个 `components=12/o6/s6` 精确槽序，不是任意成本 18 Switch 网络或
完整字节加法器下界；没有 `<560` 候选，正式存档保持 `80/7/560`，且没有启动游戏。

规范证据：

```text
.research/byte_adder_phase_shortcut_restart/2026-08-04-s567c8-g18-o6s6双Switch前置相邻ordinary完整5x5.md
.research/byte_adder_phase_shortcut_restart/s567c8_g18_o6_s6_midbus_matrix_SHA256SUMS.txt
.research/byte_adder_phase_shortcut_restart/ubuntu_s567c8_g18_o6_s6_midbus_matrix_w1_audit.json
.research/byte_adder_phase_shortcut_restart/ubuntu_s567c8_g18_o6_s6_midbus_matrix_w1_crosscheck.json
```

## 2026-08-04 / 提交：封闭六早期 Switch 深交错 ordinary 尾部

Han 线继续检查 `fixed 73 + high residual 29` 的 486 行 D5 接口，本次固定槽序为：

```text
SWITCH,SWITCH,SWITCH,SWITCH,SWITCH,SWITCH,K1,SWITCH,K2,SWITCH
K1,K2 in {NOT,AND,OR,NAND,NOR}
components/o/s/x = 10 / 2 / 8 / 0
gate/delay       = 18 / 5
```

八个固定 Switch 已占满 quota，两个 wildcard 因而严格只覆盖五种 ordinary；单次 exact
solver 自由选择全部 BUS、Z、driver 与连线关系，完整覆盖 `25/25` 个有序 kind 对。与上一族
的相邻 ordinary 不同，slot7 的中间 Switch 可以读取 K1，K2 又可以消化该 resolved BUS，
属于新的深交错拓扑。

```text
status/solver       = UNSAT / CaDiCaL 195
variables/clauses  = 174812 / 1340420
solve/wall seconds = 265.6077585099847 / 266
exit/class         = 0 / solver_exit
ordered pairs      = 25 / 25
missing/invalid    = 0 / 0
```

独立 auditor 连续重放稳定，result/log/run、worker/dependency、486 行接口、physical-net、
Z/BUS、D5、25 个 assignment 和资源记录均匹配；七项证据清单 `7/7` 通过，清单 SHA-256：

```text
fda067bcf89b0af659c315589a92fd36b202df036826ebeb1b94cf30d1cbb4a7
```

该结果只封闭上述固定十槽 topology，不是完整 cost-18 下界；没有生成 `<560` 候选，正式
存档仍为 `80/7/560`，游戏未启动。

规范证据：

```text
.research/byte_adder_han_knowles_fused_agent/2026-08-04-s567c8-o2s8六早期Switch深交错wildcard完整UNSAT.md
.research/byte_adder_han_knowles_fused_agent/audit_s567c8_interleave_s6_o_s_o_s1_wildcard.py
.research/byte_adder_han_knowles_fused_agent/s567c8_interleave_s6_o_s_o_s1_wildcard_audit.json
.research/byte_adder_han_knowles_fused_agent/s567c8_interleave_s6_o_s_o_s1_wildcard_manifest.json
.research/byte_adder_han_knowles_fused_agent/s567c8_interleave_s6_o_s_o_s1_wildcard_SHA256SUMS.txt
```

## 2026-08-04 / 提交：封闭 bit3:5 的 n13 双驱动 C5 normal form

Conditional 线在 96 行严格相关域上，把 `g16/n13` 中以下完整子域拆成十二个互斥片：

```text
C5 = exactly two Switch drivers
T5 = exactly one component driver
S5 = exactly one component driver
C5 strict-ancestor count k = 0..11
gate/components/delay bound = 16 / 13 / 5
```

成本恒等式 `g = components + switches + 2*xors <= 16` 与 C5 已消费两只 Switch 一起
强制 `xors=0`、全网至多再有一只 Switch；96 行静态等价又排除了 T5/S5 为 paid source
或常量。故 T5/S5 的多驱动需要至少两只额外 Switch，超出预算，十二个 `k=0..11`
分片确实覆盖这一 normal-form 类的全部可能，而非抽样。

```text
expected/observed shards = 12 / 12
strict UNSAT             = 12
SAT/UNKNOWN              = 0 / 0
missing/extra            = 0 / 0
total solve seconds      = 731.327804572997
hardest shard            = k3 / 346.94092824304244 s
```

聚合 auditor 独立扫描每片的 result/run/launcher/terminal-audit，重算 constraint、phase、
依赖与 transport SHA；最终 `status=pass`、`complete=true`、`errors=[]`：

```text
aggregate audit SHA-256 = 4bc837956f9146743a24f8fb27b04bea19d4144e90e858b81a779ba423a06dae
collection SHA-256      = 22134ebce4e938f0938506d247dc5663ed120c5b0ecd41bfd0f9b5a3e703ccca
SHA256SUMS (60 entries) = 7ec1f13ca23e6f2aa029d6b9dc7b6f0469ab3684f07051425ec6789f7e503715
```

该结论不覆盖 C5 singleton、三 Switch C5 或较小 component count，不是全局 g16
UNSAT。没有 SAT 可 graft，正式存档保持 `80/7/560`，游戏未启动。

规范证据：

```text
.research/byte_adder_cross_boundary_joint_restart/2026-08-04-bit35-n13-multi-d2-完整封闭.md
.research/byte_adder_cross_boundary_joint_restart/bit35_joint_g16_n13_multi_d2_t1_s1_complete_audit.json
.research/byte_adder_cross_boundary_joint_restart/audit_bit35_joint_n13_multi_d2_complete.py
.research/byte_adder_cross_boundary_joint_restart/bit35_joint_g16_n13_multi_d2_SHA256SUMS.txt
```

## 2026-08-04 / 提交：封闭混合门局部任意扇出

Root 线补齐了 S2/S4 私有根 exact-cost-4 分类中，公式树不能表达的最后一种
局部拓扑：两个普通门加一个 Switch、三个组件允许任意内部扇出。S2/S4 在原
source pool 和额外加入免费 `0/1` 的两种域中，CaDiCaL195 与 Glucose42 均对
完全相同 CNF 返回 UNSAT：

```text
negative records = 8 / 8 strict UNSAT
cross-solver CNF = 4 / 4 identical
positive controls = 4 / 4 SAT
full replay rows per positive = 131072
positive mismatch/Z/conflict/partition/dead = 0/0/0/0/0
```

独立审计器没有导入 SAT worker，重新建立 48 行 source partition，并完整重放
四个五普通门正见证；总计 12/12 记录通过，`errors=[]`。负例没有 proof trace，
所以结论只限明确 source pool 的局部双求解器封闭，不是全局 `79/7` 下界。
没有产生 `<560` 候选，正式存档保持 `80/7/560`，游戏未启动。

规范证据：

```text
.research/byte_adder_root/2026-08-04-字节加法器混合门任意扇出局部封闭.md
.research/byte_adder_root/exact_80d7_weighted_cost4_fanout.py
.research/byte_adder_root/audit_80d7_weighted_cost4_fanout.py
.research/byte_adder_root/test_audit_80d7_weighted_cost4_fanout.py
.research/byte_adder_root/weighted-cost4-fanout-80d7-audit.json
.research/byte_adder_root/weighted_cost4_fanout_80d7_SHA256SUMS.txt
```

## 2026-08-04 / 提交：复用 S3/S4 已付相位并分片搜索 102/5

Root 线发现既有 `S3/S4=11/D5` 见证中，前七个普通门输出此前没有暴露给
`S5/S6/S7/C8` 尾部。新接口逐门重放并复用 `u0..u6`，到达时间为
`[3,4,4,3,4,4,5]`，其中 `u6==S3`；S4 的两个 Z/Switch driver 明确不作为
Boolean source。若尾部能在 `18/D5` 内完成，完整结构将达到 `102/5/510`。

包装器本机/远端回归均通过 486 行、见证 SHA、到达时间和零组件直连 SAT。首批四种
无 XOR 分解 `o2/s8`、`o4/s7`、`o6/s6`、`o8/s5` 均在 1200 秒外层 watchdog
处返回 `UNKNOWN`，没有被误记为 UNSAT。

另完整封闭了 `o2/s8` 的“两个普通门先行、每输出恰为两只 Switch”的受限正规形：

```text
independent one-gate functions / pairs = 1118 / 625521
dependent first functions / pairs      = 752 / 67847
representable S5/S6/S7/C8              = 0/0/0/0
```

该结论不覆盖 Switch->ordinary 交错结构，不是全局 `o2/s8` 下界。当前已把高价值
`o4/s7`、`o6/s6`、`o8/s5` 拆成 `3 x 16` 个互斥 suffix shard，以 8 workers
持续搜索。全局合并器会恢复九组件 S3/S4 网络，并重新检查 BUS/Z、分区、liveness 和
D5；独立审计发现的 NOT 非空右 BUS 契约缺口也已修复并加入负回归。

目前没有完整 SAT 候选，因此正式存档保持用户已验收的 `80/7/560`，游戏未启动。

规范证据：

```text
.research/byte_adder_paper_synthesis_root/2026-08-04-S3S4已付相位复用与尾部精确搜索.md
.research/byte_adder_paper_synthesis_root/exact_tail_with_s34_free.py
.research/byte_adder_paper_synthesis_root/combine_s34_free_tail_high29.py
.research/byte_adder_paper_synthesis_root/audit_s34_free_o2s8_output_pair_normal_form.py
.research/byte_adder_paper_synthesis_root/s34-free-o2s8-output-pair-normal-form-audit.json
.research/byte_adder_paper_synthesis_root/remote-sweeps/s34_free_tail_g18_20260804_r1/summary.json
```

## 2026-08-04 / 提交：建立全原语物理副产物目录并核实 7/4 全加器

本轮不再把全加器、超前进位、条件和等命名结构当作搜索空间边界，而是把所有合法
原语及其显式展开网络中的每一个真实节点作为独立资产。统一记录字段为：

```text
value / driven / Z / conflict / arrival / input arcs / physical owner
```

首版目录覆盖普通门、XOR/XNOR 展开、三输入门展开、Full Adder、Switch、完整
多驱动 BUS、word lane、Maker/Splitter 与当前 `80/7` DAG。独立审计重新计算全部
压缩 mask、Boolean/physical truth SHA、owner set 与 Pareto 前沿，并完整重放当前
电路：

```text
domains / truth classes / producers = 6 / 155 / 295
packed masks / owner sets           = 166 / 245
current nodes / Switch drivers      = 82 / 10
embedded expansion hits             = 22
131072-row mismatch/conflict/Z       = 0 / 0 / 0
catalog SHA-256                      = 387fae011a0acc49db710704132d07f7acc74f9cb415a3919ee1cedcc7d4f9a6
independent audit SHA-256            = 5cd2a33c6770c3ae4da077c267691a0c5b32114c271617dcddf7990bfa5d9ba4
```

这里没有把“主输出真值相同”误当成可复用：Switch partial driver 仍由各自实体拥有，
resolved BUS 另存完整 driver owner set；只有整组 owner 可复制、同一赋值下无 0/1
冲突且到达时间满足时，才允许进入全局 graft。首轮直接 truth reuse 没有发现净省门
命中，但目录已经把 88 个副产物角色和 73 个中间角色稳定暴露给后续 connected-cut
与跨架构拼接器。

同时核实了 Full Adder 子关卡的真实导入链。当前正式 Full Adder 候选本身通过：

```text
gate / delay / energy = 7 / 4 / 28
truth rows             = 8 / 8
connectivity/geometry  = 0 / 0
v15 rebuild            = byte-identical
candidate SHA-256      = 9f83306a02ed064f7eb834b874daf786202651e73beb8d0f7ce3050f221572b2
formal audit SHA-256   = a166bdf0409a888cfa0eb5419ac9f34bfca0dfa056b8f2a3c8df993c0703be6c
```

静态成本链证明：只有用户在游戏中真实完成该关卡后，`7/4` 才会经
`complete_level -> add_cost` 进入有效 frontier，并严格支配旧的 `16/8`；没有手改
`levels.txt` 或伪造成绩。把它替换到当前 Byte Adder bit0 后，完整实物链的真实结果是：

```text
gate / delay / energy = 77 / 9 / 693
131072-row mismatch   = 0
BUS/Z/owner/timing    = pass
v15/connectivity      = pass
candidate SHA-256     = 16a0601651ff41a5c31050b663fe5835e69db404aa2d00f69e1a2fbc221879b4
audit SHA-256         = 93b437fbfd11135f9ba6bf91e975dda640c92088b0b40920c524a02d0365bf89
```

这明确否定了先前粗略的 `77/7` 估计：opaque Full Adder 的 carry 到达为 4，接入当前
后级后输出到达变为 `[4,6,9,7,9,8,9,9,9]`。因此该候选只作为正确的组件语义与
物理 graft 回归，不覆盖正式 Byte Adder 存档；正式存档仍保持用户已验收的
`80/7/560`，游戏未启动。

规范证据：

```text
.research/byte_adder_component_byproduct_catalog/audit_truth_byproduct_catalog.py
.research/byte_adder_component_byproduct_catalog/truth-byproduct-catalog-v1.audit.json
.research/byte_adder_component_byproduct_catalog/build_full_adder_effective_cost_audit.py
.research/byte_adder_component_byproduct_catalog/full-adder-effective-cost-audit-2.1.292.json
.research/byte_adder_component_byproduct_catalog/full-adder-effective-cost-audit-2.1.292.md
.research/byte_adder_builder_verify_restart/materialize_kind15_bit0_candidate.py
.research/byte_adder_builder_verify_restart/test_materialize_kind15_bit0_candidate.py
.research/byte_adder_builder_verify_restart/full_adder_7_4_intake/formal_9f83306a_r1/audit.json
.research/byte_adder_builder_verify_restart/kind15_bit0_intake/g77_d9_r2/audit.json
```

## 2026-08-04 / 提交：完成 23 种前置元件、零延迟归一化与 owner 约束目录

在上一版 truth producer 清单之上，本轮直接从 `Turing Complete 2.1.292` 当前 EXE 的
prototype 表、23 个 codegen switch case、成本导入函数和关卡资产建立全原语目录。目录不把
生成代码里的局部变量误认成可接线引脚：prototype 公共输出数与 codegen 写出数逐 kind
一致，未发现任何隐藏可接线 native 输出。

```text
component kinds                 = 23
native owner groups             = 23
Maker/Splitter exact constraints= 6
self checks                     = 35 / 35 pass
component catalog SHA-256       = 2f193e52e0fb554c72cdc97310409f691ff8e32ecb2b405b6e3efe49a94d1347
Chinese report SHA-256          = 4b2510bd7ac576f8dd3f5da0887c91e45c160521488175fab5387aff07c358d9
codegen cases SHA-256           = 8ea88edd7e7dd46ec840400a6fcba287611040eac8bbeb6cc2d47f42dbcab43b
```

Maker/Splitter 不再按“透明布线”建模。当前 codegen 的精确语义是：

```text
input side       = 读取 data plane，Z 当作 0
output side      = ordinary active driven output
gate / delay     = 0 / 0
conflict         = 上游短路必须 fail-closed，不能被归一化吞掉
physical effect  = 新输出网形成 owner barrier
```

因此 `Maker2/4/8 -> Splitter2/4/8` 可以合法地产生 fully-driven lane clone，并与原
may-Z net 并存；但不能免费复制上游 partial driver 的 physical owner，也不能把冲突解释成
Boolean 值。该 producer 已送入 Switch/BUS atlas、跨架构 hyperedge 搜索与通用物化链。

本目录的 `80/7/560` truth replay 使用研究 DAG：

```text
.research/byte_adder_av_reduced_forward/byte-adder-hybrid-shared-s34-audit-g80-d7.json
structural SHA-256 = 67587e25f156565af5539a0e78dc3ea183786c63a658ea12b7a3d088f2adbe2a
output arrivals   = [4,4,7,5,6,6,7,7,7]
```

它与正式存档中的 `phasefold` DAG 都通过 131072 行，但不是同一个结构。正式存档仍是用户
已验收的：

```text
.research/byte_adder_root/byte-adder-hybrid-phasefold-g80-d7.json
structural SHA-256 = ba31029a5b3fa05c180a1f6ce23d90140dbaed75b4dd22bd19b7090ed3e1d15f
formal circuit SHA-256 = fbedb28b185c36d6c99db71ffe2086d5ff8127e7999a6bf84054843fb2cf47bf
```

二者在研究库中分别保留，绝不把未实机验收的 `shared-s34` 冒充正式前沿。独立 truth audit
对研究 DAG 再次得到 `131072` 行、`80/7`、10 个 Switch partial driver，九输出
mismatch/conflict/Z 均为 0，SHA-256：

```text
30d9bcfdebd01d3211ca113497defdc8007c8ee45e0ce780130a37ba9cbfe8d7
```

最后，先前资料中的 `79/7/553` 是公开排行榜 Pareto 点和若干局部投影目标，不是本项目已经
生成的完整候选。本轮重新核对后，当前可部署且实机验收的项目前沿仍只有 `80/7/560`。

规范证据：

```text
.research/byte_adder_component_byproduct_catalog/audit_component_catalog.py
.research/byte_adder_component_byproduct_catalog/component-catalog-v1.json
.research/byte_adder_component_byproduct_catalog/2026-08-04-字节加法器全元件副产物与短弧审计.md
.research/byte_adder_component_byproduct_catalog/ida_export_pretarget_codegen_cases.py
.research/byte_adder_component_byproduct_catalog/pretarget-codegen-cases-2.1.292.json
.research/byte_adder_component_byproduct_catalog/build_truth_byproduct_catalog.py
.research/byte_adder_component_byproduct_catalog/truth-byproduct-catalog-v1.json
.research/byte_adder_component_byproduct_catalog/truth-byproduct-catalog-v1.audit.json
```

## 2026-08-04 / 提交：建立 FullAdder 7/4、10/3、17/2 三点物理前沿

本轮没有把 Full Adder 当作 Byte Adder 的唯一架构，而是把它作为一个可选的双输出
物理 tuple，分别搜索不同延迟预算下的真实实现。结果得到三条互不支配的显式关系：

```text
7 gate / 4 delay   七普通门展开，保留全部中间相位
10 gate / 3 delay  六普通门 + 两 Switch，指定 late input 到双输出均有两级短弧
17 gate / 2 delay  五普通门 + 六 Switch + 两只 0/0 normalizer
```

`10/3` 已物化为 v15 `circuit.data`，并通过独立的 truth、连通、几何、BUS/Z、owner
partition、timing 与 byte-identical rebuild 审计：

```text
truth assignments                    8 / 8
mismatch / conflict / undriven       0 / 0 / 0
ordinary gates / Switch              6 / 2
candidate SHA-256                    18b8e25952d00499f093bdad61f70a5374205bf2d71074e70881d899f04ea4a5
independent physical audit SHA-256   abb90f72ebeFD3bb26a95f304c2bcc01131bd04b8a6830a17ac84ea5628ae66c
```

完整 `gate<=9, delay<=3` 物理闭包的 90 个分片全部 UNSAT，因此 `10/3` 是当前
reviewed library 中 D3 的 gate 最小点。该结论包含普通门、Switch、Z/BUS、
Maker/Splitter normalizer 与 physical-net partition，但不外推未知的新原语。

另对当前 `80/7` 的 bit0 具名二输出切口固定 `SUM<=4`、`MAJ<=2`、`gate<=9`，45 个
严格物理分片全部 UNSAT；这只封闭该 connected cut，不是全局 `79/7` 下界。

D1 结构枚举证明三输入 parity 无法由一层普通门、raw Switch、direct BUS 或 normalized
BUS 构成。D2 找到 `17/2` 严格 fully-driven witness；六个最接近的 `gate=16`
composition 为 UNSAT，但尚未完整覆盖所有 `<=16` 结构，所以没有宣称 `17/2` gate
最小。

规范证据：

```text
.research/byte_adder_pair_cross_restart/2026-08-04-FullAdder-10-3与双前沿及局部闭包审计.md
.research/byte_adder_pair_cross_restart/full_adder_10_3/candidate.circuit.data
.research/byte_adder_pair_cross_restart/full_adder_10_3/physical_certificate.json
.research/byte_adder_pair_cross_restart/full_adder_10_3/dual_frontier_audit.json
.research/byte_adder_component_byproduct_catalog/fa_g9_d3_physical_closure/manifest.json
.research/byte_adder_pair_cross_restart/bit0_sum4_maj2_g9_physical_closure/manifest.json
.research/byte_adder_pair_cross_restart/full_adder_d1_unreachable_audit.json
.research/byte_adder_pair_cross_restart/truth_tuple_cases/fa_d2_g17_p11_n2_s6_x0.json
.research/byte_adder_pair_cross_restart/full_adder_d2_g17_independent_audit.json
```

## 2026-08-04 / 提交：按当前 levels.txt 冻结全部元件真实成本

Root 线新增只读审计，把当前 EXE 默认成本、游戏真实登记成绩和未登记研究 witness
严格分开。当前 23 种合法元件中只有 8 种拥有非空登记成绩，逐 kind 与全元件目录
核对后差异为 0：

```text
registered kinds / default kinds = 8 / 15
catalog mismatch                 = 0
native hidden wireable outputs   = 0
levels.txt SHA-256               = 178f80adf2acc08027c07044e6ea8428d7955b65b30e054e0ac56918245a7303
```

关键纠正是当前 `com_full_adder` 真正登记的仍是 `16/8`，不能因 EXE 默认表或研究
候选而按 `8/4`、`7/4` 或 `10/3` 作为 opaque 元件计费。现有 `7/4`、`10/3`、
`17/2` 只有显式内联结构可以立即进入 Byte Adder；要作为 native Full Adder 选择行，
必须先由用户在游戏中分别真实完成并登记。

同理，当前 opaque 三输入 AND/OR 为 `3/2`、XNOR 为 `5/4`，均劣于显式展开；XOR
已登记为正确的 `3/2`，但显式展开在同分下还能保留相位副产物。

```text
effective-frontier JSON SHA-256  25ee1eb97924c9c7a2a4140994e8db32f2831b06c8815c173b25a069d8a306a8
canonical artifact SHA-256       83d48ab849f406ab5bb7c594292867189c1a3eb979e147ee6c1ecd14057b6443
manual tests / py_compile        3/3 pass / pass
game launched / save modified    false / false
```

规范证据：

```text
.research/byte_adder_effective_frontier_root/audit_effective_frontier.py
.research/byte_adder_effective_frontier_root/test_audit_effective_frontier.py
.research/byte_adder_effective_frontier_root/effective-frontier-audit.json
.research/byte_adder_effective_frontier_root/2026-08-04-字节加法器真实元件成本前沿审计.md
```

## 2026-08-04 / 提交：完成 23-kind 顺序域与动态副产物闭包

Han 线把静态 23-kind 目录扩展到固定 `test.si` 的完整 131072 拍顺序域，覆盖：

```text
allowed kinds             23 / 23
current truth classes     82
autonomous phases         305
history rails             82
complete input rows       131072
```

它没有只检查全加器或前缀结构，而是逐类枚举 current、one-Delay、Delay+NOT、
Delay+一普通门、Delay 参与 Switch、AND3/OR3 late-input、FullAdder joint output、
word lane、BUS2、BUS normalizer 和 connected cut。

严格非递归结果为：history/phase 对九输出与 `C1..C8` 的直接命中均为 0；338 个
动态一元/二元表面命中全部被 current `<=1 gate` 方案支配或递归依赖目标；动态
FullAdder joint、AND3/OR3 late-input、非递归 phase BUS 和 score-improving connected
cut 均为 0。所有负结论只限报告声明的有限 grammar，不外推全局 sequential 下界。

真正保留下来的副产物是当前四条 Switch BUS 的 data plane：

```text
n45 == C1
n56 == C3
n62 == C5
n69 == C7
```

它们可经 `Maker4 -> Splitter4` 以 `0/0` 同时转换为四条 fully-driven active owner。
该转换依赖原 BUS，不能删除原 carry cone，也不能复制 partial-driver owner；但它是后续
跨层共享、owner 重置和物理重定时可合法使用的真实资产。

规范证据：

```text
.research/byte_adder_han_knowles_fused_agent/2026-08-04-字节加法器23-kind顺序域动态副产物审计.md
.research/byte_adder_han_knowles_fused_agent/audit_23kind_dynamic_byproducts.py
.research/byte_adder_han_knowles_fused_agent/byte_adder_23kind_dynamic_byproduct_audit.json
.research/byte_adder_han_knowles_fused_agent/byte_adder_23kind_dynamic_SHA256SUMS.txt
```

```text
main audit SHA-256   e4a8e59f537b3f18266b164c7ac50c6bff10ca6878a351381c923885cdeb96bb
report SHA-256       6f2c1374ff73add16c26461245f541126610ff0217ffce54637e6010d998792c
manifest SHA-256     3658acc78e6d26d181e98d2c70e72234ea808e187fdb99dce49ba39b06e4c2ea
manifest entries     18 / 18 pass
game/save modified   false / false
```

## 2026-08-04 / 提交：冻结全原语物理关系闭包与 203 条联合替换入口

本轮没有继续围绕 Full Adder、CLA、前缀树或其他命名架构展开，而是从 Byte Adder
关卡前可用的全部 23 个 kind 重新建立统一物理关系。每个元件都保留 raw truth、全部
active-constant cofactor、`value / driven / Z / conflict`、physical owner、Switch driver、
resolved BUS partition、per-input timing arc、word lane tuple 和不可拆分多输出 owner。

覆盖范围包括：

```text
OFF / ON
NOT / AND / AND3 / NAND / OR / OR3 / NOR / XOR / XNOR
bit Switch / Delay Line / Full Adder
Maker8 / Splitter8 / Maker2 / Splitter2 / Maker4 / Splitter4
word NOT / word NAND / word Switch
```

其中 Switch 仍严格按 `2 gate / 1 delay` 计。其 `enable AND data` 的 `1/1` 形式只是一
个 Boolean data-plane 投影，不保留关闭时 Z、partial driver 或 resolved BUS owner，
不能作为物理等价替换。XOR 严格按 `3/2` 计。Maker/Splitter 可以 `0/0` 把 Z 的
data plane 归一化成新的 active owner，但不能免费复制原 partial-driver owner。

对当前 `80/7` 基线的完整 131072 行重放得到：

```text
live nodes                    82
resolved BUS nodes             5
Switch partial drivers        10
pair relations              2952
primary-input cofactors      1394
physical truth classes        155
concrete producers            295
owner sets                    245
```

随后枚举所有满足 `connected + convex`、removed gate 为 `1..8`、boundary 不超过 10、
cut output 不超过 4 的 1411 个局部区域。构造库不只含普通门，还含两级共享公式、正确
成本 XOR、带已有或新一门 endpoint 的 Switch、二/三驱动 BUS、零成本 active clone、
owner-union sharing 和当前 miner 内显式登记的不可拆 `7/4` FullAdder tuple。

```text
single-cut strict gate_delta < 0       0
single-cut gate_delta = 0 near       203
no complete physical plan           1208
```

`strict=0` 只封闭上述 cut 和上述有限构造库，不是 Byte Adder 全局下界。本 miner 的
冻结输入没有把其他研究线的外部 tuple 证书自动并入，所以它不会凭历史文字把未导入的
tuple 当作可用 owner。尚未覆盖的空间包括无界 DAG、更深 Switch endpoint、任意 driver
BUS、任意状态机、更大 cut，以及两个或三个 near 的跨 cut 联合替换。

203 条 near 是本轮最重要的正资产。高门 near 中反复出现“普通副产物 + 两个 Switch
owner”的等成本实现；单个 cut 不省门，但多个 cut 可能共享同一普通上游、相同
enable/data endpoint、同一 resolved BUS，或因联合删除使旧共享上游失去最后一个外部
用户。下一轮将它们视为带 physical owner 的 hyperedge 做 pair/triple/owner-connected
联合搜索，不按单 cut 的 `gate_delta` 提前剪枝。

固定顺序域也已对当前 82 个 live node 完整检查 `Delay -> 可选一个普通门或一个
Switch`；直接输出命中为 0。该结果只排除这一个有限 grammar，不排除重新合成的多拍
状态编码、跨 lane 历史或 word/history 与 BUS 的联合副产物。

规范证据：

```text
.research/byte_adder_primitive_relation_miner/2026-08-04-字节加法器全原语关系闭包.md
.research/byte_adder_primitive_relation_miner/primitive_relation_miner.py
.research/byte_adder_primitive_relation_miner/primitive-relation-miner-v2.json
.research/byte_adder_primitive_relation_miner/audit_primitive_relation_miner.py
.research/byte_adder_primitive_relation_miner/primitive-relation-audit-v2.json
.research/byte_adder_primitive_relation_miner/sequence_history_probe.py
.research/byte_adder_primitive_relation_miner/sequence-history-probe-v1.json
```

```text
primitive relation file SHA-256     9e404c08069cde665aad50f08073cd16c57e7a98d81d07b5ce14414f758992d3
primitive relation internal SHA     e69af5204dcb777d637995192fed727557d3f98003f34af890729809714854e3
independent audit file SHA-256       b526048aa774fe3f1fe78a386a2c78eeac9d02d987effccee7d30663d501a483
independent audit internal SHA       ccfac95b1fc2516f3d82e67ed574afca1497c97276d784868c3195684eee3178
sequence history file SHA-256        4c1012338d634f5f2038447d0ee015920993414c62ea8dd048053ca7634a8beb
full truth rows                       131072
audit status                          pass
game launched / save modified         false / false
```

## 2026-08-04 / 提交：撤销旧关系闭包并建立 80/7、95/6 架构级副产物账本

上一段记录的 primitive v2、audit、203 条 near 与 SHA 因终端合同错误全部撤销。
旧模型强制九个 scalar output fully driven，但正确合同允许 `Cout=false` 为 active 0 或 Z；
所以旧工件不再是冻结证书，也不得接入 builder。修正后的完整 v2 没有重建。

本轮停止 relation miner、分片、near pair/triple、SAT/SMT、超图和 131072 行探索仿真，
改为只读解析两个已经完整验证过的 Factory DAG：权威 `80/7` 与完整 `95/6`。新账本为
每个 live node 记录结构函数、命名相位、arrival、fanout、owner、单扇出、consumer role
与 carry/sum domain，并保留每条 BUS 的完整 Switch enable/data/owner driver set。

```text
80/7: nodes=82, BUS=5, single-fanout=26, cross carry/sum=16
95/6: nodes=86, BUS=8, single-fanout=30, cross carry/sum=9
truth rows / solver / relation miner = 0 / 0 / 0
```

人工优先级不再压在单一命名架构上，而并列保留：

```text
80/7 bits5-6 carry/sum                  14 -> 13，目标 79/7
80/7 Q34 carry + S3/S4                 14 -> 13，目标 79/7
80/7 C2/C3 + S1/S2                     17 -> 16，目标 79/7
80/7 bit0 partial carry + bit1         14 -> 13，目标 79/7
80/7 C7/S7/Cout-Z                      11 -> 10，目标 79/7
95/6 Q34 carry + S3/S4                 14 -> 12，目标 93/6
95/6 AV56/C7 + resolved S6             23 -> 21，目标 93/6
95/6 Q12/C3 + S1/S2                    16 -> 14，目标 93/6
```

另保留两个全局物理机制：Maker/Splitter 可 `0/0` 把 may-Z carry data plane 变成新
active owner，但 word arrival 取所捆 lane 最大值；`T7` 必须作为 S7/C8 原子双输出
边界计账。Cout-Z 本身不是免费一门，用 Switch 替换现有 `1/1` OR 反而会增门，只有
与 C7 partial driver/T7 联合重写才可能获益。

规范证据：

```text
.research/byte_adder_primitive_relation_miner/2026-08-04-字节加法器架构级副产物账本.md
.research/byte_adder_primitive_relation_miner/build_architecture_byproduct_ledger.py
.research/byte_adder_primitive_relation_miner/architecture-byproduct-ledger-v1.json
```

```text
ledger file SHA-256       8261eb3e4150e9fc0dae1666b3cbc260a900046047e6b4c25b2c9e7e5650331b
ledger internal SHA-256   f47b6e0e78af9dd6454d7788b8898ae55b66551668cadc811eb541bc116f4f87
game/save modified        false / false
```

## 2026-08-04 / 提交：以共享数据 Switch 相位折叠取得 93/6/558

本轮按用户要求停止几十亿规模的电路空间遍历，不再扩大 SAT、MaxSAT、BDD、超图或
全真值生成空间。工作方法改为直接审计已闭合 `94/6` DAG 的真实扇出、到达时间、
Switch owner 和已经支付的相位副产物。首先通过跨区域 OR 重关联删除一门：

```text
旧 carry： A34=G3|G4；Apre=G2|A34
S3/S4：    X23=G2|G3

新 carry： X23=G2|G3；Apre=X23|G4
```

`X23` 原本已由 S3/S4 支付，重关联后私有 `A34` 从 live cone 死亡，得到真实
`94/6/564`。随后不再死磕 carry spine，而把目标缩到 `S5/S6 10 -> 9`。

### 新的九门 S5/S6 单元

保留 S5 的标准三门相位，并把其中已经支付的 `T5` 复用于 S6：

```text
T5 = AND(P5,C5)                         # 1 gate, arrival 5
R5 = NOR(P5,C5)                         # 1 gate, arrival 5
S5 = NOR(T5,R5)                         # 1 gate, arrival 6

D  = NAND(P6,C7)                        # 1 gate, arrival 5
E  = OR(G5,P6)                          # 1 gate, arrival 3

S6_raw = BUS(
    SW(enable=E,  data=D),
    SW(enable=T5, data=D)
)                                           # 4 gates, arrival 6
```

令：

```text
C6 = G5 | T5
C7 = G6 | P6*C6
```

则：

```text
S6_raw
= NAND(P6,C7) * (G5 | P6 | T5)
= NAND(P6,C7) * (P6 | C6)
= P6 XOR C6
= S6
```

两只 Switch 使用完全相同的数据 `D`，所以即使 enable 重叠也不会产生 0/1 冲突。
所有 `S6=1` 的输入行至少启用一只 driver；`S6=0` 的一部分输入行保持 Z。该 Z 不是
伪造成绩：当前 2.1.292 关卡的固定 `Output` 元件本身就是 kind16 Maker8，明确允许
八条 scalar lane 为 Z，按数据面把 Z 读取为 0，并以 `0 gate / 0 delay` 输出 active U8。

成本因此为：

```text
S5                                3
S6 ordinary phase                2
S6 complete two-Switch owner     4
----------------------------------
S5/S6 total                      9

94 - 1 = 93 gate
delay   = 6
energy  = 93 * 6 = 558
```

### 完整验证与物化

只在公式闭合后运行一次完整 `131072` 行验证：

```text
九输出 mismatch                       0
全图 BUS conflict                     0
raw S6 expected-one active          65536
raw S6 active-zero                  32768
raw S6 zero-Z                       32768
raw S6 conflict                         0
最终 Maker8 Sum mismatch                0
```

物化与 v15 回环审计：

```text
score                         93 / 6 / 558
component count                        85
wire count                            175
native com_add                           0
unconnected pins                        0
unsafe multi-driver networks            0
cycle components                        0
component overlap cells                 0
wire/component collisions               0
wire interior unrelated-pin contacts    0
Switch outputs with one owner        16/16
v15 round trip                       pass
```

候选 SHA-256：

```text
509ff8329a3816564098535d469e11de468d3f97aac36018ed108bd3bfc60d86
```

规范工件：

```text
.research/byte_adder_candidate_builder/build_g94_static_candidate.py
.research/byte_adder_candidate_builder/s56_phasefold_switch_patch.py
.research/byte_adder_candidate_builder/byte-adder-g93-d6-s56-tail9-full.json
.research/byte_adder_candidate_builder/materialized-g93-d6-s56-tail9/machine_certificate.json
.research/byte_adder_s56_architecture/2026-08-04-S5S6九门相位折叠审计.md
examples/byte_adder/candidate/circuit.data
```

部署结果：游戏未由脚本启动；未创建备份；仓库候选与正式存档均已直接覆盖，二者
SHA-256 完全一致。该结构等待用户在游戏本体中实机验收。

## 2026-08-04 / 提交：收缩 B12 的生成 enable，取得 92/6/552

在 `93/6/558` 的真实 DAG 上继续按架构副产物而非大空间枚举推进。低两位组的完整
resolved owner 原为：

```text
A12 = G1 | G2
B12 = BUS(
    SW(enable=C1,  data=N12),
    SW(enable=A12, data=N12)
)
```

精确 fanout 表明 `A12` 只有 `B12` 一个消费者；`B12` 也只有四个消费者：`C3`、
`C5`、`C7` 与 S4 低前缀。四处都已经支付了吸收 `G2` 的旁路：

```text
C3       的另一输入       G2
C5/C7    的另一 enable    Apre = G2 | G3 | G4
S4       的另一输入       X23  = G2 | G3
```

因此将 owner 收缩为：

```text
B12' = BUS(
    SW(enable=C1, data=N12),
    SW(enable=G1, data=N12)
)
```

记 `Bo=N12(C1|G1|G2)`、`Bn=N12(C1|G1)`，则 `Bo=Bn|N12*G2`。逐消费者有：

```text
G2   | Bo = G2   | Bn
Apre | Bo = Apre | Bn
X23  | Bo = X23  | Bn
```

两只 B12 Switch 使用同一个 `N12` 数据，重叠开启没有冲突。需要特别区分 value 与
driven：新 owner 自身的 Z 行从 `36864` 增到 `49152`，但 active-1 的差异全部位于
`G2=1`，在每个下游 owner 中都由同数据旁路覆盖；所以 `C5/C7` 的 value、driven、
Z、conflict 和 arrival 全部保持不变。`B12'` 仍为 `4 gate / arrival 3`，私有
`A12` OR 从 live cone 死亡，净删一门。

完整验证：

```text
score                              92 / 6 / 552
truth rows                               131072
九输出 mismatch                               0
global BUS conflict                            0
output arrivals              4,4,6,6,6,6,6,6,6
reachable Factory nodes                     84
```

物化审计：

```text
component count                              84
wire count                                  173
native com_add                                0
unconnected / unsafe / undriven               0
cycle / overlap / wire collision              0
tri-state pins with one owner             16/16
v15 round trip                             pass
```

规范工件：

```text
.research/byte_adder_candidate_builder/byte-adder-g92-d6-b12g1-s56tail9-full.json
.research/byte_adder_candidate_builder/materialized-g92-d6-b12g1-s56tail9/machine_certificate.json
.research/byte_adder_low_bits_recode/2026-08-04-A12中G2跨消费者吸收.md
examples/byte_adder/candidate/circuit.data
```

```text
Factory structural SHA-256
ac90b7f72586f5726464e2e44ccdf6f112e12ac7945075eec8eff13d78548f76

circuit.data SHA-256
6399bd212f04b822ee8937ad9be3d6fa115b231af62af4bbc38973a979ff88b9
```

仓库候选与正式存档已由主部署流程无备份直接覆盖，二者哈希一致；没有启动游戏。

## 2026-08-04 / 提交：重写 bits5:7 高位架构并复用 J7，取得 87/6/522

本轮不再围绕旧 carry spine 做局部门搜索，而是删除整个高位状态表示，从已经支付的
`C5`、`A56` 和逐位 `G/Q/P` 副产物直接生成 `S5/S6/S7/C8`。低位继续使用上一阶段
已经全域闭合的 `B12-G1` 重编码，因此私有 `A12=G1|G2` 仍不在 live cone 中。

### 成本变化

从 `93/6` 高位结构中整体删除：

```text
V5、V6                                  2 gate
V56                                     4 gate
V36                                     6 gate
C7                                      6 gate
旧 S5/S6                                9 gate
旧 S7/C8                                4 gate
-----------------------------------------------
合计                                   31 gate
```

新高位结构为 26 门；再叠加 `B12-G1` 删除 `A12` 的 1 门：

```text
93 - 31 + 26 - 1 = 87 gate
delay                 6
energy              522
```

### S5/S6

```text
O5 = OR(P5,C5)
D5 = NAND(P5,C5)
S5 = AND(O5,D5)

E6 = NOR(A56,Q6)
F6 = NOR(P6,Q5)
H6 = OR(G5,C5)

S6raw = BUS(
    SW(enable=E6, data=D5),
    SW(enable=F6, data=H6)
)
```

到达时间：

```text
S5 = 6
S6 = 6
```

`E6/F6` 的相位保证 BUS 没有异值重叠；全域审计得到：

```text
S6raw Z rows       32768
conflict rows          0
```

### S7

```text
N56 = NOR(Q5,Q6)
K56 = NOR(G6,N56)
R7  = NOR(C5,A56)
J7  = NOR(P7,K56)
H7  = OR(A56,C5)

S7raw = BUS(
    SW(enable=K56, data=P7),
    SW(enable=R7,  data=P7),
    SW(enable=J7,  data=H7)
)
```

owner 重叠证明：

```text
K56 与 J7 互斥
K56 与 R7 使用同数据 P7
R7 与 J7 重叠时 P7=H7=0
```

所以：

```text
S7raw arrival      6
S7raw Z rows   32768
conflict rows      0
```

最终 Output kind16 Maker8 按数据面读取这些 scalar lane，Z 贡献 0，U8 输出保持 active。

### C8 与 J7 跨输出复用

初版高位宏为 C8 单独生产：

```text
X7 = OR(G7,Q7)
F7 = NOR(X7,K56)
```

但 S7 已经支付：

```text
J7 = NOR(P7,K56)
```

利用恒等式：

```text
NOR(K56,J7) = P7 & ~K56
```

可直接写成：

```text
F7 = NOR(K56,J7)

C8raw = BUS(
    SW(enable=G7, data=G7),
    SW(enable=F7, data=H7)
)
```

这删除 C8 私有的 `X7`，比原高位宏再省 1 门。`J7@4 -> F7@5 -> C8raw@6`，因此
没有增加全局延迟。`G7/F7` 互斥，C8raw 无冲突。

C8raw 的 false 行允许 Z，但 Carry out 需要独立 active scalar，因此使用游戏中的
Maker2/Splitter2 数据面边界：

```text
Maker2(in0=C8raw, in1=Cin)
Splitter2(out0=C8)
```

这两个元件的记分合同均为 `0 gate / 0 delay`。机器证书独立确认：

```text
raw/clone physical network distinct      true
C8raw Z rows                             57344
C8 clone Z rows                              0
data-plane mismatch rows                     0
raw/clone arrival                          6/6
normalizer cost/delay                      0/0
```

Splitter2 的 `out1` 是填充 lane，对功能无用；它是整个电路唯一未连接 pin，并已被
物化审计明确分类为允许的多输出元件未使用端口，不是断线。

### 全域、物理与实机验收

```text
score                                  87 / 6 / 522
truth rows                                   131072
九输出 mismatch union                             0
全图 BUS conflict                                  0
output arrivals                  4,4,6,6,6,6,6,6,6
reachable Factory nodes                         86
```

```text
circuit.data size                              8592
component count                                  84
wire count                                      170
native com_add                                     0
unexpected unconnected pins                        0
intentional unused Splitter out1                   1
unsafe multi-driver / undriven / width mismatch    0
cycle / component overlap / wire collision         0
wire interior unrelated-pin contact                0
v15 round trip                                  pass
```

候选已经直接落入正式存档；脚本没有启动游戏，也没有创建备份。机器侧验证闭合后，
用户已在游戏本体中运行全部测试并确认实际计分为 `87/6/522`。

规范工件：

```text
.research/byte_adder_candidate_builder/byte-adder-g87-d6-b12-g1-hub87-full.json
.research/byte_adder_candidate_builder/materialized_g87_d6_b12_g1_hub87/candidate/circuit.data
.research/byte_adder_candidate_builder/materialized_g87_d6_b12_g1_hub87/machine_certificate.json
.research/byte_adder_candidate_builder/2026-08-04-字节加法器87门6延迟架构重编码.md
examples/byte_adder/candidate/circuit.data
examples/byte_adder/metrics.json
```

```text
Factory JSON SHA-256
23346b2bcc21eea85dd27ae2cb15912f7e808264d585edbf567e180cfb2254ed

circuit.data SHA-256
81847cb32151226de413f1479ea4efde7b5f8e593eed022bc82a868dfd93470c

machine_certificate.json SHA-256
8f051b7c152c3622d8e2c881ad6785804b075b1cc58ba831911eca99a7781d2a
```

本轮的决定性变化不是更大规模的逻辑遍历，而是三个架构动作：删除旧高位 carry
表示、跨输出复用 S7 的 `J7`、只在 C8 边界显式使用零成本数据面 normalizer。

## 2026-08-04：Carry out 直接采用原始 resolved BUS

### 提交：字节加法器：移除C8零成本归一化壳

这次提交不声称降低排行榜门数，而是把已经验证的 `87/6/522` 改造成一份物理拓扑
不同且更简洁的候选。旧结构在 `C8raw` 之后放置：

```text
Maker2(in0=C8raw, in1=Cin)
Splitter2(out0=Carry out)
```

两个元件的记分成本和延迟均为 `0/0`，作用只是把 `C8raw` 的 Z 零行转换成 active
zero。Byte Adder 的真实 Carry out 合同允许：

```text
期望 1：必须主动驱动 1
期望 0：允许主动驱动 0 或 Z
任何行：禁止多驱动冲突
```

因此新版直接连接：

```text
C8raw = BUS(
    SW(enable=G7, data=G7),
    SW(enable=F7, data=H7)
)

C8raw -> Carry out
```

完整验证结果：

```text
gate / delay / energy                   87 / 6 / 522
truth rows                                     131072
九输出 mismatch union                               0
BUS conflict assignments                            0
Carry out Z rows                                57344
Carry out 期望 1 但未主动驱动的行                     0
sum / carry correct                         true / true
```

物理变化：

```text
component count                              84 -> 82
wire count                                  170 -> 167
unexpected unconnected pins                          0
unsafe multi-driver / undriven / width mismatch      0
cycle / component overlap / wire collision           0
wire interior unrelated-pin contact                  0
native com_add                                        0
v15 round trip                                      pass
```

该候选于 `2026-08-04 21:22:09` 直接覆盖仓库候选和正式存档。构建、物化和部署过程
没有启动游戏，也没有创建备份。

规范工件：

```text
.research/byte_adder_candidate_builder/build_g87_raw_c8_variant.py
.research/byte_adder_candidate_builder/byte-adder-g87-d6-raw-c8-full.json
.research/byte_adder_candidate_builder/materialized_g87_d6_raw_c8/candidate/circuit.data
.research/byte_adder_candidate_builder/materialized_g87_d6_raw_c8/machine_certificate.json
examples/byte_adder/candidate/circuit.data
examples/byte_adder/metrics.json
```

```text
Factory JSON SHA-256
ff33ea8cd2df4d2c93865fec812ac3708456414d7ed94b02a5098c5803502746

circuit.data SHA-256
3ca8806c27ffd2532f3c80041edd1466d169d41e1c81f8b3f79f6fb9da057aac

正式存档后续 v15 规范化写回 SHA-256
def38696651a3a01c9e3f0621344edc121c4671a801d181bdb58ddc44f5cc400
```

规范化写回后的正式文件为 `3818` 字节；仓库中的可复现构建输出为 `8411` 字节。
二者均解析为 `87/6`、`82` 个元件和 `167` 条导线，导线对象完全相同。差异只在顶层
`custom_id/design` 以及三个 Maker/Splitter 元件的冗余 `word_size` 字段，不改变拓扑、
引脚、计分或功能。

这个版本清除了高位尾部最后两个只负责 active-zero 归一化的物理元件。后续若继续重写
`S7/C8` 联合 owner，可以直接以 raw Carry out 合同为边界，不再把归一化壳误计为架构
依赖。

## 2026-08-04 / 提交：直接审计物理网表，排除恒零 Switch

人工审核发现一只 Bit Switch 看起来可能满足 `enable=0`。本轮没有根据 Factory DAG
的逻辑名字作判断，而是直接解码正式 `byte_adder/Default/circuit.data`，按最新版
kind `12` 的下方 enable 端口建立物理网表，并枚举全部 `131072` 个输入。

权威快照与基线结果：

```text
SHA-256       3CA8806C27FFD2532F3C80041EDD1466D169D41E1C81F8B3F79F6FB9DA057AAC
components    82
wires         167
Switches      13
truth rows    131072
九输出错误         0
BUS 冲突           0
```

逐只 enable 的非零计数为：

```text
65536, 32768,
32768, 65536,
75776, 49152,
49152, 49152,
49152, 36864, 40960,
32768, 40960
```

因此：

```text
恒零 enable                          0
逐只断开仍完全正确的 Switch          0
最少 enable=1 行数                32768
逐只断开后最少错误输入行数           8192
```

特别容易产生视觉误判的是 Cout 路径上的：

```text
SW(enable=G7, data=G7)
```

它的 enable 和 data 接在同一网络，但并非多余：`G7=0` 时输出 Z，`G7=1` 时主动输出
1；单独断开会造成 `32768` 行 Cout 错误。

同时纠正了先前临时报告中的证据问题：Factory 物化阶段的 `permanent_id` 不能当作
当前正式序列化文件的物理 ID。本轮报告的 index、PID、坐标和 enable 网络全部直接
来自指定 SHA 的正式文件。

可复跑工件：

```text
.research/byte_adder_h6_state_recode/audit_physical_switch_enables.py
.research/byte_adder_h6_state_recode/switch-enable-full-domain-audit.json
.research/byte_adder_h6_state_recode/2026-08-04-87门电路Switch恒零线索独立审计.md
```

本轮没有启动游戏，没有修改字节加法器逻辑，也没有删除任何 Switch。

## 2026-08-05：S7 双 Owner 相位合并降至 86 门

### 提交：字节加法器：合并S7互斥开关相位

本轮停止全域电路空间遍历，直接比较两份已经闭合的独立 `87/6/522` 架构，审计
高位 `S7/C8` 的真实三态 owner。命中的关键不是删除逻辑条件，而是识别两个使能
互斥、且在新增使能域中数据恒等的 Switch。

人工架构原有付费轨道：

```text
n278 = NOR(Q6,P7)                   @3
n258 = AND(Q6,P7)                   @3  # 已由 C8 支付
n207 = OR(C6,nP6)                   @5
n285 = NOR(C6,OR(G6,G7))            @5
```

旧 `S7` owner：

```text
S7_old = BUS(
    SW(n278,n207),
    SW(Q6,P7),
    SW(n285,P7)
)
```

新结构把 `Q6&P7` 的已付费轨道并入第一路使能：

```text
U = OR(n278,n258)                   @4

S7_new = BUS(
    SW(U,n207),
    SW(n285,P7)
)
```

成立原因是：

```text
n278 = ~Q6 & ~P7
n258 =  Q6 &  P7
```

二者互斥；在新增的 `n258` 域中 `nP6=1`，所以：

```text
n207 = C6 | nP6 = 1 = P7
```

因此第一只新 Switch 精确覆盖旧 `SW(n278,n207)` 与 `SW(Q6,P7)` 的全部 active-one
行。部分正确值为 0 的行由主动 0 变成 Z，现有 Maker8 按当前运行时合同把 Z lane
读取为 0，不影响最终 U8 输出。成本变化为：

```text
删除一只 Bit Switch    -2 gate
新增一只 OR             +1 gate
净变化                   -1 gate
```

完整 DAG 与物化结果：

```text
gate / delay / energy                         86 / 6 / 516
truth rows                                          131072
九输出 mismatch union                                    0
BUS conflict assignments                                 0
S7 old/new active-one difference                          0
S7 expected-one undriven                                  0
S7 old driven / Z                             98304 / 32768
S7 new driven / Z                             86016 / 45056
components / wires                                  79 / 161
native com_add                                             0
unconnected / undriven / unsafe multi-driver               0
cycles / width mismatches                                  0
component overlap / wire collision                         0
wire interior unrelated-pin contact                        0
v15 round trip                                          pass
```

候选于 `2026-08-05` 直接覆盖仓库候选和正式存档。部署过程没有创建备份，也没有
启动或结束游戏。部署后两个文件逐字节一致：

```text
examples/byte_adder/candidate/circuit.data
C:/Users/cong/AppData/Roaming/Turing Complete/schematics/byte_adder/Default/circuit.data

SHA-256
5ef71e14bdf803390c3a98bf1317e00f6d0c13f87b8fc87d6e0b950fd7eda36b
```

规范工件：

```text
.research/byte_adder_architecture_restart/build_human86_s7_merge_factory.py
.research/byte_adder_architecture_restart/verify_human87_s7_merge.py
.research/byte_adder_architecture_restart/byte-adder-human86-s7-owner-merge-full.json
.research/byte_adder_architecture_restart/materialized_human86_s7_owner_merge/candidate/circuit.data
.research/byte_adder_architecture_restart/materialized_human86_s7_owner_merge/machine_certificate.json
.research/byte_adder_d5_carry_spine/byte_adder_high_joint_onegate/2026-08-05-S7双Owner合并得到86门6延迟.md
.research/byte_adder_d5_carry_spine/byte_adder_high_joint_onegate/verify_human86_s7_owner.py
.research/byte_adder_d5_carry_spine/byte_adder_high_joint_onegate/human86-s7-owner-certificate.json
examples/byte_adder/candidate/circuit.data
examples/byte_adder/metrics.json
```

核心哈希：

```text
Factory JSON
f56c0f93055ac76beab9b3f15ed70da792d8eaeda26e196598cf7556de025b4d

machine_certificate.json
9751530cfddd5f26c9f27ed31090753acf0beed567b14188a90524d1bcfda775

独立 131072 行验证脚本
45cb96ecd1be358ca4da2f96db433ffd57e4a4d2b1bcb745f72e520dec2cbed3

独立证明证书
5adce7742bf619ceeaba10e4cfe2a4264e83916c29b38351b4f7abdc2d08b7a2
```

该版本已完成机器侧全域证明和正式落盘；游戏本体实机验收状态将在用户反馈后追加。

## 2026-08-05：bits4:5 联合状态重编码降至 85 门

### 提交：字节加法器：共享 T4 重构 S4、S5 与 C6

用户已在游戏内实机验收上一版 `86/6`。本轮没有继续做全域 Boolean DAG 搜索，而是把
bits4:5、`C6`、`S4` 和 `S5` 作为一个完整架构边界，从旧 `N/V/not-P` 状态改成
`G/Q/P` 状态。

关键共享相位为：

```text
T4 = P4 AND C4
C5 = G4 OR T4
C6 = G5 OR (P5 AND C5)

D5 = NAND(P5,C6)
E5 = G4 OR P5

S4 = P4 XOR C4
S5 = BUS(
    SW(enable=E5,data=D5),
    SW(enable=T4,data=D5)
)
```

`T4` 同时服务 S4、bit4 carry phase 和 S5，令联合区域从 `24 gate` 降到
`23 gate`：

```text
gate / delay / energy                   85 / 6 / 510
output arrival              4,5,4,6,6,6,6,6,6
truth rows                              131072
nine-output mismatch union                   0
BUS conflict assignments                     0
components / wires                       78 / 159
native com_add                                  0
v15 round trip                               pass
```

物化候选没有未连接引脚、危险多驱动、无驱动网络、位宽冲突、组合环、元件重叠、
导线穿件或导线内部接触无关引脚。用户随后在游戏本体中确认 `85/6` 全部测例通过。

规范工件：

```text
.research/byte_adder_architecture_restart/build_human85_s45_c6_recode.py
.research/byte_adder_architecture_restart/byte-adder-human85-s45-c6-recode-full.json
.research/byte_adder_architecture_restart/materialized_human85_s45_c6_recode/candidate/circuit.data
.research/byte_adder_architecture_restart/materialized_human85_s45_c6_recode/machine_certificate.json
.research/byte_adder_literature_mapping/2026-08-05-停止遍历后的85门架构突破与84门边界.md
```

核心哈希：

```text
Factory DAG JSON
6d1c87fd476141e7a54b0f81862c8e892161eb15db59965bf2131bbb583ab3ac

circuit.data
0d9f4d0bbfe02d725e3ae91b6d37a6ff5e31811e90ae8fffec85b5466dda59b8

machine_certificate.json
f2d01bd68ff2b3a8b23b7681a2e74f53adf57874329c04ed6885be962d012c6d
```

下一阶段停止 SAT、MaxSAT、BDD、随机搜索和旧模板枚举，只研究完整架构的 paid-state、
arrival、Switch Z、BUS owner，以及 Sum/Carry 跨输出副产物复用。新的物化门槛固定为
`<=84/6`。

## 2026-08-05：停止超大电路空间遍历，按架构合同继续优化

### 提交：字节加法器：冻结85门榜一并改用架构级净账

公开排行榜只读复核确认：

```text
cong0707             85 / 6 / 510   rank 1
原人工第一档         103 / 5 / 515   rank 2
```

此前长时间计算没有及时产生前沿的根因，是把完整游戏问题错误压缩成任意 Boolean DAG
最小化。该模型没有原生表达完整 BUS owner、Z/active-zero 冲突、真实 Switch arrival，
也不能主动改变 carry 状态编码；因此数十亿候选主要在同一模板族内重复排列。

从本次提交起停止：

```text
SAT / MaxSAT 任意门网超优化
BDD 全函数闭包
随机变异和大邻域搜索
旧模板排列组合重跑
未闭合净账的全真值验证
```

只接受先闭合以下账本的架构改写：

```text
删除节点和删除成本
新增普通门与 Switch
逐节点 arrival
完整 BUS driver set
driver 重叠时的同值或互斥证明
普通节点的跨输出 fanout
完整 gate/delay/energy
```

新的有效窗口固定为 `84/6=504` 或 `101/5=505`。只有静态结构先进入其中一个窗口，
才运行一次 `131072` 行语义与 Z/conflict 验证，随后才允许物化和写入存档。

## 2026-08-05：S3 正相位同分重构并直接部署

### 提交：字节加法器：重关联 S3 正相位与 C6 生成树

为在停止大空间遍历后尽快交付一个可以实机检查的全新电路，本轮以已经实机通过的
`85/6/510` 为基线，只改写两个固定局部，不改变总门数：

```text
旧 S3：
nC3 = NOR(T2,G2)
S3  = P3 XOR C3 的 NAND/OR/NAND 相位

新 S3：
D3 = NAND(P3,C4)
E3 = OR(G2,P3)
O3 = OR(E3,T2)
S3 = AND(D3,O3)
```

因为 `P3=1` 时 `C4=C3`，所以：

```text
D3 = NOT(P3 AND C3)
O3 = P3 OR G2 OR T2 = P3 OR C3
S3 = (P3 OR C3) AND NOT(P3 AND C3)
```

同时把 C6 的 generate OR 树从：

```text
(G4 OR G5) OR G3
```

重关联为：

```text
(G3 OR G4) OR G5
```

新电路的结构哈希与上一版不同，但完整指标保持：

```text
gate / delay / energy                   85 / 6 / 510
output arrival              4,5,4,6,6,6,6,6,6
truth rows                              131072
nine-output mismatch union                   0
BUS conflict assignments                     0
components / wires                       78 / 159
unconnected pins                              0
unsafe multi-driver networks                  0
component overlaps                            0
wire/component collisions                     0
wire interior pin contacts                    0
tri-state pins in one owner                13/13
native com_add                                  0
v15 round trip                               pass
```

本轮还验证并否决了一个表面为 `84/6` 的改写：

```text
nP3 = G3 OR Q3
S3  = XNOR(nP3,C3)
```

普通二值代数上该式正确，但当前 `C3` 是为了提前一层而构造的 partial reason BUS，
共有 `49152` 行为 Z，并非完整 carry。把它当成完整 `C3` 后，九输出验证在 S3 上出现
`16384` 行失配。因此该方案没有部署，也不能计为 84 门前沿。

部署于游戏退出状态下执行，直接覆盖仓库候选和正式存档，没有创建备份、没有启动或
结束游戏。部署后三份电路逐字节一致：

```text
examples/byte_adder/candidate/circuit.data
.research/byte_adder_architecture_restart/materialized_human85_s3_positive_phase/candidate/circuit.data
C:/Users/cong/AppData/Roaming/Turing Complete/schematics/byte_adder/Default/circuit.data

SHA-256
0ff157bf611a5b2aade66b09e1ef9e7c79540b5ce422f2bba41a182a14df776e
```

规范工件：

```text
.research/byte_adder_architecture_restart/build_human85_s3_positive_phase.py
.research/byte_adder_architecture_restart/byte-adder-human85-s3-positive-phase-full.json
.research/byte_adder_architecture_restart/materialized_human85_s3_positive_phase/candidate/circuit.data
.research/byte_adder_architecture_restart/materialized_human85_s3_positive_phase/machine_certificate.json
```

核心哈希：

```text
Factory JSON
b3dbe1d83ed28f32c929f4c840a6fa69a9d747e84895c0df9af794d7f704feee

Factory DAG structure
5b3aa51e17c763d85617f28b7db20ac89635157251241498626f2fb148e928ca

output vector
5786e3f47f5ca74fe1b0d6439246f17b3937357f9381c4a90eeaf3d85a6f6410
```

该版本是可实机验收的同分新架构，不冒充 `84/6` 新前沿；下一步仍只接受能够完整闭合
`84/6` 或 `101/5` 的架构级改写。

## 2026-08-05：以 TuringSynth 总线优先流程重排 85/6 电路

### 提交：工具链：建立线路主导的字节加法器自动布局流程

本轮不改变已经实机通过的逻辑结构，而是让 TuringSynth 完整接管 v15 导入、静态时序、
Floorplan IR、放置、结构化布线、物理审计和重新序列化。权威输入为：

```text
.research/byte_adder_architecture_restart/
materialized_human85_s45_c6_recode/candidate/circuit.data

SHA-256
0d9f4d0bbfe02d725e3ae91b6d37a6ff5e31811e90ae8fffec85b5466dda59b8
```

新流程的固定结构为：

```text
InputPort
-> Splitter bank
-> flat BusTrunk
-> timing-driven GrowthCone
-> OutputMerge / Maker / OutputPort
```

顶层端口、Splitter 和 Maker 的旧坐标及旧朝向不再作为布局约束；v15 输出会写回布局后的
`position + rotation`，但永久 ID、组件顺序、immutable 标志和其余组件元数据必须逐项
保持不变。人工参考中的 driver-only 装饰 rail 被单独保存，不会误入逻辑 DAG。

路由正确性合同同步收紧：

```text
普通直线内部允许正交交叉
tap、hub、wire endpoint 禁止外国导线穿越
禁止导线穿元件、穿引脚或重叠同一物理边
禁止非正交外国接触
每个逻辑网必须对应一个且仅一个物理分区
```

首轮完整生成已经保持：

```text
gate / delay / energy                   85 / 6 / 510
component / logical net                    78 / 83
component overlaps                              0
wire/component collisions                       0
wire interior pin contacts                      0
foreign endpoint/tap contacts                   0
non-orthogonal foreign contacts                 0
mixed / fragmented networks                  0 / 0
v15 round trip                               pass
logical pin partition                        pass
```

修复 horizontal collector 的封口问题后，又将普通 fanout 和多驱动 owner 的后备策略改为
优先使用局部纵向 collector。两次完整构建的几何变化为：

```text
                              首轮        纵向优先
wire count                     483             439
total wire length             8821            7954
maximum wire length            182             176
bend count                     313             288
backtrack unit segments        706             477
foreign ordinary crossings     961             727
```

纵向优先版已在游戏未运行时直接覆盖正式 Byte Adder 存档，没有建立备份，也没有启动游戏：

```text
C:/Users/cong/AppData/Roaming/Turing Complete/
schematics/byte_adder/Default/circuit.data

SHA-256
d9bac888bca8a81c939ac938482d8409a9dd49db7cad6a7055c433d3009a4b37
```

当前 43 个 TuringSynth 单元测试全部通过。几何仍未达到人工参考的 `15 bends / 0
backtrack`；剩余折返已量化定位到 GrowthCone 内少数 fanout 和 resolved BUS owner，后续
继续改造 conductor-first 局部 collector，不以调整 A* 权重替代架构修复。

### 提交：工具链：压缩逻辑出口并按轨道稀缺度规划

进一步归因发现，旧路由器为每个普通门和 Switch 输出强制保留两格 breakout，使相邻
逻辑层之间往往只剩一条 collector 轨。该规则只对 Splitter、输入端口和 Maker 的平整
适配段有意义；普通逻辑输出只需一格即可离开元件边界。

本轮因此保持 I/O adapter 的两格基线，但将普通门输出改为一格。随后不再按 fanout
数量粗略分配局部通道，而是预先计算每条 GrowthCone 网络实际可用的静态纵向轨道数，
让候选更少的网络先占轨道；没有纵向候选的网络继续交给原 axis-adaptive growth 顺序，
避免把“不可用”错误当成“最稀缺”。

两步改动均经过完整 `85/6/510` 重生成，而不是仅运行局部几何测试：

```text
                              纵向优先      一格出口      稀缺度调度
wire count                         439           449            447
total wire length                 7954          5760           5550
maximum wire length                176            99             90
bend count                         288           158            148
backtrack unit segments            477           106             78
foreign ordinary crossings         727           306            262
```

最终版本保持全部物理错误计数为零，并再次通过 v15 round trip、逻辑 pin partition 和
43/43 单元测试。游戏未运行时已直接覆盖正式 Byte Adder 存档：

```text
C:/Users/cong/AppData/Roaming/Turing Complete/
schematics/byte_adder/Default/circuit.data

SHA-256
fb9cab6d700e54dc8e77a942c67d7e906c1550f9bdf78c64adb58fe87b13cee6
```

本轮还实测否决了“无条件允许源输出列作为 fanout trunk”的捷径。该做法虽然能救一个
局部拥塞网络，却会使其他网络的 source socket 被本网络 spine 和 feeder 同时封住，
所有 margin 与路由顺序均无法恢复。因此失败试验未写入存档；稳定版本仍要求每个 tap
保留明确可达的 terminal socket。

### 提交：工具链：建立导体通道驱动的紧凑布局

本轮将“先铺导线主干，再让逻辑沿主干生长”从布局原则落实为生成器的数据合同，而不是
继续依赖 A* 在固定门阵列外侧补救。`Floorplan` 升级为 v2，并为全部 83 条逻辑网络生成
`PlannedConductor`；两个真实输出反向形成两个 `GrowthCone`，每条导体保存一个或多个
driver tip、全部 sink socket、关键程度和所属生长区域。

放置阶段会为高扇出网络预留局部 spine/branch 通道，并输出
`planned_conductor_spines`。后续元件不能再占用已经承诺的 conductor corridor；路由阶段
优先验证放置器给出的通道提示，提示失效时仍必须按完整几何合同回退，不能绕过碰撞检查。

路由器同步加入三项通用机制：

```text
局部纵向 comb：允许 hub 与 terminal 重合，也允许零长度 tap
Hanan dogleg：二端网络优先采用最多一次正交转向的最短轴向路径
planned_path：规划期已经验证的 feeder/tap/spine 原子提交，不再交给 A* 重走
```

为了避免“规划时合法、提交时出口被其他网络抢占”，每个 terminal 的 escape、每段 spine
和 branch 都会立即进入 reservation。外国网络仍不得穿越 tap、hub、endpoint、元件或
引脚；普通导线内部的正交交叉继续合法；任何同边重叠仍是硬错误。

生成报告现在逐网络记录：

```text
wire count / total length / maximum wire length
internal bends / backtrack segments / role counts
planning mode / fallback reason / conductor hint
```

这使热点不再依赖截图猜测。导体通道改造的连续完整构建结果为：

```text
                         wires  length  max  bends  backtrack  fallback  crossings
旧自动布局                  447    5550   90    148         78        26        262
终端行错位                  448    5624   90    120         56        23        213
通道预留                    434    5560   91     77         28         7        219
放置到路由的 spine hint     434    5558   91     77         28         7        219
```

最后一版没有改变逻辑、组件身份、immutable 标志或任何非布局元数据，完整结果为：

```text
gate / delay / energy                         85 / 6 / 510
component / logical network                      78 / 83
wire count / total length                       434 / 5558
maximum wire / bends / backtrack                91 / 77 / 28
monotonic fallbacks / ordinary crossings         7 / 219
component overlap                                    0
wire/component collision                             0
wire interior pin contact                            0
foreign endpoint/tap contact                         0
non-orthogonal foreign contact                       0
overlapping edge                                     0
mixed / fragmented logical network               0 / 0
v15 round trip                                    pass
logical pin partition                             pass
TuringSynth tests                                47/47
```

游戏进程门禁通过后，已直接覆盖正式 Byte Adder 存档，没有创建备份、没有启动游戏：

```text
C:/Users/cong/AppData/Roaming/Turing Complete/
schematics/byte_adder/Default/circuit.data

SHA-256
56c4a0819e451edf42210882e966c97c7e64a45cbadcfe5a1f8d05f9f7a04b0d
```

逻辑研究前沿仍是已经实机验收的 `85/6/510`，本提交只改善可读布局与生成器可靠性，
不把几何优化冒充新的门数成绩。剩余最明显的自动布局热点是 `net20`、`net28` 和少数
direct fallback；后续继续以通用 conductor/floorplan 规则消除它们，不写入任何
Byte Adder 网络名特例。

### 提交：工具链：原子规划紧邻级导体树

继续审计逐网络报告后，定位到一个放置与布线之间的事务错误：同一个组件同时接收多条
fanout 网络时，旧放置器会先独立计算所有 corridor，再统一提交。后计算的计划看不到
同批次尚未提交的纵轨、横支和 hub，因此可能让两条网络获得同一列上的重叠承诺；路由器
只能把其中一条赶到外围。

放置器现在会在单个组件候选内部维护 staged vertical、staged horizontal 和 staged hub，
每批准一条 incoming conductor 就立即对下一条可见；只有该组件最终位置被接受后，整批
计划才原子写入全局 corridor。新增的通用回归使用两个 fanout 共同驱动同一消费者，验证
重叠纵向区间不会再获得相同 spine。

路由阶段也不再让所有结构导体固定服从同一个批准顺序。四种既有 bounded strategy 现在
会真实改变 conductor planning order；`longest-first` 优先兑现纵向跨度大的放置承诺，
短局部网络负责在旁边形成小树，最终仍由完整几何指标选择结果。该变化将原先被赶到画面
底部的长 U 形网络从：

```text
157 length / 12 bends / 13 backtracks
```

压缩为：

```text
21 length / 0 bends / 0 backtracks
```

随后发现承担绕让的相邻短网只有三格级间距。旧 growth comb 强制 branch 同时距离 source
和 sink 至少两格，导致本来可以一格接入的近端消费者被误判为不可局部生长。本轮允许：

```text
一格 terminal tap
source-column branch
source stem、水平 spine 与纵向 branch 只在共同 endpoint 连接
```

source-column branch 会把纵向段拆在 source access endpoint，绝不依赖游戏不支持的
endpoint-to-interior 连接；同网共享边仍然拒绝，外国纵轨、pin、tap 和 endpoint 仍是硬
阻塞。近端短网因此从：

```text
134 length / 6 bends / 8 backtracks
```

恢复为：

```text
26 length / 0 bends / 0 backtracks
```

最终相对上一次正式交付的变化为：

```text
                         上次交付      本次交付
wire count                    434           446
total wire length            5558          5401
maximum wire length             91            90
bend count                      77            65
backtrack segments              28            19
monotonic fallbacks              7             6
ordinary crossings             219           216
```

wire 数量增加来自把共享树明确切分为 spine、stem、branch 和 tap，并不增加游戏门数、延迟
或能耗。完整构建仍保持 `85/6/510`，全部物理错误为零，v15 round trip 与逻辑网络分区
通过，TuringSynth 测试增加到 `50/50`。

在游戏进程门禁通过后，本版已直接覆盖正式 Byte Adder 存档，没有创建备份、没有启动
游戏：

```text
C:/Users/cong/AppData/Roaming/Turing Complete/
schematics/byte_adder/Default/circuit.data

SHA-256
4649f2a26d59039f7945035188ceadc97f292c8c278bc5a2cfa256c6f16146fb
```

本轮还否决了把 `hub == terminal` 无条件推广到所有 horizontal growth 的试验：它虽减少
一个 bend，却把总长度推高到 `5645`，因此从未写入正式存档。后续优化继续以完整构建的
多指标支配关系为准，不以单一局部线段变短代替全局可读性。
