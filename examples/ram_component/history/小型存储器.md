# 小型存储器

## 2026-08-01 / RAM 原生候选

- 读取当前安装版本的 `campaign/ram_component/test.si`，确认每拍先校验 Load 输出，随后才按 Save 改写状态；Load 为 0 时必须输出 Z。
- 对照当前 v15 存档中的 `com_ram` 实例，确定 kind `118` 的实际五端口位置：`load`、`save`、`address`、`in` 位于中心相对 `(-15,-15)` 至 `(-15,-12)`，`out` 位于 `(+16,-15)`。
- 使用一个 `buffer_size=4`、`word_size=8` 的原生 RAM；地址输入为 RAM 的 U8 端口，关卡 U2 地址用 `maker_bit_2` 合成并由游戏作合法零扩展。候选头目标为 `368 gate / 5 delay / 1840 energy`。
- 生成器逐一验证不可变关卡接口、v15 编解码往返、真实网络连通性、`1,048,576` 个本地 U8 状态转移，以及安装版精灵 alpha 几何。导线允许彼此交叉，但不穿过元件或非端点引脚；仅地址源的两个必经一格引线与官方 `hint_solution` 一致，已被逐坐标白名单审计。
- 该候选没有写入正式存档；门数和延迟仍应由游戏载入时重新计分，并以游戏实测结果为准。
