# 字节常量

## 2026-08-01 / 提交：功能：新增六个原生字节元件候选

## 灵感

关卡只要求恒定输出 164。使用一个 8 位 Constant 可直接驱动输出，不需要八个单比特常量
和 Maker，也不需要任何输入。

## 修改点

- 新建 `init_data=164`、`word_size=8` 的 Constant。
- 只保留一条从常量到固定 Output 的导线。
- 声明目标为 `0 gate / 0 delay / 0 energy`。

## 离线验证

组合模拟器现在允许零输入真值表，并严格执行一次测试向量；输出为 164。v15 往返与全部
引脚连通检查通过，真实通过状态仍须游戏验收。
