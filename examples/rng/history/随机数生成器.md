# 随机数生成器

> **重要：这是版本相关的漏洞利用方案，不是长期有效的常规解。** 当前
> `245 / 7 / 66` 已由游戏完整测试并被排行榜服务端接受，但它依赖
> `kind 118` 的隐藏 `settings[0]=2`，利用 RAM 计分路径与运行时流水分支之间的
> 零延迟差异。官方后续可能修复该引擎漏洞、回滚成绩或使本电路失效；任何复现和比较都应
> 明确标注游戏版本，并重新进行客户端全测与服务端验证。

## 2026-08-01 / 提交：研究：建立三十二位异或移位专用网络

### 结论

本关应当使用只服务于测试脚本的 32 位线性反馈 ASIC，不应使用、修复或优化
`OVERTURE`、`LEG`、`Overture`、`RV64` 或任何通用处理器。当前 `RV64` 基线为
`235437 gate / 2773 delay / 1440 cycle`，与公开榜首
`431 / 9 / 66 / 256014` 相差数个数量级，继续优化取指、译码、寄存器堆或 ISA 没有意义。

推荐先复刻公开榜首对应的 66 周期结构：一个初始化控制位、32 位状态、固定布线移位和
三层 XOR 反馈。之后再单独研究 65 周期首拍旁路，不要让旁路增加榜首方案的关键路径。

### 判定语义

证据来自最新版 2.1.281 的只读文件：

- `D:\Game\Steam\steamapps\common\Turing Complete\campaign\rng\meta.txt`
- `D:\Game\Steam\steamapps\common\Turing Complete\campaign\rng\test.si`

测试随机选择一个 `1..0xfffffffe` 的非零 32 位初始种子，然后连续检查：

```text
t1 = (seed xor (seed lsr 13)) & 0xffffffff
t2 = (t1   xor (t1   lsl 17)) & 0xffffffff
y  = (t2   xor (t2   lsr 5 )) & 0xffffffff
seed = y
```

`count` 初始为 0，在检查 `count == 64` 时获胜，因此必须按顺序提交 65 个正确结果。测试
没有要求执行程序、保留通用架构状态或支持别的输入协议。公开前缀中的最少周期为 65，
所以周期严格下界为 65；66 周期方案只浪费一个初始化拍，但有更低的门数与延迟乘积。

可用于离线复核的前几项为：

| 初始种子 | 第 1 项 | 第 2 项 | 第 3 项 |
| --- | --- | --- | --- |
| `00000001` | `00021001` | `21211091` | `12828955` |
| `00000002` | `00042002` | `42403122` | `0424023a` |
| `12345678` | `996cc1e4` | `880f6ad6` | `dbbba3f8` |
| `ffffffff` | `f807c000` | `ffbfe03f` | `c219dd2e` |

三次变换都是 GF(2) 上的可逆线性映射，组合矩阵秩为 32。因此非零种子不会进入零状态，
但本方案不依赖把零值当作初始化标志，避免把数据与控制耦合。

### 直接线性网络

固定移位不能使用通用桶形移位器。将 32 位字拆成单比特线后，只为实际发生变化的位放
XOR，其余位直接连线：

| 阶段 | 需要 XOR 的输出位数 | TC 单比特 XOR 成本 | 延迟 |
| --- | ---: | ---: | ---: |
| `x xor (x >> 13)` | `32 - 13 = 19` | `19 * 3 = 57` | 2 |
| `t1 xor (t1 << 17)` | `32 - 17 = 15` | `15 * 3 = 45` | 2 |
| `t2 xor (t2 >> 5)` | `32 - 5 = 27` | `27 * 3 = 81` | 2 |
| 合计 | 61 | 183 | 6 |

按最低位为 `x[0]` 的约定，可直接用下面三组生成式布线：

```text
t1[i] = x[i] xor x[i+13]  , 0 <= i < 19
t1[i] = x[i]              , 19 <= i < 32

t2[i] = t1[i]             , 0 <= i < 17
t2[i] = t1[i] xor t1[i-17], 17 <= i < 32

y[i]  = t2[i] xor t2[i+5] , 0 <= i < 27
y[i]  = t2[i]             , 27 <= i < 32
```

这 61 个 XOR 是规范三阶段分解的精确成本，不是全局最小性声明。无论怎样共享中间量，
32 个输出都不是原始输入或常量，二输入线性直线程序至少需要 32 个新 XOR 结果，即变换
本身有 `96 gate` 的保守下界。最重输出依赖 7 个输入，二输入 XOR 深度至少为 3，按当前
单比特 XOR 的 `delay=2` 计，纯变换延迟下界为 6。

### 候选 A：66 周期低能耗反馈机

状态机语义：

```text
initialized = 0

tick 0:
    state <- initial_seed
    initialized <- 1
    output_enable = 0

tick 1..65:
    next = F(state)
    output = next
    output_enable = 1
    state <- next
```

推荐拓扑：

1. `com_level_input_switched` 只在 `initialized=0` 时驱动状态写入总线。
2. `com_delay_line_bit` 保存初始化控制位；常量 1 接其输入，反相信号控制首拍输入。
3. 反馈结果通过 `com_switch_word` 或 32 个共享控制的 `com_switch_bit`，仅在初始化后驱动
   状态写入总线。
4. 状态输出进入 61-XOR 固定网络；`y` 同时送反馈开关和
   `com_level_output_switched`。
5. `initialized` 直接作为输出有效控制。首拍不提交结果，随后每拍提交一次，共 65 次。

输入和反馈必须是互斥三态驱动，不能把普通输出直接并到同一网络。控制位要放在数据区外，
并让所有反馈开关共用同一条控制网，避免 32 份反相器。

### 排行榜反推

`examples/leaderboard-current.json` 的当前公开前缀给出：

| gate | delay | cycle | energy |
| ---: | ---: | ---: | ---: |
| 431 | 9 | 66 | 256014 |
| 421 | 10 | 66 | 277860 |
| 381 | 11 | 66 | 276606 |
| 448 | 10 | 65 | 291200 |
| 413 | 11 | 65 | 295295 |
| 381 | 12 | 65 | 297180 |

前三个 66 周期点具有同一功能和相邻延迟，最合理的解释是相同 61-XOR 核心配合不同粒度
的状态存储：

- `431/9`：状态拆成单比特低延迟存储，省去状态输出侧的拆分深度。
- `421/10`：部分分组存储，减少 10 gate，增加一层布线/拆分延迟。
- `381/11`：32 位字状态或最低门数存储，减少 50 gate，增加两层拆分延迟。

这三项是根据分数差值和现代元件成本反推的候选解释，并非从榜首电路文件读取。真实元件
组合必须等项目具备时序模拟或人工游戏验收后确认，文档不得把它写成已验证事实。

`431/9` 的关键路径可以由以下结构解释：状态单比特延迟 1、三层 XOR 共 6、两层输出
拼接共 2，总计 9。它也给出该布局下的延迟下界：若输出必须从单比特状态重新组成 32 位
字，`1 + 6 + 2 = 9` 已经不能再压低。

### 候选 B：65 周期首拍旁路

若要达到周期下界，必须在 tick 0 直接计算 `F(initial_seed)` 并提交，同时在之后切到反馈
状态：

```text
current = initialized ? state : initial_seed
next = F(current)
output = next
state <- next
initialized <- 1
```

公开点 `448/10/65` 是优先参考目标。该路线比榜首多 17 gate、关键路径多 1，却只省一个
周期，能耗反而从 `256014` 上升到 `291200`。因此在拿到可工作的 `431/9/66` 前，不应
投入时间优化首拍旁路。禁止在 32 位数据前直接放通用 `com_mux` 后就宣称完成；应比较
三态输入、分组状态和首拍选择是否能把选择逻辑移出三层 XOR 的公共关键路径。

### 布线风险

- 左移 17 和右移 13、5 的位序最容易接反。所有拆分、拼接都统一声明 `bit 0 = LSB`，
  用上述四组种子做逐拍差分。
- `<< 17` 后必须截断到 32 位；不得把高位接回低位形成旋转。
- 输入端与反馈端共总线时只能有一个有效驱动。首拍边界若两端同时有效，会形成多驱动；
  同时无效则状态写入高阻或零，第二拍立刻失败。
- 输出值应当是 `F(state)`，不是旧 `state`。把状态直接输出会整体错一项。
- 初始化控制和数据状态应同拍更新。控制早一拍会丢失初始种子，晚一拍会重复使用初始
  种子。
- 字状态方案的拆分器位宽、分组顺序和额外延迟必须计入关键路径；不能只计算 XOR 深度。
- 自定义 Foundry 封装若用于复用，递归 gate/delay 必须由游戏重新计分，不能信任头部
  声明值。

### 验证计划

1. 离线时序模型先跑固定四组种子，每组至少 65 项，并检查逐拍输出与 Python 参考函数
   完全一致。
2. 随机抽取非零 32 位种子，验证 65 拍内永不使用未初始化状态，且输出使能恰好脉冲
   65 次。
3. 分别统计 61 个 XOR、状态元件、反馈开关和控制元件；门数差异必须能解释
   `431/421/381` 三个目标，而不是手填头部成绩。
4. 检查所有普通网络单驱动，只有输入/反馈汇合网允许三态驱动，并证明控制互斥。
5. 最终人工验收时保持游戏退出，并直接覆盖正式存档中的目标文件；本次研究不启动游戏。

## 2026-08-01 / 提交：功能：生成随机数 ASIC 候选

### 实现

- 新建 `CODEX-RNG` 的当前 v15 候选：`78` 个元件、`172` 条导线。
- 不使用通用 32 位移位器；三次固定移位完全展开为 `61` 个单比特 XOR。
- 使用一个 U32 `DelayLineWord` 保存状态，首拍由受 `not ready` 控制的 Architecture Level
  Input 写入随机种子；之后由受 `ready` 控制的 U32 Switch 写入反馈结果。二者均为三态输出，
  离线逐拍证明不会在同一 tick 共同驱动状态输入。
- 第一拍关闭 Architecture Level Output，后续连续输出 `F(seed)` 到 `F^65(seed)`；离线模型的
  周期为 `66`，其中有效输出恰好 `65` 次。

### 验证

- v15 编解码往返一致；候选 SHA-256 为
  `55aa28fb1355e2861d957fa4352c1c4384dafaaed17acab5d581d8e38a9864be`。
- 固定种子 `00000001`、`00000002`、`12345678`、`ffffffff` 与额外 64 个确定性随机向量均
  经 66 tick 离线模拟通过；`12345678` 的前三项为
  `996cc1e4`、`880f6ad6`、`dbbba3f8`。
- 连通性检查无未连引脚、非三态多驱动、位宽不匹配或组合环；保守几何检查的元件接触、内部
  引脚接触、元件重叠均为 0。

### 成绩与边界

- 文件头按已核对的元件成本声明为 `413 gate / 11 delay / 66 cycle`。其中门数由
  `U32 Word Delay=160`、`U32 Word Switch=64`、`61 XOR=183`、初始化 bit Delay=`5`
  和控制反相器=`1` 相加得出；`11 delay` 仍待游戏内重新计分确认。
- `Splitter32`/`Maker32` 的引脚顺序已由现有 JALRD0 完整样本交叉验证。U32
  `DelayLineWord` 的 `(-2, 0)` / `(+2, 0)` 端口来自当前组件元数据和同族尺寸规则，但本机
  当前存档没有同类 U32 样本；因此正式写入前仍应由人工在已退出游戏的环境中加载一次确认。

## 2026-08-01 / 提交：修复：校正随机数生成器门数声明

### 修改

- 将候选的文件头和元数据从错误的 `381 gate` 改为 `413 gate`。
- `381/11/66` 保留为公开排行榜参考点，不再被表述为本候选可达到的成绩。

### 依据

- 游戏的组件成本逻辑中，U32 `Word Switch` 为 `2 * 32 = 64` gate，U32
  `Word Delay Line` 为 `5 * 32 = 160` gate。
- 本电路还有 `61` 个 XOR（`183` gate）、一个 bit Delay（`5` gate）和一个 NOT（`1` gate），
  合计 `413` gate。
- 延迟和最终排行榜分数仍以游戏加载后的重新计分为准。

## 2026-08-01 / 提交：优化：逐位状态消除字开关并冲击榜首

### 灵感

公开前沿中的 `381/11/66` 与旧候选 `413/11/66` 正好相差 32 门。结合当前元件成本可反推：
它不是减少了 61 个 XOR，而是用零初态的线性性质替代反馈 Word Switch：

```text
state(0) = 0
tick 0: state <- F(state) OR seed = seed
tick n: state <- F(state) OR 0    = F(state)
```

Architecture Level Input 只在首拍启用；之后其数据值为零。32 个单比特 OR 共 32 门，替代
U32 Switch 的 64 门，所以总门数从 413 降为 381。

进一步把一个 U32 Delay Line 拆成 32 个 Delay Bit。存储门数仍为 `32 * 5 = 160`，但稳定
状态不再经过 `Splitter32 + Splitter8`。输出直接从逐位状态进入三层固定 XOR，再通过两级
Maker 组成 U32；预测关键路径为 `1 + 3 * 2 + 2 = 9`。

### 实现

- 新增 `src/tc_save_lab/rng_bit_state_asic.py`。
- 使用 32 个 Delay Bit、32 个 OR、61 个 XOR、一个初始化 Delay Bit 和一个 NOT。
- 首拍只装载种子且关闭输出；之后连续提交 `F(seed)` 到 `F^65(seed)`，仍为 66 周期。
- 不再使用 Switch，因此不受 2.1.281 中 Switch enable 端口从上侧改到下侧的影响。
- 每一条长反馈线使用独立的左右外侧通道和底部通道，不共享线段，不穿过元件或非目标引脚。

### 验证

- 68 个不同非零 U32 种子均完整验证 65 次连续输出。
- `00000001` 的前三项为 `00021001`、`21211091`、`12828955`。
- v15 编解码往返、全部引脚连通、位宽、多驱动、未驱动网络及组合环检查全部通过。
- 保守矩形审计：元件接触 0、内部引脚接触 0、元件重叠 0。
- 当前安装版 PNG 精灵审计：内部导线碰撞 0、内部引脚接触 0、元件重叠 0；仅保留
  Architecture Input/Output 精灵自身的 11 个合法端口出线格。

### 候选与部署

- 候选 SHA-256：`c47a9dbf6c3ebbe260dcb004ca962ebb7ac28d923c55606e0439748a8bce4c5d`。
- 预测成绩：`381 gate / 9 delay / 66 cycle = 226314 energy`。
- 当前公开榜首：`431 / 9 / 66 = 256014`；若游戏重算确认 9 delay，则降低 29700 energy。
- 已按用户要求在游戏退出时直接覆盖
  `schematics/architecture/CODEX-RNG/circuit.data`，未创建备份，未改动其它关卡。
- `9 delay` 仍必须由 2.1.281 实际加载和运行重算确认；离线工具没有伪造排行榜提交结果。

## 2026-08-01 / 提交：优化：复用初始化拍并生成九延迟编码状态机

### 实测反馈与新目标

- 上一版逐位自然状态电路已由人工验收为 `381 gate / 11 delay / 66 cycle`，并到达排行榜第三。
- 它的关键反馈路径是 `Delay Bit(4) + OR(1) + 3 层 XOR(6) = 11`；先前记录中的
  `9 delay` 只是错误预测。
- 新方案不再削减存储门，而是把三层自然状态递推改写成两层编码状态递推，目标为
  `396 / 9 / 66`。

### 状态重编码

令 `A` 为关卡规定的 xorshift32 变换，并在 GF(2) 上定义：

```text
T = (I + R17)(I + R13)
q = T*x
C = A*T^-1
B = T*A*T^-1
```

于是运行拍满足：

```text
visible = C*q = A*x
q_next  = B*q = T*A*x
```

固定的 `B+C` 联合网络只需 `61` 个二输入 XOR，其中第一层 `27` 个、第二层 `34` 个，
最大 XOR 深度为二。初始化拍也没有另建 `T` 网络，而是给这 61 个物理门重新赋予种子线性标签。

### 47 个双模叶子

每个双模叶子是一个单比特 OR：

```text
mode_pair(seed_i, q_j) = seed_i OR q_j
```

- tick 0：32 个状态 Delay Bit 均为零，OR 输出为 `seed_i`，共享 XOR 网络生成 `T(seed)`。
- steady：Architecture Input 已禁用，其数值为零，OR 输出为 `q_j`，同一网络生成 `B(q)`
  和 `C(q)`。
- 固定 61-XOR DAG 下，Z3 已证明至少需要 47 个不同 `(seed_i, q_j)` 配对；生成器恰好使用
  47 个，没有重复放置相同配对。

### 成本与时序

```text
32 * Delay Bit = 160 gate
61 * XOR       = 183 gate
47 * OR        =  47 gate
ready Delay    =   5 gate
NOT            =   1 gate
总计           = 396 gate

反馈关键路径 = Delay Bit(4) + OR(1) + XOR(2) + XOR(2) = 9 delay
周期         = 1 个装载 tick + 65 个有效输出 = 66 cycle
能量         = 396 * 9 * 66 = 235224
```

公开参考榜首为 `431 / 9 / 66 = 256014`。若游戏端重算与离线拓扑一致，新方案能量低
`20790`，并将超过该参考成绩。

### 实现与版图

- 新增 `src/tc_save_lab/rng_encoded_asic.py`，固化 `A/T/B/C`、61 门 DAG、47 个双模配对和
  确定性组件 ID。
- `architecture_candidates` 中的 `rng` writer 已切换到编码状态版本。
- 候选包含 `155` 个组件、`294` 条导线；47 个 OR 贴近唯一 XOR 消费端放置，避免形成高而
  混乱的中央元件列。
- 每条导线以当前元件实体和全部非目标引脚为障碍独立寻路；允许导线互相交叉，但不会穿过
  元件、元件引脚或其它元件的端口走廊。

### 离线验证

- `C*T=A`、`T*C=B`、`T*T^-1=I` 全部成立。
- 69 个确定性 U32 种子逐个验证：tick 0 精确装载 `T(seed)`，随后 65 个输出和每拍编码状态
  不变量全部正确；种子 `00000001` 的前三项为 `00021001`、`21211091`、`12828955`。
- v15 编解码往返完全一致；479 个引脚全部连接，无多驱动、未驱动网络、位宽错误或组合环。
- 保守矩形审计为元件接触 0、内部引脚接触 0、元件重叠 0。
- 当前安装版 PNG alpha 审计为未知精灵 0、元件重叠 0、内部穿线 0、内部引脚接触 0；另有
  7 个 Architecture I/O 面板端口合法出入格。
- RNG 专项 4 项测试及 codec、pins、simulate、architecture registry 共 27 项回归全部通过。

### 候选与直接部署

- 候选及正式文件 SHA-256：
  `844f4a950499cd8823bcee48dffafc4474e3a176225716c65f5d87403026d0c1`。
- 已在确认游戏未运行后直接覆盖
  `schematics/architecture/CODEX-RNG/circuit.data`，未创建备份，未改动其它存档文件。
- 文件头为 `396 gate / 9 delay`，但排行榜指标和 65 个关卡测例仍必须以游戏端重新编译、运行
  和提交后的结果为准。

## 2026-08-02 / 提交：优化：以一位字异或压缩反馈网络

### 灵感

实机已经确认上一版编码状态机实际为 `396 / 10 / 66`。当前 RNG 关卡中，Bit XOR 的成本
是 `3 gate / 2 delay`；将 Word XOR 配置为 `word_size=1` 后，同样执行一位异或，但成本为
`1 gate / 3 delay`。因此不能把 61 个 XOR 无差别替换，否则会抬高可见输出关键路径。

对联合 `B/C` DAG 做精确静态时序约束后，找到 19 个满足以下条件的第二层节点：

- 只参与反馈变换 `B`，不参与可见输出变换 `C`；
- 替换后反馈路径最多从 9 增至 10，而当前可见输出路径仍为 10；
- 每个节点节省 2 gate，合计节省 38 gate。

这 19 个节点按逻辑行和稳定 `permanent_id` 双重固化，生成器不会按元件顺序猜测替换目标。
控制协议同时恢复为实机验证过的全零初态：常量 1 驱动 `ready` Delay Bit，`NOT ready`
只在 tick 0 开启种子输入，`ready` 从 tick 1 起开启唯一输出。33 个 Delay Bit 的
`init_data` 全部为 0，不再使用已经被实机否定的 `init_data=1` 首拍假设。

### 成本

```text
32 * Delay Bit = 160 gate
42 * Bit XOR   = 126 gate
19 * U1 Word XOR = 19 gate
47 * OR        =  47 gate
ready Delay    =   5 gate
NOT            =   1 gate
总计           = 358 gate

预测成绩       = 358 / 10 / 66
预测能量       = 236280
公开榜首参考   = 431 / 9 / 66 = 256014
```

### 验证与部署

- 69 个确定性 U32 种子各验证 65 个连续输出，共 4485 项，全部与 `xorshift32` 一致；
- 479 个引脚全部连接，无多驱动、未驱动网络、位宽错误或组合环；
- 元件重叠、内部导线穿元件、导线穿非目标引脚均为 0；
- Bit XOR 与 Word XOR 的当前 PNG 实体占格完全相同，替换不改变任何导线或元件位置；
- 项目候选 SHA-256 为
  `303bcbc20e18a0d987ae43915ff7ac248f0c0c3b33396e3e8cc53047d108e337`；
- 相同组件与导线拓扑已直接写入正式 `CODEX-RNG`，没有创建备份；游戏保留的
  `custom_id/design` 和旧计分缓存会使正式文件字节哈希不同，最终成绩仍以实机重新编译为准。

另已离线生成 `304 / 11 / 66` 与 `274 / 12 / 66` 两个更激进的 U1 Word XOR 前沿，二者
同样通过 4485 项输出和几何审计。为保证每轮人工反馈可归因，本次先验收 `358 / 10 / 66`，
后续再按实测决定是否覆盖更激进版本。

## 2026-08-02 / 提交：赛后：归档随机数生成器榜一方案

> **漏洞风险声明**：本方案依赖 RAM 的隐藏 `settings[0]=2`，利用了当前版本中
> “非零模式按流水 RAM 计分”与“仅模式 `1` 启用实际流水延迟”之间的不一致，从而得到
> 零运行延迟的 RAM。Turing Complete 2.1.281 客户端与排行榜服务端目前均已重算并接受
> `245 / 7 / 66`，但这很可能是引擎漏洞，而不是应长期有效的常规元件语义。官方修复、
> 回滚或取消该成绩都属于合理结果；复现者不应把它当作跨版本稳定方案。

### 最终结果

Turing Complete 2.1.281 实机完成全部 `256/256` 测例，并由用户确认成功进入
[RNG 排行榜](https://turingcomplete.game/leaderboard/rng)第一名：

```text
gate    245
delay     7
cycle    66
energy 113190
```

此前公开参考榜首为 `431 / 9 / 66 = 256014`，本方案降低 `142824 energy`，降幅约
`55.79%`。上一份稳定实测基线为 `396 / 10 / 66 = 261360`，本方案降低
`148170 energy`。

游戏完成测试和提交后自动重写的正式文件已原样保存为
`examples/rng/candidate/circuit.data`：

```text
SHA-256   15E1BAA8D2AC7599592417534CED3E4583B87D9823D80AB6B509DEE47AE8F069
format    v15
components 122
wires      278
immutable    0
cost override 0
```

该文件是本次赛后分析的最高级证据。生成器、文件头预测、静态反编译和注释若与它冲突，
一律以实机重新编译、全测和服务端接受的结果为准。

### 决定性结构

最终电路回到自然状态，不再使用编码状态的 47 个 `(seed_i, q_j)` 双模叶子：

```text
state(0) = 0

tick 0:
    next  = F(state) OR seed = seed
    state <- next
    output disabled

tick 1..65:
    next   = F(state) OR 0 = F(state)
    output = F(state)
    state  <- next
```

其中 `F` 是关卡规定的 xorshift32：

```text
t1 = x  XOR (x  >> 13)
t2 = t1 XOR (t1 << 17)
y  = t2 XOR (t2 >> 5)
```

固定移位展开后恰有 `19 + 15 + 27 = 61` 个二输入 XOR。首拍依赖状态全零，把种子放在
三层 XOR 之后用 32 个 OR 注入；后续 Architecture Input 关闭，其高阻输出按运行时 Wire
值零参与 OR。这样无需 47 个编码态配对，也无需通用 Word Switch。

32 位状态由一个原生 RAM 组保存：

- `kind 118`：pinless backing RAM，`word_size=8`、`buffer_size=8`、
  `settings=(2,512,0)`；
- `kind 54`：U32 Load；
- `kind 56`：U32 Store；
- 两个端口按空间位置绑定到 backing RAM，不能把 kind 118 误画成独立五端口元件；
- 地址恒为 0，Load 和 Store 始终启用；
- 同一 tick 的拓扑顺序严格为“读旧值、计算下一状态、写新值”。

隐藏模式 `2` 是版本相关但由游戏本身接受、保存、执行和服务端重算的状态：计分路径把非零
模式按流水 RAM 计，运行时只有模式严格等于 `1` 才启用流水深度，所以模式 `2` 的实际深度
为 0。它没有修改元件成本、`immutable`、关卡进度、认证或网络请求。

### 实机成本账本

赛前静态审计曾错误推导 `word_size=1` 的 Word XOR 为 `1 gate`。游戏重算给出了反例：
61 个 U1 Word XOR 仍各收取 `3 gate / 2 delay`，与 Bit XOR 相同。最终 245 门应按实机账本
解释：

```text
32 * OR Bit                         =  32 gate
61 * U1 Word XOR @ 3                = 183 gate
ready Delay Bit + NOT               =   6 gate
RAM backing, buffer_size=8          =   8 gate
U32 RAM Load                        =   8 gate
U32 RAM Store                       =   8 gate
                                        --------
total                               = 245 gate
```

因此 U1 Word XOR 替换本身没有节省门数。真正的门数突破来自把 32 个 Delay Bit 的
`160 gate` 状态存储替换成总计 `24 gate` 的 RAM backing/load/store 组。

实际关键路径为：

```text
状态路径：RAM Load 0 + XOR 2 + XOR 2 + XOR 2 + OR 1 + Store 0 = 7
种子路径：ready Delay 4 + NOT 1 + Architecture Input 0 + OR 1 = 6
```

所以全局 delay 为 7。首拍只装载，随后提交 65 个输出，cycle 为 66。

### 生成文件与游戏文件

`archive/rng_natural_ram_u1/candidate/circuit.data` 是赛前生成器输出：

```text
SHA-256 D50ED4F56367270BCBEF35E1309949820F6118C912FDF2CDE00230FD52A66A27
header  123 / 7
```

它和实机榜一文件的 122 个组件、278 条导线逐项完全相同。差异只有：

- 游戏分配了正式 `custom_id`；
- 游戏把错误的 `123 gate` 重新计算为 `245 gate`；
- 游戏写入 512 字节 `design` 缓存；
- 游戏使用更紧凑的 v15 数值编码，所以原始文件从 12639 字节缩为 4455 字节。

这解释了为什么两个文件字节哈希不同而电路相同。项目最终候选保存实机原始字节，整理后的
`rng_natural_ram_asic.py` 则生成 header 已修正为 `245 / 7` 的规范编码文件。

### 离线验证

最终生成拓扑完成了以下验证：

- 按关卡同一公式生成 256 个运行时种子；
- 每个种子模拟 66 tick，共 16896 tick；
- tick 0 没有输出，tick 1..65 与参考 xorshift32 完全一致；
- RAM 状态每拍与期望自然状态一致；
- 459 个已建模引脚全部连接；
- 多驱动、未驱动、无接收端、位宽不匹配和组合环均为 0；
- 内部导线穿元件为 0，导线穿非目标引脚为 0；
- Architecture I/O 出线走廊和 RAM load/store/backing 的实体重叠均按当前精灵单独审计。

之后由实机完成 256/256 测例，并由服务端重算接受。离线验证用于提前排错，不能替代这两个
最终步骤。

### 关键研究路径

本轮探索并非线性前进，主要分为以下路线：

1. 自然状态 61-XOR 网络，先得到 `381 / 11 / 66` 并实测排行榜第三。
2. GF(2) 状态重编码，把 B/C 联合网络压为两层并使用 47 个双模 OR，稳定实测
   `396 / 10 / 66`。
3. 普通 XOR2/XOR3 两层网络做 SAT/MaxSAT 下界搜索；多个 `<=233 gate` 原子模型为 UNSAT，
   说明仅在同一普通元件模型内继续挤 XOR 不足以跨越目标。
4. 联合状态重编码、42 状态嵌入、共享叶子、特征多项式、65 周期、Switch 三态总线和
   样本特化均做过独立搜索；它们提供边界或失败证书，但没有成为最终电路。
5. 对当前 EXE 的 RAM 计分、反序列化、UI、pipeline depth、preorder 和同拍 load/store
   顺序做静态审计，发现模式 `2` 的计分/运行分支差异。
6. 先后排除错误的单体 RAM `202/6`、`240/10` 模型，改用真实的
   `kind 118 + kind 54 + kind 56` 原生组。
7. 将自然状态的 32 个 Delay Bit 替换成 RAM8，并保留 32 个种子 OR，得到最终拓扑。

完整脚本、证书、反编译证据和候选保存在 `examples/rng/research/archive/`，文件级索引见
`archive-manifest.json`。

### 明确排除的错误路线

- 通过 `immutable`、`cost_gate`、文件头、`levels.txt` 或本地响应伪造分数：服务端会重算，
  不属于有效结果。
- `335/10/66` 和 `387/10/66` 的本地伪计分版本：已撤回，不能作为排行榜成果。
- 把 `kind 118` 当作带 load/save/address/in/out 的单体 RAM：当前引擎不会按该模型执行。
- 在输入未 enable 时提前打开输出：首个结果读成 0，实机已证明失败。
- 只做矩阵证明而不模拟实际 wire graph：无法发现接错端口、穿引脚或读写顺序错误。
- 假定 U1 Word XOR 是 `1 gate`：实机最终结果精确证明其当前成本为 3 gate。

### 版本边界与复现注意

- 本结论针对 Turing Complete 2.1.281；引擎更新后必须重新审计 RAM mode、端口位置和成本。
- 不要点击 RAM 模式下拉框。UI 只列出 0/1，点击会把隐藏的 `settings[0]=2` 覆盖掉；仅打开
  属性面板不会改值。
- 复制候选后必须让游戏重新计算，不得相信文件头声明。
- 导线允许互相交叉，但不能穿过元件实体、非目标引脚或错误的端口走廊。
- 最终复现应依次检查结构、离线 16896 tick、游戏 256/256 和排行榜服务端结果。

### 赛后待研究问题

榜一已经取得，后续研究应从“继续试错”切换为可证伪分析：

- 解释 Word XOR 在静态反推和实机成本间的精确分支差异，并修正统一成本模型；
- 验证 `buffer_size=4` 是否能以当前原生 RAM 组稳定保存和服务端执行，理论上还能减少
  backing/load/store 成本；
- 研究能否把 32 个种子 OR 与 xorshift 最后一层做布尔联合综合，而不增加 7-delay 关键路径；
- 给 mode 2 建立跨版本探针，检测反序列化、UI 保存、计分和运行语义是否变化；
- 将完整生成器从研究脚本收敛为稳定公共 API，并让候选元数据区分“预测”“实机”“服务端”
  三种证据等级。

## 2026-08-02 / 提交：修复：统一异或元件成本模型

### 实机成本结论

后续无 RAM 开关解搜索统一采用游戏实测成本，不再按元件封装名称推测折扣：

| 元件 | gate | delay |
| --- | ---: | ---: |
| 普通 XOR / U1 Word XOR | 3 | 2 |
| U2 Word XOR | 6 | 2 |
| U3 Word XOR | 9 | 2 |
| U4 Word XOR | 12 | 2 |
| U8 Word XOR | 24 | 2 |

因此不存在合法的“U1 Word XOR 为 1 gate”候选。任何按位宽打包只改变表示方式，成本仍为
`3 * width`，延迟均为 2。

### 代码修正

- 将 `rng_encoded_asic.py` 的非 RAM 实测基线从错误的 `358 / 10 / 66` 修正为
  `396 / 10 / 66`。
- 保留已验证电路中的 19 个 U1 Word XOR 表示，但明确其与 42 个 Bit XOR 都按 `3 / 2`
  计费；61 个 XOR 的总成本为 `183 gate`。
- 元数据新增显式 `xor_cost_model` 与 `xor_gate_cost`，防止后续生成器再次从元件类型推导
  虚假优惠。

### 验证

`tests.test_rng_encoded_asic` 的 4 项专项测试全部通过，包括 69 个确定性种子的完整 66 拍
仿真、矩阵恒等式、v15 编解码、连通性和几何检查。测试耗时 78.156 秒；本次没有启动游戏，
也没有写入正式存档。

## 2026-08-02 / 提交：研究：收口随机数开关编码前沿

### 成本口径

本轮所有求解器统一使用实机成本：标量 XOR2 与 U1 Word XOR 都是 `3 gate / 2 delay`；
U2、U3、U4、U8 Word XOR 分别为 `6/2`、`9/2`、`12/2`、`24/2`。这里的 U3 Word
XOR 是三位宽的两个字逐位异或，不等于三个标量输入的奇偶宏；后者在当前已复核的
Switch 构型中仍为 `12/2`。

### 66 周期 radius 14 完整审计

对 `radius14-x56-neighbors.jsonl` 的 128 个唯一状态编码做了两层审计：

1. 完整枚举 386 个最小 pair cover，Glucose4 与 CaDiCaL 对
   `3 * XOR2 + OR <= 221` 均返回 UNSAT；
2. 在单一 CNF 中放开任意真正有用的非最小 pair、B weight-1/2 mediated 实现、
   XOR 引脚方向、首拍标签和 OR 并集，最终仍为
   `128 UNSAT / 0 SAT / 0 UNKNOWN`。

第二层审计消除了“非最小 cover 可能以更多 XOR 换取更少 OR”的缺口。它对应的完整目标是
`<=387 / 10 / 66`，足以严格超过公开参考 `431 / 9 / 66`，但这 128 个编码内不存在证书。

### 65 周期首拍输出审计

单输出协议要求连续提交 `F(seed)..F^65(seed)`。一次性装载方案在 tick 0 必须同时满足：

```text
output_0 = A * seed
state_0  = T * A * seed
```

不能只存 `T*seed`，否则下一拍会重复输出 `A*seed`。在固定 166 门状态/控制壳下，
`393 / 10 / 65` 的逻辑预算为 `3 * XOR2 + OR <= 227`。radius 14 的 128 个 x56 编码、
386 个完整最小 cover 在这个正确双目标下仍全部 UNSAT，无枚举截断。

另对标准三段 xorshift 的四种循环重定时做了真实晚 OR 位点审计。最优的 `T=R13`
固定 61-XOR DAG 在完整 32 个 seed 列上的逐列 OR 下界总和为 202，远超允许的 44；
因此不能把已有 `392 / 10 / 66` 的单目标证书直接改成 65 周期候选。要继续走 65 周期，
必须改变共享 DAG、采用混合三态奇偶结构，或研究持续 seed 的仿射状态协议。

### 证据边界

关键脚本和结果分别位于：

```text
.research/rng_x56_or10/
.research/rng_x56_or10_exact/
.research/rng_65cycle_dualmode/
.research/rng_deep_xor_dag/
```

这些结果严格排除的是已声明的状态编码和拓扑族，不是所有无 RAM 开关电路的全局不可能性
证明。本轮没有启动游戏、没有读取或修改正式存档，也没有把任何未完成候选写入游戏文件。

## 2026-08-03 / 提交：研究：穷举六十一异或开关相位覆盖

### 成本复核

本轮继续严格使用逐位成本：普通 XOR 与 U1 Word XOR 均为 `3 gate / 2 delay`；
U2、U3、U4、U8 Word XOR 分别为 `6/2`、`9/2`、`12/2`、`24/2`。
Word 封装没有门数折扣。因此 66 周期 U32 Word Switch 外壳的账本为：

```text
gate = 230 + 3*x + 2*k
delay = 9
cycles = 66
```

其中 `x` 是逐位二输入 XOR 数，`k` 是额外 Bit Switch 数。x61 只有在 `k<=8`
时严格超过 `431/9/66`，`k=9` 仅追平。

### 全 cover 结果

从 radius-7 中全部 145 个 direct/pair-clean x61 状态基出发，完整枚举了 427 个
pair cover 和 427 个 B 分解；共访问 `8,050,797` 个 cover 搜索状态，截断数为零。
随后在允许 Word lane 扇出的宽松相位模型中逐个求解 `k<=9`：

```text
427 / 427 UNSAT
SAT = 0
UNKNOWN = 0
```

所以这一完整范围内既没有 x61 严格超榜解，也没有 x61 追平解。严格双射 lane 映射是
宽松模型的子集，同样被排除。

radius-7 的 x60、x59 全集还分别至少有 1、2 个 `direct_target_not_unit` 冲突；
直通反馈位无法由 Word lane 或最终 Bit Switch 生成非单位首拍标签。因此这 4,329 个
x60 基和 842 个 x59 基也不能靠更换 cover 修复。该结论只覆盖 radius-7 与当前
U32 Word Switch + late Bit Switch 拓扑，不是所有开关电路的全局不可能性证明。

### 证据与验证

脚本、全量 cover、逐项结果和中文结论位于
`.research/rng_65cycle_switch/`。`x61-all-covers-fanout-k9.jsonl` 的 427 行已逐行
解析并确认索引连续、状态全为 UNSAT；5 个 Python 脚本均通过 `py_compile`。本轮没有
启动游戏、没有访问存档、没有使用 RAM，也没有生成不可验证的候选电路。

## 2026-08-03 / 提交：优化：以零初态双位相位机实现九延迟

### 灵感与错误路线撤回

最直接的降延迟设想是把原 `ready` Delay 初始化为 1，以其正相直接开启输入，再用 NOT
开启输出。离线模拟器会把这个结构算成 `396 / 9 / 66`，但既有实机证据已经证明最新版
不会把 Delay Bit 的 `init_data=1` 作为首拍高电平。该结构在实机首拍关闭输入、开启输出并
提交 0，因此已经在 `.research/rng_switch_algebra/` 中明确标记为 `RETRACTED`，没有写入
正式存档。

正确方案把“所有 Delay 首拍均为 0”作为硬约束，不再强求第 0 拍读取种子。关卡脚本没有
总拍数超时，只在唯一 Architecture Output 开启时检查一次结果；因此可以增加一个空闲拍，
换取正相 Delay 直接控制输入，并把关键路径中的 NOT 去掉。

### 两位控制状态机

令 `p` 为种子装载脉冲、`o` 为输出开启状态，两者都是 `init_data=0` 的 Delay Bit：

```text
u      = p OR o
p.next = NOT u
o.next = u

Architecture Input.control  = p
Architecture Output.control = o
```

状态轨迹完全确定：

| tick | `(p,o)` | 输入 | 输出 | 数据动作 |
| ---: | :---: | :---: | :---: | --- |
| 0 | `00` | 关 | 关 | 空转，32 位编码状态保持 0 |
| 1 | `10` | 开 | 关 | 唯一一次读取 seed，捕获 `T(seed)` |
| 2..66 | `01` | 关 | 开 | 依次提交 `F(seed)..F^65(seed)` |

`01` 是稳定状态；即使异常进入 `11`，下一拍也会回到 `01`。两个 Delay 加 OR、NOT 共
`12 gate`。一寄存器无法产生 `0,1,0,0...` 脉冲；对常量、直连、NOT、AND、OR、NOR、
NAND 的两寄存器枚举也确认一门组合逻辑无解，所以该小控制器在此元件集合内达到门数下界。

### 成本、时序与周期

数据面继续使用已经实机验证过的 32 位编码状态、47 个双模 OR 和 61 个两层 XOR：

```text
32 * Delay Bit                   = 160 gate
61 * XOR @ 3                     = 183 gate
47 * mode OR                     =  47 gate
2 * phase Delay + OR + NOT       =  12 gate
                                     --------
total                            = 402 gate

load/steady critical path
= Delay 4 + mode OR 1 + XOR 2 + XOR 2
= 9 delay

cycles = 1 idle + 1 load + 65 output = 67
energy = 402 * 9 * 67 = 242406
```

公开参考 `431 / 9 / 66 = 256014`，所以游戏若按当前实测成本重算，本候选低 `13608`
能耗。现有固定 47-mode 映射若只把临界 OR 换成 Switch，需要处理 42 个临界配对，至少为
`438 / 9 / 66`，反而高于参考；双位控制状态机是当前固定数据面的更强解。

### 离线验证与部署

- 候选包含 `156` 个元件、`297` 条导线，34 个 Delay 的初态全部为 0；
- 两套独立验证均覆盖 256 个种子、每个 67 拍，共 17152 拍和 16640 次输出；
- 前两拍均无输出，后 65 项严格匹配 `F(seed)..F^65(seed)`；
- `C*T=A`、`T*C=B`、`T*T^-1=I` 与每拍编码状态不变量全部成立；
- 483/483 引脚连接，多驱动、未驱动、位宽错误、组合环和三态冲突均为 0；
- 保守矩形与当前 PNG alpha 审计均为元件重叠 0、内部穿线 0、穿引脚 0；
- v15 规范编码往返一致，专项 4 项单元测试全部通过。

项目候选：

```text
examples/rng/candidate/circuit.data
SHA-256 21f638b968ef9f2c3a2601fd46de1624e81e0a4e1198c39f5c6eff1ba566aa93
```

在确认游戏未运行后，已直接覆盖正式 `schematics/architecture/CODEX-RNG/circuit.data`，
保留原电路级 `custom_id/design`，未创建备份、未改 `levels.txt` 计分缓存。正式文件 SHA-256
为 `14de5a03222fe59a74022e67ac0fc29691f38c04279397aba4bc2b6ea66ee282`。最终成绩仍以游戏
本体 256/256 测例和服务端重算为准。
