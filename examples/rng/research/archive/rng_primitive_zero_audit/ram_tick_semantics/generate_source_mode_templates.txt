## TM__THWBxVSaWN2Zh7OMooFH0w_508 @ 0x140a25b40
'}\n\nvar level_input = Input {}\nvar level_output = Output {}\n\ndef mode_refresh() {\n\n    '

## TM__THWBxVSaWN2Zh7OMooFH0w_2426 @ 0x140a2c5b0
"\n}\n\ndef mode_run(target_tick: Int) {\n\n    if get_setting(sim_test_result) == U64 win { return }\n    if (get_setting(sim_test_result) == U64 fail && .tick + 1 >= #TICK_PAST_FAIL) { return }\n    if get_setting(sim_short_circuit_component_id_1) != 0 { return }\n\n    var halt_run = false\n\n    var original_start_time = Time 0\n    var orig_start_tick = U64 .tick\n    var last_time = original_start_time\n\n    var burst_ticks = 1\n\n    var nanos_per_tick = U64 0\n\n    var i = 0\n    var last_target_speed = U64 0\n    var speed = U64 1\n\n    while get_command(ctl_command_id) == .last_command_id && !halt_run && target_tick > .tick {\n        i += 1\n\n        let target_speed = max(1, get_command(ctl_tick_speed_ms))\n\n        if target_speed != last_target_speed {\n\n            original_start_time = get_time()\n            orig_start_tick = U64 .tick\n            last_time = original_start_time\n\n            speed = min(U64 get_command(ctl_tick_speed_ms), 10000000000000) // Cap at 10 ghz to avoid overflow\n            last_target_speed = target_speed\n\n        }\n\n        let ticks_at_start = .tick\n        let burst_target = .tick + burst_ticks\n\n        var burst_target_tick = min(target_tick, burst_target)\n\n        def halt() {\n\n            .burst_target_tick = .tick\n            .halt_run = true\n\n        }\n\n        def handle_test_result(result: TestResult) {\n\n            if result != pass {\n\n                var res = result\n\n                halt() // Halt regardless of wether we are above '#TICK_PAST_FAIL'\n\n                set_setting(sim_test_result, max(get_setting(sim_test_result), U64 res))\n\n            }\n\n        }\n\n        while .tick < burst_target_tick {\n\n"

## TM__THWBxVSaWN2Zh7OMooFH0w_2521 @ 0x140a2d3b0
'\n            .tick += 1 // Do this late as it signals to the front end that it can update\n\n        }\n\n        set_setting(sim_tick, U64 .tick)\n\n        let time_now = get_time()\n\n        let time_diff = U64 (time_now - last_time)\n\n        nanos_per_tick = time_diff / U64 burst_ticks\n\n        if time_now - last_time > 33.ms() {\n\n            // Burst took too long\n            burst_ticks = max(1, burst_ticks - 1)\n\n        } else {\n\n            // Burst was too fast\n            burst_ticks = burst_ticks + 1\n\n        }\n\n        #if in_scope(on_ui_update){\n\n            on_ui_update(.level_input, .level_output)\n\n        }\n\n        last_time = time_now\n\n\n        def get_target_tick(time_now: Time) U64 {\n            let time_diff = U64 (time_now - .original_start_time)\n            return .orig_start_tick + ((time_diff / 1000000) * .speed) / 1000000 // Double divide to prevent overflow\n        }\n\n        var run_target_tick = get_target_tick(time_now)\n\n        // Sleep any ahead of time away\n        while .tick > Int run_target_tick && get_command(ctl_command_id) == .last_command_id {\n\n            sleep(1.ms())\n            .tick -= 1\n            mode_refresh()\n            .tick += 1\n\n            run_target_tick = get_target_tick(get_time())\n\n        }\n\n    }\n\n}\n\nvar manual_input_seen = false\n\nrun_sim: while true {\n\n    reset_sim()\n\n    '

## TM__THWBxVSaWN2Zh7OMooFH0w_2525 @ 0x140a2da60
'\n    while true {\n\n        mode_refresh()\n\n        #if in_scope(on_ui_update){\n\n            on_ui_update(.level_input, .level_output)\n\n        }\n\n        while get_command(ctl_command_id) == last_command_id {\n\n            #if in_scope(on_manual_input){\n\n                if last_level_manual_input != get_command(ctl_level_manual_input_id) {\n\n                    last_level_manual_input = get_command(ctl_level_manual_input_id)\n                    on_manual_input(Int get_command(ctl_level_manual_input))\n                    manual_input_seen = true\n\n                }\n\n            }\n\n            sleep(1.ms())\n\n        }\n\n        if manual_input_seen {\n\n            manual_input_seen = false\n            break\n\n        }\n\n        last_command_id = get_command(ctl_command_id) // Read this before the command in case of race condition\n        set_setting(sim_last_command_id, last_command_id)\n        var command = SimCommand get_command(ctl_command)\n\n        switch command\n\n            run {\n\n                var target_tick = Int get_setting(sim_target_tick)\n\n                set_setting(sim_running, U64 1)\n                mode_run(target_tick)\n                set_setting(sim_running, U64 0)\n\n            }\n\n            refresh {}\n\n            mode_reset {\n\n                var i = 0\n                while i < 1024 {\n                    input_replay[i] = ctl_input_replay_reset[i]\n                    i += 1\n                }\n\n                break\n\n            }\n\n            quit_simulation {\n\n                break run_sim // We need to break out instead of thread_exit() to make sure everything is cleaned up\n\n            }\n\n    }\n\n}\n\n'
