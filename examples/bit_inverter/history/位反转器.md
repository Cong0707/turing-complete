# 位反转器

## 2026-07-31 / 提交：功能：新增位反转器与一位解码器候选

## 灵感

该关卡的两个输入分别是被翻转的位和控制位，输出真值表为 `0,1,1,0`，正好是 XOR。
直接使用一个二输入 XOR，避免原方案中两个 NOT 加一个 XOR 的串联路径。

## 离线验证

- 1 个 XOR 元件，声明 `3 gate / 2 delay / 6 energy`。
- 4 个输入向量全部通过模拟器穷举。
- 所有固定端口连接且没有组合环；真实可用性仍须游戏验收。

## 2026-08-01 / 提交：部署：直接写入榜首候选

- 游戏退出后，已将单 XOR 的 `3 gate / 2 delay` 候选直接覆盖正式 `Default/circuit.data`。
- 再次穷举 4 组输入并检查当前精灵几何，导线不穿越元件体或非端点引脚。
- 未创建备份、临时文件或额外架构槽位。
