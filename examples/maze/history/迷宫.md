# 迷宫

## 2026-08-01 / 提交：修复：同步迷宫运行时八位输入

### 运行时证据

候选首次写入正式存档时，架构输入声明为 `word_size=1`。游戏 `2.1.281` 成功加载后，
将同一个 `CODEX-MAZE/circuit.data` 原位保存为 `word_size=8`；除此字段外，13 个元件、
14 条导线、永久 ID、门数和延迟均未改变。运行时文件 SHA-256 为：

```text
349e3170dcd03daf70128269ff2c9c87dcdb90cbf23ca4b875307c80138e9eb8
```

### 修改点

- 架构输入按游戏规范改为 8 位，不再让后续部署把游戏保存的正确接口覆盖回 1 位。
- 保留输入总线直接连接一位 NOR 的低位截断。最新版游戏会接受并保存该连接，因此离线
  连通性报告中的一条 `U8 -> U1` 位宽差异是有意结构，不再作为生成失败条件。
- 重新生成无身份的项目候选；直接安装时保留游戏分配的架构 `custom_id` 和 512 字节
  `design`。因此项目候选 SHA-256 为 `4ba2a9e1...`，带当前游戏身份的正式文件仍为
  `349e3170...`，二者的逻辑、布局和运行接口完全一致。

## 2026-08-01 / 提交：功能：重制迷宫榜首 ASIC

### 灵感

最新版关卡的 `test.si` 明确规定输入只有“前方是否为墙”，输出只有左转、前进、右转。
因此不再修复或优化 Overture、LEG 或任何通用处理器，而是直接综合控制机器人的最小状态机。

对 1、2、3 状态 Mealy 机穷举后，2 状态已经可以通过全部 8 张迷宫。其中与当前公开榜首
`6 门 / 5 延迟 / 373 周期` 完全一致的状态转移为：

| 当前状态 `q` | 前方 `wall` | 动作 | 下一状态 |
| ---: | ---: | --- | ---: |
| 0 | 0 | 前进 | 1 |
| 0 | 1 | 左转 | 0 |
| 1 | 0/1 | 右转 | 0 |

化简后只有两条等式：

```text
q_next = NOR(q, wall)
command = {q, q_next}
```

这等价于：遇到墙时持续左转；找到空路后前进一步；下一拍右转，重新优先贴左侧搜索。

### 修改点

- 新增独立的现代 v15 架构候选，不读取旧 Overture/LEG 电路实现。
- 使用一个 `NOR` 和一个位延迟线；位拼接器、常零、架构端口不增加门数。
- 补齐最新版架构切换输入、切换输出和位延迟线的真实引脚位置。
- 内置 8 张当前迷宫并逐张离线模拟，周期为
  `347, 161, 263, 207, 167, 373, 325, 123`。
- 生成目标为 `schematics/architecture/CODEX-MAZE/circuit.data`，当前阶段只写项目候选，
  不写正式存档。

### 验证

- 声明成绩：`6 门 / 5 延迟 / 373 周期 / 11190 能耗`。
- v15 编码后完整反解析与原对象一致。
- 所有引脚已连接，无多驱动、位宽冲突或未切断的组合环。
- 未启动游戏，最终成绩仍需用户在最新版游戏中人工验收。
