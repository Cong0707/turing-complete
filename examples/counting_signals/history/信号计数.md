# 信号计数

## 2026-08-01 / 提交：功能：加入信号计数ASIC候选

## 关卡边界

当前版本的 `test.si` 穷举四位输入 `0..15`，要求三位输出等于四个输入中高电平的数量。
`meta.txt` 同时明确设置 `no_score = true`，因此本关没有官网排行榜成绩；本次只建立现代
v15 专用组合电路，不为不存在的榜单继续消耗搜索时间。

## 灵感

把四位人口计数拆成四个可共享进位的低门数半加器。每个半加器使用：

```text
Carry  = X AND Y
Neither = X NOR Y
Sum    = Carry NOR Neither
```

令 `(p, g) = HA(a, b)`、`(q, h) = HA(c, d)`，再计算：

```text
(bit0, r) = HA(p, q)
(x, bit2) = HA(g, h)
bit1 = x OR r
```

当 `r = 1` 时两对输入都恰好有一个高电平，必有 `g = h = 0`；当 `x = 1` 时只有一对
输入同时为高，必有 `r = 0`。因此中间位可直接使用一门 OR，不需要再构造 XOR。

## 修改点

- 补齐最新版 `com_level_output_counter` 的三个输入脚，依次表示权重 `1、2、4`。
- 新候选使用 4 个三门半加器结构和 1 个 OR，共 13 个可计费逻辑门。
- 候选直接连接关卡固定输入输出，不依赖 OVERTRUE、LEG、Overture 或通用处理器。

## 离线验证

- 穷举 `16` 组四位输入，全部满足 `Output = Input.bit_count()`。
- `46` 个引脚全部连接，无多驱动、位宽冲突和组合环。
- 声明目标为 `13 gate / 4 delay / 52 energy`；原存档基线为 `34/8/272`。
- 项目只生成 `examples/counting_signals/candidate/circuit.data`，未启动游戏，未写正式存档。
