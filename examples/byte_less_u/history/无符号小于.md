# 无符号小于

## 2026-08-01 / 提交：功能：新增六个原生字节元件候选

## 灵感

本批先实现低延迟方向：一个 8 位 Unsigned Less 直接计算 `A < B`，目标是公开样本的
`90 gate / 4 delay / 360 energy`。全球最低能耗点为 `41/6/246`，属于另一条 Pareto 路径，
后续应作为独立候选保留，不能覆盖本方案。

## 修改点

- A、B 对称接入原生无符号比较器。
- 单比特结果直连固定 Result。
- 不沿用当前 `120/98/11760` 基线中的减法器结构。

## 离线验证

穷举 65536 对字节，全部满足 Python 无符号整数比较；所有引脚连接，无组合环。真实成绩
仍须游戏验收。
