# 字节取模

## 2026-08-01 / 待提交

### 当前版本原生基线

- 静态读取 `Turing Complete.exe` 的序列化组件枚举，确认当前 2.1.281 中 `com_mod` 的 `kind` 为 `108`；对应精灵为 `com_mod.png`。
- 读取 `campaign/byte_mod/test.si` 并逐 tick 重放。65536 次测试只覆盖 32768 个不同的 `(A, B)` 对，且每对都满足位奇偶约束 `0xb679`。
- 对该受限域逐位进行低内存逻辑最小化试验后，最低位仍需 1179 项积和式，不能形成比原生 8 位模块更小、可审计的基础门候选。因此保留一份直接使用原生 `com_mod` 的并列榜首基线。
- 候选使用原关卡 I/O、一个 U8 `com_mod`，并在离线验证中覆盖：65536 个完整 U8 组合、65536 个实际脚本 tick、v15 往返、端口连通性、以及当前 `com_mod.png` 的 alpha 精灵几何。
- 公开榜单参考为 `428 gates / 34 delay / 14552 energy`。该文件是研究候选，未加入自动直写名单，需游戏内实际评分确认后才允许部署。
